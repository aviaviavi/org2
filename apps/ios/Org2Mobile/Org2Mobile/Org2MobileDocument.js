// Generated by tools/build-mobile-document.mjs. Do not edit.
"use strict";
var Org2MobileDocument = (() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // src/mobileDocument.ts
  var mobileDocument_exports = {};
  __export(mobileDocument_exports, {
    indexDocument: () => indexDocument,
    renderDocument: () => renderDocument,
    resolveEntry: () => resolveEntry
  });

  // src/timestampModifiers.ts
  var TIMESTAMP_REPEATER_RE = /(?:^|\s)(\+\+|\.\+|\+)(\d+)([dwmy])(?=[^A-Za-z0-9]|$)/i;
  var TIMESTAMP_WARNING_RE = /(?:^|\s)(--|-)(\d+)([dwmy])(?=[^A-Za-z0-9]|$)/i;
  function parseTimestampRepeater(raw) {
    const match = TIMESTAMP_REPEATER_RE.exec(raw);
    if (!match) return void 0;
    const mode = match[1];
    const value = Number.parseInt(match[2] ?? "", 10);
    const unit = (match[3] ?? "").toLowerCase();
    if (mode !== "+" && mode !== "++" && mode !== ".+") return void 0;
    if (!Number.isSafeInteger(value) || value <= 0) return void 0;
    if (unit !== "d" && unit !== "w" && unit !== "m" && unit !== "y") return void 0;
    return { mode, value, unit, raw: match[0].trim() };
  }
  function parseTimestampWarning(raw) {
    const match = TIMESTAMP_WARNING_RE.exec(raw);
    if (!match) return void 0;
    const mode = match[1];
    const value = Number.parseInt(match[2] ?? "", 10);
    const unit = (match[3] ?? "").toLowerCase();
    if (mode !== "-" && mode !== "--") return void 0;
    if (!Number.isSafeInteger(value) || value <= 0) return void 0;
    if (unit !== "d" && unit !== "w" && unit !== "m" && unit !== "y") return void 0;
    return { mode, value, unit, raw: match[0].trim() };
  }

  // native-boundary:node:fs
  var unavailable = () => {
    throw new Error("Filesystem access is unavailable in the mobile document runtime");
  };
  var node_fs_default = new Proxy({}, { get: (target, key) => target[key] ?? unavailable });

  // native-boundary:node:path
  var unavailable2 = () => {
    throw new Error("Filesystem access is unavailable in the mobile document runtime");
  };
  var node_path_default = new Proxy({ basename: (value) => value.replace(/\/+$/, "").split("/").pop() || "" }, { get: (target, key) => target[key] ?? unavailable2 });

  // src/config.ts
  function findConfigFile(startDir) {
    let currentDir = node_path_default.resolve(startDir);
    while (true) {
      const configPath = node_path_default.join(currentDir, "org2.json");
      if (node_fs_default.existsSync(configPath)) {
        return configPath;
      }
      const parentDir = node_path_default.dirname(currentDir);
      if (parentDir === currentDir) {
        break;
      }
      currentDir = parentDir;
    }
    return null;
  }
  function loadConfig(configPath) {
    try {
      const content = node_fs_default.readFileSync(configPath, "utf8");
      return JSON.parse(content);
    } catch (err) {
      throw new Error(
        `Failed to load config from ${configPath}: ${err instanceof Error ? err.message : String(err)}`
      );
    }
  }

  // src/todo.ts
  var TODO_KEYWORDS = ["TODO", "IN_PROGRESS", "DONE", "CANCELED", "CANCELLED"];
  function documentTodoSequences(input, defaults = []) {
    const sequences = [];
    let block;
    let drawer = false;
    for (const line of input.split(/\r?\n/)) {
      const begin = /^\s*#\+begin_(\S+)/i.exec(line);
      if (begin && !block) {
        block = begin[1].toLowerCase();
        continue;
      }
      if (block) {
        if (/^\s*#\+end_(\S+)\s*$/i.exec(line)?.[1].toLowerCase() === block) block = void 0;
        continue;
      }
      if (/^\s*:END:\s*$/i.test(line)) {
        drawer = false;
        continue;
      }
      if (/^\s*:[A-Za-z0-9_]+:\s*$/.test(line)) {
        drawer = true;
        continue;
      }
      if (drawer) continue;
      const match = /^\s*#\+(?:TODO|SEQ_TODO|TYP_TODO):\s*(.*)$/i.exec(line);
      if (!match) continue;
      const tokens = match[1].trim().split(/\s+/).map((token) => token.replace(/\([^)]*\)$/, ""));
      const separator = tokens.indexOf("|");
      const keywords = tokens.filter((token) => token !== "|" && /^[\p{L}][\p{L}\p{N}_-]*$/u.test(token));
      if (!keywords.length) continue;
      const terminal = separator < 0 ? keywords.slice(-1) : tokens.slice(separator + 1).filter((token) => keywords.includes(token));
      sequences.push({ keywords: [...new Set(keywords)], terminal: [...new Set(terminal)] });
    }
    return sequences.length ? sequences : [...defaults];
  }
  function documentTodoKeywords(input, defaults = []) {
    return [.../* @__PURE__ */ new Set([...TODO_KEYWORDS, ...documentTodoSequences(input, defaults).flatMap((sequence) => sequence.keywords)])];
  }
  function parseTodoSequenceDefinitions(value) {
    if (!Array.isArray(value) || !value.every((item) => typeof item === "string")) {
      throw new Error('todo.sequences must be an array of strings, such as ["TODO WAITING | DONE CANCELED"].');
    }
    const result = [];
    const seen = /* @__PURE__ */ new Set();
    for (const definition of value) {
      const tokens = definition.trim().split(/\s+/);
      if (tokens.filter((token) => token === "|").length > 1 || tokens.some((token) => token !== "|" && !/^[\p{L}][\p{L}\p{N}_-]*(?:\([^\s()]*\))?$/u.test(token))) {
        throw new Error(`Invalid TODO sequence: ${definition}. Use space-separated keywords and one | before terminal states.`);
      }
      const sequence = documentTodoSequences(`#+TODO: ${definition}`)[0];
      if (!sequence || !sequence.terminal.length || sequence.terminal.length === sequence.keywords.length) {
        throw new Error("Each TODO sequence needs at least one active state and one terminal state.");
      }
      for (const token of tokens.filter((token2) => token2 !== "|").map((token2) => token2.replace(/\([^)]*\)$/, ""))) {
        if (seen.has(token)) throw new Error(`TODO keyword ${token} appears more than once. Give each state one sequence.`);
        seen.add(token);
      }
      result.push(sequence);
    }
    return result;
  }
  function todoSequencesForFile(filePath, includeFileDeclarations = false) {
    if (!filePath) return [];
    const configPath = findConfigFile(node_path_default.dirname(node_path_default.resolve(filePath)));
    const value = configPath ? loadConfig(configPath).todo?.sequences : void 0;
    const defaults = value === void 0 ? [] : parseTodoSequenceDefinitions(value);
    return includeFileDeclarations && node_fs_default.existsSync(filePath) ? documentTodoSequences(node_fs_default.readFileSync(filePath, "utf8"), defaults) : defaults;
  }
  var TODO_KEYWORD_SET = new Set(TODO_KEYWORDS);
  function statusFromKeyword(keyword, sequences = []) {
    const sequence = sequences.find((sequence2) => sequence2.keywords.includes(keyword ?? ""));
    if (sequence) return sequence.terminal.includes(keyword) ? keyword.toUpperCase().startsWith("CANCEL") ? "canceled" : "done" : keyword === "IN_PROGRESS" ? "in_progress" : "todo";
    const k = (keyword || "").trim().toUpperCase();
    if (k === "IN_PROGRESS") return "in_progress";
    if (k === "DONE") return "done";
    if (k === "CANCELED" || k === "CANCELLED") return "canceled";
    return "todo";
  }
  function terminalTodoStatusFromKeyword(keyword, sequences = []) {
    const status = statusFromKeyword(keyword ?? void 0, sequences);
    return status === "done" || status === "canceled" ? status : void 0;
  }
  function isTerminalTodoKeyword(keyword, sequences = []) {
    return terminalTodoStatusFromKeyword(keyword, sequences) !== void 0;
  }

  // src/parser.ts
  function makeError(message, line, column) {
    return { message, line, column };
  }
  function fail(err) {
    const loc = `${err.line}:${err.column}`;
    throw new Error(`${loc} ${err.message}`);
  }
  function text(value) {
    return { type: "Text", value };
  }
  function timestamp(active, raw) {
    const repeater = parseTimestampRepeater(raw);
    const warning = parseTimestampWarning(raw);
    return { type: "Timestamp", active, raw, ...repeater ? { repeater } : {}, ...warning ? { warning } : {} };
  }
  function timestampRange(start, separatorRaw, end) {
    return {
      type: "TimestampRange",
      start,
      separatorRaw,
      end
    };
  }
  function emphasis(kind, marker, content) {
    return {
      type: "Emphasis",
      kind,
      marker,
      content
    };
  }
  function link(node) {
    return { type: "Link", ...node };
  }
  function rawInline(node, endIndex) {
    return { node, endIndex };
  }
  function progressCookie(node) {
    return { type: "ProgressCookie", ...node };
  }
  function isTimestampDatePrefix(value) {
    return /^\d{4}-\d{2}-\d{2}/.test(value);
  }
  function parseTimestampAt(value, startIndex) {
    const opener = value[startIndex];
    if (opener !== "<" && opener !== "[") return null;
    const closer = opener === "<" ? ">" : "]";
    const closeIndex = value.indexOf(closer, startIndex + 1);
    if (closeIndex === -1) return null;
    const inner = value.slice(startIndex + 1, closeIndex);
    if (!isTimestampDatePrefix(inner)) return null;
    const raw = value.slice(startIndex, closeIndex + 1);
    return {
      node: timestamp(opener === "<", raw),
      endIndex: closeIndex + 1
    };
  }
  function parseTimestampOrRangeAt(value, startIndex) {
    const first = parseTimestampAt(value, startIndex);
    if (!first) return null;
    const afterFirst = first.endIndex;
    let i = afterFirst;
    while (i < value.length && value[i] === " ") i += 1;
    if (value.slice(i, i + 2) !== "--") {
      return { node: first.node, endIndex: first.endIndex };
    }
    i += 2;
    while (i < value.length && value[i] === " ") i += 1;
    const secondStart = i;
    const second = parseTimestampAt(value, secondStart);
    if (!second) {
      return { node: first.node, endIndex: first.endIndex };
    }
    const sepRaw = value.slice(afterFirst, secondStart);
    return {
      node: timestampRange(first.node, sepRaw, second.node),
      endIndex: second.endIndex
    };
  }
  function isWordChar(ch) {
    return /^[A-Za-z0-9]$/.test(ch);
  }
  function isWhitespace(ch) {
    return ch === " " || ch === "\n";
  }
  function isHeadlineStart(line) {
    return /^\*+\s/.test(line);
  }
  function isBoundaryChar(ch) {
    if (ch === void 0) return true;
    if (isWhitespace(ch)) return true;
    return !isWordChar(ch);
  }
  var EMPHASIS_MARKERS = [
    { marker: "*", kind: "bold" },
    { marker: "/", kind: "italic" },
    { marker: "_", kind: "underline" },
    { marker: "+", kind: "strike" },
    { marker: "=", kind: "verbatim" },
    { marker: "~", kind: "code" },
    { marker: "`", kind: "code" }
  ];
  function parseEmphasisAt(value, startIndex) {
    const opener = value[startIndex];
    const rule = EMPHASIS_MARKERS.find((r) => r.marker === opener);
    if (!rule) return null;
    const prev = startIndex > 0 ? value[startIndex - 1] : void 0;
    const next = startIndex + 1 < value.length ? value[startIndex + 1] : void 0;
    const isBacktickCode = opener === "`";
    if (!isBacktickCode && !isBoundaryChar(prev)) return null;
    if (next === void 0 || isWhitespace(next)) return null;
    if (isBacktickCode && next === opener) return null;
    for (let closeIndex = startIndex + 1; closeIndex < value.length; closeIndex += 1) {
      if (value[closeIndex] !== opener) continue;
      const beforeClose = closeIndex > startIndex + 1 ? value[closeIndex - 1] : void 0;
      const afterClose = closeIndex + 1 < value.length ? value[closeIndex + 1] : void 0;
      if (beforeClose === void 0 || isWhitespace(beforeClose)) continue;
      if (isBacktickCode && (beforeClose === opener || afterClose === opener)) continue;
      if (!isBacktickCode && !isBoundaryChar(afterClose)) continue;
      const content = value.slice(startIndex + 1, closeIndex);
      if (content.includes("\n")) continue;
      return {
        node: emphasis(rule.kind, opener, content),
        endIndex: closeIndex + 1
      };
    }
    return null;
  }
  function parseBracketLinkAt(value, startIndex) {
    if (value.slice(startIndex, startIndex + 2) !== "[[") return null;
    const closeIndex = value.indexOf("]]", startIndex + 2);
    if (closeIndex === -1) return null;
    const raw = value.slice(startIndex, closeIndex + 2);
    const inner = value.slice(startIndex + 2, closeIndex);
    const splitIndex = inner.indexOf("][");
    if (splitIndex === -1) {
      return {
        node: link({
          format: "bracket",
          raw,
          targetRaw: inner
        }),
        endIndex: closeIndex + 2
      };
    }
    return {
      node: link({
        format: "bracket",
        raw,
        targetRaw: inner.slice(0, splitIndex),
        descriptionRaw: inner.slice(splitIndex + 2)
      }),
      endIndex: closeIndex + 2
    };
  }
  function parsePlainUrlAt(value, startIndex) {
    if (!value.startsWith("http://", startIndex) && !value.startsWith("https://", startIndex)) return null;
    const rest = value.slice(startIndex);
    const match = /^(https?:\/\/[^\s]+)/.exec(rest);
    if (!match) return null;
    let url = match[1] ?? "";
    while (/[),.!?;:]$/.test(url)) {
      url = url.slice(0, -1);
    }
    if (url.length === 0) return null;
    return {
      node: link({
        format: "plain",
        raw: url,
        targetRaw: url
      }),
      endIndex: startIndex + url.length
    };
  }
  function parseAngleLinkAt(value, startIndex) {
    if (value[startIndex] !== "<") return null;
    const match = /^<([A-Za-z][A-Za-z0-9+.-]*:[^<>\s]+)>/.exec(value.slice(startIndex));
    if (!match) return null;
    const raw = match[0];
    return {
      node: link({ format: "angle", raw, targetRaw: match[1] ?? "" }),
      endIndex: startIndex + raw.length
    };
  }
  function parseLinkAt(value, startIndex) {
    return parseBracketLinkAt(value, startIndex) ?? parseAngleLinkAt(value, startIndex) ?? parsePlainUrlAt(value, startIndex);
  }
  function parseTargetAt(value, startIndex) {
    if (value[startIndex] !== "<") return null;
    const rest = value.slice(startIndex);
    const radioMatch = /^<<<([^<>\n]+)>>>/.exec(rest);
    if (radioMatch) {
      const raw2 = radioMatch[0];
      return rawInline({ type: "Target", raw: raw2, valueRaw: radioMatch[1] ?? "", radio: true }, startIndex + raw2.length);
    }
    const match = /^<<([^<>\n]+)>>/.exec(rest);
    if (!match) return null;
    const raw = match[0];
    return rawInline({ type: "Target", raw, valueRaw: match[1] ?? "", radio: false }, startIndex + raw.length);
  }
  function parseFootnoteReferenceAt(value, startIndex) {
    if (value[startIndex] !== "[") return null;
    const match = /^\[fn:([^\]\n:]*)(?::([^\]\n]*))?\]/i.exec(value.slice(startIndex));
    if (!match) return null;
    const raw = match[0];
    const labelRaw = match[1] || void 0;
    const definitionRaw = match[2] !== void 0 ? match[2] : void 0;
    return rawInline({
      type: "FootnoteReference",
      raw,
      ...labelRaw ? { labelRaw } : {},
      ...definitionRaw !== void 0 ? { definitionRaw } : {}
    }, startIndex + raw.length);
  }
  function parseCitationAt(value, startIndex) {
    if (value[startIndex] !== "[") return null;
    const match = /^\[cite(?:\/([^:\]\s]+))?:([^\]\n]+)\]/i.exec(value.slice(startIndex));
    if (!match) return null;
    const raw = match[0];
    const body = match[2] ?? "";
    const references = [];
    for (const segment of body.split(";")) {
      const keyMatch = /^(.*?)(?:@)([A-Za-z0-9_:.+\-/]+)(.*)$/.exec(segment);
      if (!keyMatch) continue;
      const prefixRaw = (keyMatch[1] ?? "").trim() || void 0;
      const suffixRaw = (keyMatch[3] ?? "").trim() || void 0;
      references.push({
        keyRaw: keyMatch[2] ?? "",
        ...prefixRaw ? { prefixRaw } : {},
        ...suffixRaw ? { suffixRaw } : {}
      });
    }
    if (references.length === 0) return null;
    return rawInline({
      type: "Citation",
      raw,
      ...match[1] ? { styleRaw: match[1] } : {},
      references
    }, startIndex + raw.length);
  }
  function parseExportSnippetAt(value, startIndex) {
    if (value[startIndex] !== "@") return null;
    const match = /^@@([A-Za-z0-9_-]+):([\s\S]*?)@@/.exec(value.slice(startIndex));
    if (!match) return null;
    const raw = match[0];
    return rawInline({
      type: "ExportSnippet",
      raw,
      backendRaw: match[1] ?? "",
      valueRaw: match[2] ?? ""
    }, startIndex + raw.length);
  }
  var ORG_ENTITY_NAMES = /* @__PURE__ */ new Set([
    "alpha",
    "beta",
    "gamma",
    "delta",
    "epsilon",
    "zeta",
    "eta",
    "theta",
    "iota",
    "kappa",
    "lambda",
    "mu",
    "nu",
    "xi",
    "omicron",
    "pi",
    "rho",
    "sigma",
    "tau",
    "upsilon",
    "phi",
    "chi",
    "psi",
    "omega",
    "Alpha",
    "Beta",
    "Gamma",
    "Delta",
    "Theta",
    "Lambda",
    "Xi",
    "Pi",
    "Sigma",
    "Upsilon",
    "Phi",
    "Psi",
    "Omega",
    "nbsp",
    "copy",
    "reg",
    "trade",
    "ndash",
    "mdash",
    "hellip",
    "laquo",
    "raquo",
    "lsquo",
    "rsquo",
    "ldquo",
    "rdquo",
    "bull",
    "middot",
    "times",
    "divide",
    "plusmn",
    "le",
    "ge",
    "ne",
    "infin",
    "rarr",
    "larr",
    "uarr",
    "darr",
    "harr",
    "check",
    "deg"
  ]);
  function parseEntityAt(value, startIndex) {
    if (value[startIndex] !== "\\") return null;
    const match = /^\\([A-Za-z]+)(?:\{\})?/.exec(value.slice(startIndex));
    if (!match || !ORG_ENTITY_NAMES.has(match[1] ?? "")) return null;
    const raw = match[0];
    return rawInline({ type: "Entity", raw, nameRaw: match[1] ?? "" }, startIndex + raw.length);
  }
  function parseLatexFragmentAt(value, startIndex) {
    if (value[startIndex] !== "$" && value[startIndex] !== "\\") return null;
    const rest = value.slice(startIndex);
    let match = /^\$\$([^\n]*?)\$\$/.exec(rest);
    if (match) {
      const raw2 = match[0];
      return rawInline({ type: "LatexFragment", raw: raw2, display: true }, startIndex + raw2.length);
    }
    match = /^\$([^$\n]+)\$/.exec(rest);
    if (match) {
      const raw2 = match[0];
      return rawInline({ type: "LatexFragment", raw: raw2, display: false }, startIndex + raw2.length);
    }
    match = /^\\\[([\s\S]*?)\\\]/.exec(rest);
    if (match) {
      const raw2 = match[0];
      return rawInline({ type: "LatexFragment", raw: raw2, display: true }, startIndex + raw2.length);
    }
    match = /^\\\(([^\n]*?)\\\)/.exec(rest);
    if (!match) return null;
    const raw = match[0];
    return rawInline({ type: "LatexFragment", raw, display: false }, startIndex + raw.length);
  }
  function parseScriptAt(value, startIndex) {
    const marker = value[startIndex];
    if (marker !== "_" && marker !== "^") return null;
    const prev = startIndex > 0 ? value[startIndex - 1] : void 0;
    if (!prev || !/[A-Za-z0-9})\]]/.test(prev)) return null;
    const match = /^(?:([_^])\{([^{}\n]+)\}|([_^])([A-Za-z0-9+\-]))/.exec(value.slice(startIndex));
    if (!match) return null;
    const raw = match[0];
    const actualMarker = match[1] ?? match[3];
    const valueRaw = match[2] ?? match[4] ?? "";
    const isUnbracedLetter = match[3] !== void 0 && /^[A-Za-z]$/.test(valueRaw);
    if (isUnbracedLetter && /[A-Za-z0-9]/.test(value[startIndex + raw.length] ?? "")) return null;
    return rawInline({
      type: "Script",
      raw,
      kind: actualMarker === "_" ? "subscript" : "superscript",
      valueRaw
    }, startIndex + raw.length);
  }
  function parseLineBreakAt(value, startIndex) {
    if (value.slice(startIndex, startIndex + 2) !== "\\\\") return null;
    const after = value[startIndex + 2];
    if (after !== void 0 && after !== "\n") return null;
    return rawInline({ type: "LineBreak", raw: "\\\\" }, startIndex + 2);
  }
  function parseProgressCookieAt(value, startIndex) {
    if (value[startIndex] !== "[") return null;
    const rest = value.slice(startIndex);
    const fraction = /^\[(\d+)\/(\d+)\]/.exec(rest);
    if (fraction) {
      const done = Number.parseInt(fraction[1] || "0", 10);
      const total = Number.parseInt(fraction[2] || "0", 10);
      return { node: progressCookie({ raw: fraction[0], format: "fraction", done, total, percent: total > 0 ? Math.round(done / total * 100) : 0 }), endIndex: startIndex + fraction[0].length };
    }
    const percent = /^\[(\d{1,3})%\]/.exec(rest);
    if (percent) {
      const valueNum = Number.parseInt(percent[1] || "0", 10);
      return { node: progressCookie({ raw: percent[0], format: "percent", percent: Math.max(0, Math.min(100, valueNum)) }), endIndex: startIndex + percent[0].length };
    }
    return null;
  }
  var INLINE_START_MARKERS = /* @__PURE__ */ new Set([
    ...EMPHASIS_MARKERS.map(({ marker }) => marker),
    "<",
    "[",
    "@",
    "$",
    "\\",
    "^",
    "h"
    // targets, timestamps, objects, and http(s)
  ]);
  function parseInlinesFromText(value) {
    const out = [];
    let i = 0;
    let lastTextStart = 0;
    while (i < value.length) {
      if (!INLINE_START_MARKERS.has(value[i])) {
        i += 1;
        continue;
      }
      const parsedRichObject = parseTargetAt(value, i) ?? parseFootnoteReferenceAt(value, i) ?? parseCitationAt(value, i) ?? parseExportSnippetAt(value, i) ?? parseLatexFragmentAt(value, i) ?? parseEntityAt(value, i) ?? parseScriptAt(value, i) ?? parseLineBreakAt(value, i);
      if (parsedRichObject) {
        if (lastTextStart < i) out.push(text(value.slice(lastTextStart, i)));
        out.push(parsedRichObject.node);
        i = parsedRichObject.endIndex;
        lastTextStart = i;
        continue;
      }
      const parsedProgressCookie = parseProgressCookieAt(value, i);
      if (parsedProgressCookie) {
        if (lastTextStart < i) {
          out.push(text(value.slice(lastTextStart, i)));
        }
        out.push(parsedProgressCookie.node);
        i = parsedProgressCookie.endIndex;
        lastTextStart = i;
        continue;
      }
      const parsedTimestamp = parseTimestampOrRangeAt(value, i);
      if (parsedTimestamp) {
        if (lastTextStart < i) {
          out.push(text(value.slice(lastTextStart, i)));
        }
        out.push(parsedTimestamp.node);
        i = parsedTimestamp.endIndex;
        lastTextStart = i;
        continue;
      }
      const parsedLink = parseLinkAt(value, i);
      if (parsedLink) {
        if (lastTextStart < i) {
          out.push(text(value.slice(lastTextStart, i)));
        }
        out.push(parsedLink.node);
        i = parsedLink.endIndex;
        lastTextStart = i;
        continue;
      }
      const parsedEmphasis = parseEmphasisAt(value, i);
      if (parsedEmphasis) {
        if (lastTextStart < i) {
          out.push(text(value.slice(lastTextStart, i)));
        }
        out.push(parsedEmphasis.node);
        i = parsedEmphasis.endIndex;
        lastTextStart = i;
        continue;
      }
      i += 1;
    }
    if (lastTextStart < value.length) {
      out.push(text(value.slice(lastTextStart)));
    }
    if (out.length === 0) return [text(value)];
    return out;
  }
  function paragraphFromLines(lines) {
    const joined = lines.join("\n");
    return { type: "Paragraph", children: parseInlinesFromText(joined) };
  }
  function paragraphFromText(value) {
    return { type: "Paragraph", children: parseInlinesFromText(value) };
  }
  function isBlank(line) {
    return line.trim().length === 0;
  }
  function parseKeywordLine(line, lineNumber) {
    const match = /^(\s*)#\+([^:\s]+):(.*)$/.exec(line);
    if (!match) return null;
    const indent = match[1] ?? "";
    const keyRaw = match[2] ?? "";
    const valueRaw = match[3] ?? "";
    if (indent.includes("	") || valueRaw.includes("	")) {
      fail(makeError("Unsupported construct: tab character", lineNumber, line.indexOf("	") + 1));
    }
    return {
      type: "KeywordLine",
      raw: line,
      indent,
      keyRaw,
      valueRaw
    };
  }
  function isAffiliatedKeyword(node) {
    const key = node.keyRaw.toUpperCase();
    return key === "NAME" || key === "CAPTION" || key === "HEADER" || key === "HEADERS" || key === "RESULTS" || key === "PLOT" || key === "CHART" || key === "DATASET" || key === "VIEW" || key.startsWith("ATTR_");
  }
  function parseCommentLine(line, lineNumber) {
    const match = /^(\s*)#(?!\+)(.*)$/.exec(line);
    if (!match) return null;
    const indent = match[1] ?? "";
    const bodyRaw = match[2] ?? "";
    if (indent.includes("	") || bodyRaw.includes("	")) {
      fail(makeError("Unsupported construct: tab character", lineNumber, line.indexOf("	") + 1));
    }
    return {
      type: "CommentLine",
      raw: line,
      indent,
      bodyRaw
    };
  }
  function parseClockLine(line, lineNumber) {
    const match = /^(\s*)CLOCK:\s*(.*)$/.exec(line);
    if (!match) return null;
    const indent = match[1] ?? "";
    if (indent.includes("	") || line.includes("	")) {
      fail(makeError("Unsupported construct: tab character", lineNumber, line.indexOf("	") + 1));
    }
    const rangeMatch = /(\[[^\]]+\]|<[^>]+>)--(\[[^\]]+\]|<[^>]+>)/.exec(match[2] ?? "");
    const start = rangeMatch ? parseTimestampAt(rangeMatch[1] || "", 0)?.node : void 0;
    const end = rangeMatch ? parseTimestampAt(rangeMatch[2] || "", 0)?.node : void 0;
    return { type: "Clock", raw: line, ...start ? { start } : {}, ...end ? { end } : {} };
  }
  function parsePlanningLine(line, lineNumber) {
    const initialMatch = /^(\s*)(SCHEDULED|DEADLINE|CLOSED):/.exec(line);
    if (!initialMatch) return null;
    const indent = initialMatch[1] ?? "";
    if (indent.includes("	")) {
      fail(makeError("Unsupported construct: tab character", lineNumber, line.indexOf("	") + 1));
    }
    const planningNodes = [];
    let pos = 0;
    while (true) {
      const keywordMatch = /(SCHEDULED|DEADLINE|CLOSED):/.exec(line.slice(pos));
      if (!keywordMatch) break;
      const kind = keywordMatch[1];
      const startPos = pos + keywordMatch.index;
      const afterKeywordPos = startPos + keywordMatch[0].length;
      let endPos = line.length;
      const nextKeywordMatch = /(SCHEDULED|DEADLINE|CLOSED):/.exec(line.slice(afterKeywordPos));
      if (nextKeywordMatch) {
        endPos = afterKeywordPos + nextKeywordMatch.index;
      }
      const after = line.slice(afterKeywordPos, endPos).trim();
      let ts;
      for (let i = 0; i < after.length; i += 1) {
        const ch = after[i];
        if (ch !== "<" && ch !== "[") continue;
        const parsed = parseTimestampOrRangeAt(after, i);
        if (parsed) {
          ts = parsed.node;
          break;
        }
      }
      planningNodes.push({
        type: "Planning",
        kind,
        raw: line,
        ...ts ? { timestamp: ts } : {}
      });
      pos = afterKeywordPos;
    }
    return planningNodes.length > 0 ? planningNodes : null;
  }
  function parseHeadline(line, lineNumber, todoKeywords) {
    const match = /^(\*+)(\s+)(.*)$/.exec(line);
    if (!match) {
      fail(makeError("Invalid headline; expected one or more '*' followed by a space", lineNumber, 1));
    }
    const stars = match[1];
    const ws = match[2];
    const raw = match[3];
    if (ws !== " ") {
      fail(makeError("Invalid headline; only a single space is allowed after '*'", lineNumber, stars.length + 1));
    }
    if (raw.length === 0) {
      fail(makeError("Invalid headline; title cannot be empty", lineNumber, stars.length + 2));
    }
    let todo;
    let priority;
    let commented = false;
    let tags;
    let rest = raw;
    {
      const parts = rest.split(/\s+/);
      const last = parts[parts.length - 1] ?? "";
      if (last.startsWith(":") && /^:(?:[^\s:]+:)+$/.test(last)) {
        const parsed = last.split(":").filter((t) => t.length > 0);
        if (parsed.length > 0) {
          tags = parsed;
          rest = rest.slice(0, rest.length - last.length).trimEnd();
        }
      }
    }
    if (rest === "COMMENT" || rest.startsWith("COMMENT ")) {
      commented = true;
      rest = rest.slice("COMMENT".length).trimStart();
    }
    if (!commented) {
      for (const kw of todoKeywords) {
        const prefix = `${kw} `;
        if (rest.startsWith(prefix)) {
          todo = kw;
          rest = rest.slice(prefix.length);
          break;
        }
      }
    }
    const priorityMatch = /^\[#([A-Za-z0-9])\](?:\s+|$)/.exec(rest);
    if (priorityMatch) {
      priority = priorityMatch[1];
      rest = rest.slice(priorityMatch[0].length);
    }
    if (rest === "COMMENT" || rest.startsWith("COMMENT ")) {
      commented = true;
      rest = rest.slice("COMMENT".length).trimStart();
    }
    if (rest.length === 0) {
      fail(makeError("Invalid headline; title cannot be empty", lineNumber, stars.length + 2));
    }
    return { level: stars.length, title: rest, todo, priority, ...commented ? { commented } : {}, tags };
  }
  function getChildrenArray(node) {
    return node.children;
  }
  function parsePropertyDrawer(lines, startLineIndex) {
    const startLineNumber = startLineIndex + 1;
    const start = lines[startLineIndex] ?? "";
    const startMatch = /^(\s*):PROPERTIES:$/.exec(start);
    if (!startMatch) {
      fail(makeError("Invalid property drawer; expected :PROPERTIES:", startLineNumber, 1));
    }
    const indent = startMatch[1] ?? "";
    if (indent.includes("	")) {
      fail(makeError("Unsupported construct: tab character", startLineNumber, start.indexOf("	") + 1));
    }
    const properties = [];
    for (let i = startLineIndex + 1; i < lines.length; i += 1) {
      const lineNumber = i + 1;
      const sourceLine = lines[i] ?? "";
      if (sourceLine === `${indent}:END:`) {
        return {
          drawer: { type: "PropertyDrawer", properties },
          nextLineIndex: i + 1
        };
      }
      if (!sourceLine.startsWith(indent)) {
        fail(makeError("Invalid property drawer line; expected matching indentation", lineNumber, 1));
      }
      const line = sourceLine.slice(indent.length);
      if (isBlank(line)) {
        fail(makeError("Invalid property drawer; blank lines are not allowed", lineNumber, indent.length + 1));
      }
      const match = /^:([^:\s]+):(\s*)(.*)$/.exec(line);
      if (!match) {
        fail(makeError("Invalid property drawer line; expected :KEY: VALUE", lineNumber, indent.length + 1));
      }
      const key = match[1];
      const ws = match[2];
      const rawValue = match[3];
      if (ws.includes("	")) {
        fail(makeError("Unsupported construct: tab character", lineNumber, indent.length + key.length + 3));
      }
      properties.push({ key, value: rawValue });
    }
    fail(makeError("Invalid property drawer; missing :END:", startLineNumber, 1));
  }
  function parseDrawer(lines, startLineIndex) {
    const startLineNumber = startLineIndex + 1;
    const start = lines[startLineIndex] ?? "";
    const match = /^(\s*):([^:\s]+):$/.exec(start);
    if (!match) {
      fail(makeError("Invalid drawer; expected :NAME:", startLineNumber, 1));
    }
    const indent = match[1] ?? "";
    const nameRaw = match[2] ?? "";
    if (indent.includes("	")) {
      fail(makeError("Unsupported construct: tab character", startLineNumber, start.indexOf("	") + 1));
    }
    for (let i = startLineIndex + 1; i < lines.length; i += 1) {
      const lineNumber = i + 1;
      const line = lines[i] ?? "";
      if (line === `${indent}:END:`) {
        const bodyLines2 = lines.slice(startLineIndex + 1, i);
        return {
          drawer: {
            type: "Drawer",
            nameRaw,
            indent,
            terminated: true,
            bodyRaw: bodyLines2.join("\n"),
            endRaw: line
          },
          nextLineIndex: i + 1
        };
      }
      if (line.includes("	")) {
        fail(makeError("Unsupported construct: tab character", lineNumber, line.indexOf("	") + 1));
      }
    }
    const bodyLines = lines.slice(startLineIndex + 1);
    return {
      drawer: {
        type: "Drawer",
        nameRaw,
        indent,
        terminated: false,
        bodyRaw: bodyLines.join("\n")
      },
      nextLineIndex: lines.length
    };
  }
  function parseListItemContent(contentRaw) {
    let content = contentRaw;
    if (content.length === 0) return null;
    let counter;
    const counterMatch = /^\[@(\d+)\]\s+(.*)$/.exec(content);
    if (counterMatch) {
      counter = Number.parseInt(counterMatch[1] ?? "0", 10);
      content = counterMatch[2] ?? "";
    }
    let checkbox;
    const checkboxMatch = /^\[([ Xx-])\]\s+(.*)$/.exec(content);
    if (checkboxMatch) {
      checkbox = checkboxMatch[1] === " " ? "unchecked" : checkboxMatch[1] === "-" ? "indeterminate" : "checked";
      content = checkboxMatch[2] ?? "";
    }
    const progressCookie2 = parseProgressCookieAt(content, 0)?.node;
    let descriptionTag;
    const descriptionMatch = findDescriptionSeparator(content);
    if (descriptionMatch && descriptionMatch.tagRaw.trim()) {
      descriptionTag = parseInlinesFromText(descriptionMatch.tagRaw.trim());
      content = descriptionMatch.contentRaw;
    }
    return {
      content,
      ...counter !== void 0 ? { counter } : {},
      ...checkbox ? { checkbox } : {},
      ...descriptionTag ? { descriptionTag } : {},
      ...progressCookie2 ? { progressCookie: progressCookie2 } : {}
    };
  }
  function findDescriptionSeparator(value) {
    const pattern = /\s+::(?:\s+|$)/g;
    for (const match of value.matchAll(pattern)) {
      const separatorIndex = match.index ?? 0;
      if (descriptionSeparatorIsInsideDelimitedObject(value, separatorIndex)) continue;
      return {
        tagRaw: value.slice(0, separatorIndex),
        contentRaw: value.slice(separatorIndex + match[0].length)
      };
    }
    return null;
  }
  function descriptionSeparatorIsInsideDelimitedObject(value, index) {
    for (let i = 0; i < index; ) {
      const parsed = parseLinkAt(value, i) ?? parseExportSnippetAt(value, i) ?? parseFootnoteReferenceAt(value, i) ?? parseCitationAt(value, i) ?? parseLatexFragmentAt(value, i) ?? parseEmphasisAt(value, i);
      if (!parsed) {
        i += 1;
        continue;
      }
      if (parsed.endIndex > index) return true;
      i = parsed.endIndex;
    }
    return false;
  }
  function parseListItemLine(line) {
    const unordered = /^([+*-])(\s+)(.*)$/.exec(line);
    if (unordered) {
      const ws = unordered[2];
      if (ws !== " ") return null;
      const parsedContent = parseListItemContent(unordered[3] ?? "");
      if (!parsedContent) return null;
      return { ordered: false, indentColumn: unordered[1].length + ws.length, ...parsedContent };
    }
    const ordered = /^(\d+)([.)])(\s+)(.*)$/.exec(line);
    if (ordered) {
      const ws = ordered[3];
      if (ws !== " ") return null;
      const parsedContent = parseListItemContent(ordered[4] ?? "");
      if (!parsedContent) return null;
      return {
        ordered: true,
        ordinal: Number.parseInt(ordered[1], 10),
        indentColumn: ordered[1].length + ordered[2].length + ws.length,
        ...parsedContent
      };
    }
    return null;
  }
  function listItemNode(content, checkbox, progressCookie2, ordinal, counter, descriptionTag) {
    return {
      type: "ListItem",
      ...ordinal !== void 0 ? { ordinal } : {},
      ...counter !== void 0 ? { counter } : {},
      ...checkbox ? { checkbox } : {},
      ...descriptionTag ? { descriptionTag } : {},
      ...progressCookie2 ? { progressCookie: progressCookie2 } : {},
      children: [paragraphFromText(content)]
    };
  }
  function parseSrcBlockLine(line, lineNumber) {
    const directiveMatch = /^(\s*)#\+([^\s]+)(.*)$/.exec(line);
    if (directiveMatch) {
      const indent = directiveMatch[1];
      const keywordRaw = directiveMatch[2];
      const afterKeywordRaw = directiveMatch[3];
      if (indent.includes("	") || afterKeywordRaw.includes("	")) {
        fail(makeError("Unsupported construct: tab character", lineNumber, line.indexOf("	") + 1));
      }
      return { indent, keywordRaw, afterKeywordRaw };
    }
    const fenceMatch = /^(\s*)```($|[^`].*)$/.exec(line);
    if (fenceMatch) {
      const indent = fenceMatch[1] || "";
      const afterKeywordRaw = fenceMatch[2] || "";
      if (indent.includes("	") || afterKeywordRaw.includes("	")) {
        fail(makeError("Unsupported construct: tab character", lineNumber, line.indexOf("	") + 1));
      }
      return { indent, keywordRaw: "```", afterKeywordRaw };
    }
    return null;
  }
  function isBeginSrc(line) {
    const key = line.keywordRaw.toLowerCase();
    return key === "begin_src" || key === "begin_org2" || key === "```";
  }
  function isEndSrc(line) {
    const key = line.keywordRaw.toLowerCase();
    return key === "end_src" || key === "end_org2" || key === "```";
  }
  function getBlockKindFromBegin(line) {
    const key = line.keywordRaw.toLowerCase();
    if (!key.startsWith("begin_")) return null;
    const kind = key.slice("begin_".length);
    if (!kind || kind === "src" || kind === "org2") return null;
    return kind;
  }
  function isEndBlockForKind(line, kind) {
    const key = line.keywordRaw.toLowerCase();
    return key === `end_${kind}`;
  }
  function parseBlock(lines, startLineIndex, kind) {
    const beginLineNumber = startLineIndex + 1;
    const begin = parseSrcBlockLine(lines[startLineIndex] ?? "", beginLineNumber);
    if (!begin) {
      fail(makeError("Invalid block; expected #+begin_...", beginLineNumber, 1));
    }
    for (let i = startLineIndex + 1; i < lines.length; i += 1) {
      const lineNumber = i + 1;
      const line = lines[i] ?? "";
      const parsed = parseSrcBlockLine(line, lineNumber);
      if (parsed && isEndBlockForKind(parsed, kind)) {
        const bodyLines2 = lines.slice(startLineIndex + 1, i);
        return {
          block: {
            type: "Block",
            kind,
            terminated: true,
            begin,
            bodyRaw: bodyLines2.join("\n"),
            end: parsed
          },
          nextLineIndex: i + 1
        };
      }
    }
    const bodyLines = lines.slice(startLineIndex + 1);
    return {
      block: {
        type: "Block",
        kind,
        terminated: false,
        begin,
        bodyRaw: bodyLines.join("\n")
      },
      nextLineIndex: lines.length
    };
  }
  function parseSrcBlock(lines, startLineIndex) {
    const startLineNumber = startLineIndex + 1;
    const begin = parseSrcBlockLine(lines[startLineIndex] ?? "", startLineNumber);
    if (!begin || !isBeginSrc(begin)) {
      fail(makeError("Invalid source block; expected #+begin_src, #+begin_org2, or ```", startLineNumber, 1));
    }
    const beginKey = begin.keywordRaw.toLowerCase();
    const beginIsFence = beginKey === "```";
    for (let i = startLineIndex + 1; i < lines.length; i += 1) {
      const lineNumber = i + 1;
      const line = lines[i] ?? "";
      const parsed = parseSrcBlockLine(line, lineNumber);
      if (!parsed) continue;
      if (beginIsFence) {
        if (parsed.keywordRaw === "```" && String(parsed.afterKeywordRaw || "").trim().length === 0) {
          const bodyLines2 = lines.slice(startLineIndex + 1, i);
          return {
            block: {
              type: "SrcBlock",
              terminated: true,
              begin,
              bodyRaw: bodyLines2.join("\n"),
              end: parsed
            },
            nextLineIndex: i + 1
          };
        }
        continue;
      }
      if (isEndSrc(parsed) && parsed.keywordRaw !== "```") {
        const bodyLines2 = lines.slice(startLineIndex + 1, i);
        return {
          block: {
            type: "SrcBlock",
            terminated: true,
            begin,
            bodyRaw: bodyLines2.join("\n"),
            end: parsed
          },
          nextLineIndex: i + 1
        };
      }
    }
    const bodyLines = lines.slice(startLineIndex + 1);
    return {
      block: {
        type: "SrcBlock",
        terminated: false,
        begin,
        bodyRaw: bodyLines.join("\n")
      },
      nextLineIndex: lines.length
    };
  }
  function matchTableLine(line, indent) {
    if (indent.length > 0) {
      if (!line.startsWith(indent)) return null;
      const rest2 = line.slice(indent.length);
      if (!rest2.startsWith("|")) return null;
      const trimmedEnd2 = rest2.trimEnd();
      if (!trimmedEnd2.endsWith("|")) return null;
      return { rest: rest2, indentLen: indent.length };
    }
    const match = /^( *)(\|.*)$/.exec(line);
    if (!match) return null;
    const rest = match[2] ?? "";
    const trimmedEnd = rest.trimEnd();
    if (!trimmedEnd.endsWith("|")) return null;
    return { rest, indentLen: (match[1] ?? "").length };
  }
  function isTableHlineRow(rest) {
    const trimmed = rest.trim();
    return /^\|[-+]+\|$/.test(trimmed);
  }
  function parseTableRowCells(rest) {
    const trimmedEnd = rest.trimEnd();
    const core = trimmedEnd.slice(1, -1);
    return core.split("|").map((c) => c.trim());
  }
  function splitTableFormulaTopLevel(value, delimiter) {
    const parts = [];
    let start = 0;
    let depth = 0;
    let quote;
    for (let i = 0; i < value.length; i += 1) {
      const ch = value[i] ?? "";
      if (quote) {
        if (ch === "\\") i += 1;
        else if (ch === quote) quote = void 0;
        continue;
      }
      if (ch === '"' || ch === "'") {
        quote = ch;
        continue;
      }
      if (ch === "(") depth += 1;
      else if (ch === ")") depth = Math.max(0, depth - 1);
      else if (depth === 0 && value.slice(i, i + delimiter.length) === delimiter) {
        parts.push(value.slice(start, i));
        start = i + delimiter.length;
        i += delimiter.length - 1;
      }
    }
    parts.push(value.slice(start));
    return parts;
  }
  function parseTableFormulaLine(raw) {
    const match = /^(\s*)#\+TBLFM:\s*(.*)$/i.exec(raw);
    const indent = match?.[1] ?? "";
    const valueRaw = match?.[2] ?? "";
    const assignments = splitTableFormulaTopLevel(valueRaw, "::").map((assignmentRaw) => {
      let depth = 0;
      let equals = -1;
      for (let i = 0; i < assignmentRaw.length; i += 1) {
        const ch = assignmentRaw[i];
        if (ch === "(") depth += 1;
        else if (ch === ")") depth = Math.max(0, depth - 1);
        else if (ch === "=" && depth === 0 && assignmentRaw[i - 1] !== "<" && assignmentRaw[i - 1] !== ">" && assignmentRaw[i - 1] !== "!" && assignmentRaw[i + 1] !== "=") {
          equals = i;
          break;
        }
      }
      const targetRaw = equals >= 0 ? assignmentRaw.slice(0, equals).trim() : "";
      const rhs = equals >= 0 ? assignmentRaw.slice(equals + 1) : assignmentRaw;
      const expressionAndMode = splitTableFormulaTopLevel(rhs, ";");
      const expressionRaw = (expressionAndMode.shift() ?? "").trim();
      const modeRaw = expressionAndMode.join(";").trim();
      return { raw: assignmentRaw, targetRaw, expressionRaw, ...modeRaw ? { modeRaw } : {} };
    });
    return { type: "TableFormulaLine", raw, indent, valueRaw, assignments };
  }
  function parseTable(lines, startLineIndex, indent) {
    const rows = [];
    let tableEndLineIndex = lines.length;
    for (let i = startLineIndex; i < lines.length; i += 1) {
      const line = lines[i] ?? "";
      const matched = matchTableLine(line, indent);
      if (!matched) {
        tableEndLineIndex = i;
        break;
      }
      const rest = matched.rest;
      const lineIndent = " ".repeat(matched.indentLen);
      if (isTableHlineRow(rest)) {
        rows.push({ type: "TableHline", indent: lineIndent, raw: rest });
        continue;
      }
      const cells = parseTableRowCells(rest);
      rows.push({ type: "TableRow", indent: lineIndent, cells, contents: cells.map(parseInlinesFromText) });
    }
    const formulas = [];
    let nextLineIndex = tableEndLineIndex;
    while (nextLineIndex < lines.length) {
      const line = lines[nextLineIndex] ?? "";
      if (!/^\s*#\+TBLFM:/i.test(line)) break;
      if (indent && !line.startsWith(indent)) break;
      formulas.push(parseTableFormulaLine(line));
      nextLineIndex += 1;
    }
    return {
      table: { type: "Table", rows, ...formulas.length > 0 ? { formulas } : {} },
      nextLineIndex,
      tableEndLineIndex
    };
  }
  function parseDynamicBlock(lines, startLineIndex) {
    const beginRaw = lines[startLineIndex] ?? "";
    const begin = /^(\s*)#\+begin:\s+([^\s]+)(.*)$/i.exec(beginRaw);
    if (!begin) return null;
    for (let i = startLineIndex + 1; i < lines.length; i += 1) {
      if (!/^\s*#\+end:\s*$/i.test(lines[i] ?? "")) continue;
      return {
        block: {
          type: "DynamicBlock",
          nameRaw: begin[2] ?? "",
          parametersRaw: begin[3] ?? "",
          indent: begin[1] ?? "",
          beginRaw,
          bodyRaw: lines.slice(startLineIndex + 1, i).join("\n"),
          terminated: true,
          endRaw: lines[i] ?? ""
        },
        nextLineIndex: i + 1
      };
    }
    return {
      block: {
        type: "DynamicBlock",
        nameRaw: begin[2] ?? "",
        parametersRaw: begin[3] ?? "",
        indent: begin[1] ?? "",
        beginRaw,
        bodyRaw: lines.slice(startLineIndex + 1).join("\n"),
        terminated: false
      },
      nextLineIndex: lines.length
    };
  }
  function parseFixedWidth(lines, startLineIndex) {
    const parsedLines = [];
    let i = startLineIndex;
    for (; i < lines.length; i += 1) {
      const raw = lines[i] ?? "";
      const match = /^(\s*):(?: ?)(.*)$/.exec(raw);
      if (!match || /^\s*:[^\s:]+:$/.test(raw)) break;
      parsedLines.push({ raw, indent: match[1] ?? "", valueRaw: match[2] ?? "" });
    }
    return parsedLines.length > 0 ? { node: { type: "FixedWidth", lines: parsedLines }, nextLineIndex: i } : null;
  }
  function parseLatexEnvironment(lines, startLineIndex) {
    const beginRaw = lines[startLineIndex] ?? "";
    const begin = /^\s*\\begin\{([^{}\s]+)\}/.exec(beginRaw);
    if (!begin) return null;
    const nameRaw = begin[1] ?? "";
    const endPattern = new RegExp(`^\\s*\\\\end\\{${nameRaw.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}\\}\\s*$`);
    for (let i = startLineIndex + 1; i < lines.length; i += 1) {
      if (!endPattern.test(lines[i] ?? "")) continue;
      return {
        node: {
          type: "LatexEnvironment",
          nameRaw,
          beginRaw,
          bodyRaw: lines.slice(startLineIndex + 1, i).join("\n"),
          terminated: true,
          endRaw: lines[i] ?? ""
        },
        nextLineIndex: i + 1
      };
    }
    return {
      node: {
        type: "LatexEnvironment",
        nameRaw,
        beginRaw,
        bodyRaw: lines.slice(startLineIndex + 1).join("\n"),
        terminated: false
      },
      nextLineIndex: lines.length
    };
  }
  function parseFootnoteDefinitionLine(line) {
    const match = /^\[fn:([^\]\s:]+)\](?:\s+(.*)|\s*)$/i.exec(line);
    if (!match) return null;
    return {
      type: "FootnoteDefinition",
      labelRaw: match[1] ?? "",
      children: parseInlinesFromText(match[2] ?? "")
    };
  }
  function parseOrgToCanonicalAst(input, options = {}) {
    if (input.includes("\r\n")) {
      fail(makeError("Unsupported line endings: CRLF", 1, 1));
    }
    const doc = { type: "Document", version: "0", children: [] };
    const headlineStack = [];
    let paragraphLines = [];
    let paragraphStartLine = null;
    let currentList = null;
    let pendingBlankLinesBeforeNextNode = 0;
    let pendingAffiliatedKeywords = [];
    const sourceLineOffset = options.sourceLineOffset ?? 0;
    function setSourceRange(node, startLine, endLine) {
      if (options.sourceRanges) {
        const absoluteStartLine = startLine + sourceLineOffset;
        const absoluteEndLine = Math.max(startLine, endLine) + sourceLineOffset;
        node.sourceRange = {
          startLine: absoluteStartLine,
          endLine: absoluteEndLine
        };
      }
      return node;
    }
    function sourceRangeStart(node) {
      const startLine = node.sourceRange?.startLine;
      return startLine === void 0 ? void 0 : startLine - sourceLineOffset;
    }
    function finalizeHeadline(headline, endLine) {
      if (!options.sourceRanges) return;
      const startLine = sourceRangeStart(headline);
      if (startLine !== void 0) setSourceRange(headline, startLine, endLine);
    }
    function currentContainer() {
      return headlineStack.length > 0 ? headlineStack[headlineStack.length - 1] : doc;
    }
    function attachBlankLinesBefore(node) {
      if (pendingBlankLinesBeforeNextNode > 0) {
        Object.defineProperty(node, "blankLinesBefore", {
          value: pendingBlankLinesBeforeNextNode,
          enumerable: false,
          configurable: true
        });
        pendingBlankLinesBeforeNextNode = 0;
      }
      return node;
    }
    function pushCurrent(node, startLine, endLine) {
      if (startLine !== void 0) {
        setSourceRange(node, startLine, endLine ?? startLine);
      }
      getChildrenArray(currentContainer()).push(attachBlankLinesBefore(node));
    }
    function flushAffiliatedKeywords() {
      if (pendingAffiliatedKeywords.length === 0) return;
      for (const keyword of pendingAffiliatedKeywords) pushCurrent(keyword);
      pendingAffiliatedKeywords = [];
    }
    function takeAffiliatedKeywords() {
      if (pendingAffiliatedKeywords.length === 0) return void 0;
      const keywords2 = pendingAffiliatedKeywords;
      pendingAffiliatedKeywords = [];
      return keywords2;
    }
    function attachAffiliatedKeywords(node) {
      const affiliatedKeywords = takeAffiliatedKeywords();
      if (!affiliatedKeywords) return node;
      return { ...node, affiliatedKeywords };
    }
    function flushParagraph() {
      if (paragraphLines.length === 0) return;
      const node = paragraphFromLines(paragraphLines);
      if (paragraphStartLine !== null) {
        setSourceRange(node, paragraphStartLine, paragraphStartLine + paragraphLines.length - 1);
      }
      pushCurrent(node);
      paragraphLines = [];
      paragraphStartLine = null;
    }
    function endList() {
      currentList = null;
    }
    function ensureList(ordered) {
      if (currentList && currentList.ordered === ordered) return currentList;
      const list = {
        type: "List",
        ordered,
        items: []
      };
      pushCurrent(list);
      currentList = list;
      return list;
    }
    function addListItem(ordered, content, checkbox, itemProgressCookie, sourceOrdinal, counter, descriptionTag) {
      const list = ensureList(ordered);
      const ordinal = ordered && sourceOrdinal !== list.items.length + 1 ? sourceOrdinal : void 0;
      const item = listItemNode(content, checkbox, itemProgressCookie, ordinal, counter, descriptionTag);
      list.items.push(item);
      return item;
    }
    const lines = input.split("\n");
    const sequences = options.todoSequences ? [...options.todoSequences] : documentTodoSequences(input, todoSequencesForFile(options.sourcePath, (options.sourceLineOffset ?? 0) > 0));
    if (sequences.length) doc.todoSequences = sequences;
    const keywords = documentTodoKeywords("", sequences);
    const documentEndLine = input.endsWith("\n") ? Math.max(1, lines.length - 1) : lines.length;
    for (let i = 0; i < lines.length; ) {
      const lineNumber = i + 1;
      const line = lines[i];
      const dynamic = parseDynamicBlock(lines, i);
      if (dynamic) {
        flushParagraph();
        endList();
        pushCurrent(attachAffiliatedKeywords(dynamic.block), lineNumber, dynamic.nextLineIndex);
        i = dynamic.nextLineIndex;
        continue;
      }
      const latexEnvironment = parseLatexEnvironment(lines, i);
      if (latexEnvironment) {
        flushParagraph();
        endList();
        flushAffiliatedKeywords();
        pushCurrent(latexEnvironment.node, lineNumber, latexEnvironment.nextLineIndex);
        i = latexEnvironment.nextLineIndex;
        continue;
      }
      const footnoteDefinition = parseFootnoteDefinitionLine(line);
      if (footnoteDefinition) {
        flushParagraph();
        endList();
        flushAffiliatedKeywords();
        pushCurrent(footnoteDefinition, lineNumber);
        i += 1;
        continue;
      }
      const horizontalRule = /^(\s*)-{5,}\s*$/.exec(line);
      if (horizontalRule) {
        flushParagraph();
        endList();
        flushAffiliatedKeywords();
        pushCurrent({ type: "HorizontalRule", raw: line, indent: horizontalRule[1] ?? "" }, lineNumber);
        i += 1;
        continue;
      }
      if (/^\s*%%\(/.test(line)) {
        flushParagraph();
        endList();
        flushAffiliatedKeywords();
        pushCurrent({ type: "DiarySexp", raw: line }, lineNumber);
        i += 1;
        continue;
      }
      const fixedWidth = parseFixedWidth(lines, i);
      if (fixedWidth) {
        flushParagraph();
        endList();
        flushAffiliatedKeywords();
        pushCurrent(fixedWidth.node, lineNumber, fixedWidth.nextLineIndex);
        i = fixedWidth.nextLineIndex;
        continue;
      }
      {
        const keyword = parseKeywordLine(line, lineNumber);
        if (keyword) {
          flushParagraph();
          endList();
          if (isAffiliatedKeyword(keyword)) {
            setSourceRange(keyword, lineNumber, lineNumber);
            pendingAffiliatedKeywords.push(keyword);
            i += 1;
            continue;
          }
          flushAffiliatedKeywords();
          pushCurrent(keyword, lineNumber);
          i += 1;
          continue;
        }
        const planning = parsePlanningLine(line, lineNumber);
        if (planning) {
          flushParagraph();
          endList();
          flushAffiliatedKeywords();
          for (const node of planning) pushCurrent(node, lineNumber);
          i += 1;
          continue;
        }
        const clock = parseClockLine(line, lineNumber);
        if (clock) {
          flushParagraph();
          endList();
          flushAffiliatedKeywords();
          pushCurrent(clock, lineNumber);
          i += 1;
          continue;
        }
        const comment = parseCommentLine(line, lineNumber);
        if (comment) {
          flushParagraph();
          endList();
          flushAffiliatedKeywords();
          pushCurrent(comment, lineNumber);
          i += 1;
          continue;
        }
        const directive = parseSrcBlockLine(line, lineNumber);
        if (directive) {
          if (isBeginSrc(directive)) {
            flushParagraph();
            endList();
            const { block, nextLineIndex } = parseSrcBlock(lines, i);
            pushCurrent(attachAffiliatedKeywords(block), lineNumber, nextLineIndex);
            i = nextLineIndex;
            continue;
          }
          const kind = getBlockKindFromBegin(directive);
          if (kind) {
            flushParagraph();
            endList();
            const { block, nextLineIndex } = parseBlock(lines, i, kind);
            pushCurrent(attachAffiliatedKeywords(block), lineNumber, nextLineIndex);
            i = nextLineIndex;
            continue;
          }
          flushParagraph();
          endList();
          flushAffiliatedKeywords();
          pushCurrent({
            type: "DirectiveLine",
            raw: line,
            indent: directive.indent,
            keywordRaw: directive.keywordRaw,
            afterKeywordRaw: directive.afterKeywordRaw
          }, lineNumber);
          i += 1;
          continue;
        }
      }
      if (matchTableLine(line, "")) {
        flushParagraph();
        endList();
        const { table, nextLineIndex, tableEndLineIndex } = parseTable(lines, i, "");
        pushCurrent(attachAffiliatedKeywords(table), lineNumber, tableEndLineIndex);
        i = nextLineIndex;
        continue;
      }
      if (isHeadlineStart(line)) {
        flushParagraph();
        endList();
        flushAffiliatedKeywords();
        const { level, title, todo, priority, commented, tags } = parseHeadline(line, lineNumber, keywords);
        while (headlineStack.length > 0 && headlineStack[headlineStack.length - 1].level >= level) {
          const popped = headlineStack.pop();
          if (popped) finalizeHeadline(popped, lineNumber - 1);
        }
        const node = {
          type: "Headline",
          level,
          ...todo ? { todo, ...sequences.length ? { todoTerminal: isTerminalTodoKeyword(todo, sequences) } : {} } : {},
          ...priority ? { priority } : {},
          ...commented ? { commented } : {},
          ...tags ? { tags } : {},
          title: parseInlinesFromText(title),
          children: []
        };
        pushCurrent(node, lineNumber);
        headlineStack.push(node);
        i += 1;
        continue;
      }
      if (/^\s*:PROPERTIES:$/.test(line)) {
        flushParagraph();
        endList();
        flushAffiliatedKeywords();
        const { drawer, nextLineIndex } = parsePropertyDrawer(lines, i);
        pushCurrent(drawer, lineNumber, nextLineIndex);
        i = nextLineIndex;
        continue;
      }
      {
        const matchDrawerStart = /^(\s*):([^:\s]+):$/.exec(line);
        const isDrawerStart = matchDrawerStart && matchDrawerStart[2] !== "END";
        if (isDrawerStart) {
          flushParagraph();
          endList();
          flushAffiliatedKeywords();
          const { drawer, nextLineIndex } = parseDrawer(lines, i);
          pushCurrent(drawer, lineNumber, nextLineIndex);
          i = nextLineIndex;
          continue;
        }
      }
      if (isBlank(line)) {
        flushParagraph();
        endList();
        flushAffiliatedKeywords();
        pendingBlankLinesBeforeNextNode += 1;
        i += 1;
        continue;
      }
      const listItem = parseListItemLine(line) ?? (() => {
        const trimmed = line.trimStart();
        if (trimmed === line) return null;
        const parsed = parseListItemLine(trimmed);
        if (!parsed) return null;
        const leadingSpaces = line.length - trimmed.length;
        return { ...parsed, indentColumn: parsed.indentColumn + leadingSpaces };
      })();
      if (listItem) {
        let flushItemParagraph2 = function() {
          if (itemParagraphLines.length === 0) return;
          item.children.push(paragraphFromLines(itemParagraphLines));
          itemParagraphLines = [];
        };
        var flushItemParagraph = flushItemParagraph2;
        flushParagraph();
        flushAffiliatedKeywords();
        const item = addListItem(
          listItem.ordered,
          listItem.content,
          listItem.checkbox,
          listItem.progressCookie,
          listItem.ordinal,
          listItem.counter,
          listItem.descriptionTag
        );
        const itemStartLine = lineNumber;
        i += 1;
        let itemParagraphLines = [];
        while (i < lines.length) {
          const contLineNumber = i + 1;
          const contLine = lines[i] ?? "";
          if (isBlank(contLine)) {
            flushItemParagraph2();
            break;
          }
          if (isHeadlineStart(contLine)) {
            flushItemParagraph2();
            break;
          }
          const maybeNextItem = parseListItemLine(contLine);
          if (maybeNextItem) {
            flushItemParagraph2();
            break;
          }
          const leadingSpaces = contLine.match(/^(\s*)/)?.[1]?.length ?? 0;
          if (leadingSpaces > 0) {
            const unindentedLine = contLine.slice(leadingSpaces);
            const maybeNestedItem = parseListItemLine(unindentedLine);
            if (maybeNestedItem) {
              const nestedIndentColumn = leadingSpaces + maybeNestedItem.indentColumn;
              if (nestedIndentColumn > listItem.indentColumn) {
                flushItemParagraph2();
                let nestedList = null;
                let nestedListIndentColumn = null;
                let nestedItemParaLines = [];
                while (i < lines.length) {
                  const nestedLine = lines[i] ?? "";
                  if (isBlank(nestedLine)) {
                    if (nestedItemParaLines.length > 0) {
                      nestedItemParaLines = [];
                    }
                    break;
                  }
                  if (isHeadlineStart(nestedLine)) {
                    break;
                  }
                  const nestedLeadingSpaces = nestedLine.match(/^(\s*)/)?.[1]?.length ?? 0;
                  if (nestedLeadingSpaces === 0) {
                    break;
                  }
                  const nestedUnindentedLine = nestedLine.slice(nestedLeadingSpaces);
                  const nestedItem = parseListItemLine(nestedUnindentedLine);
                  if (!nestedItem) {
                    break;
                  }
                  const nestedItemIndentColumn = nestedLeadingSpaces + nestedItem.indentColumn;
                  if (nestedListIndentColumn === null) {
                    nestedListIndentColumn = nestedItemIndentColumn;
                  } else if (nestedItemIndentColumn < nestedListIndentColumn) {
                    break;
                  }
                  if (!nestedList) {
                    nestedList = {
                      type: "List",
                      ordered: nestedItem.ordered,
                      items: []
                    };
                    item.children.push(nestedList);
                  }
                  if (nestedList.ordered !== nestedItem.ordered) {
                    break;
                  }
                  if (nestedItemParaLines.length > 0) {
                    nestedItemParaLines = [];
                  }
                  const nestedOrdinal = nestedItem.ordered && nestedItem.ordinal !== nestedList.items.length + 1 ? nestedItem.ordinal : void 0;
                  const nestedListItem = listItemNode(
                    nestedItem.content,
                    nestedItem.checkbox,
                    nestedItem.progressCookie,
                    nestedOrdinal,
                    nestedItem.counter,
                    nestedItem.descriptionTag
                  );
                  const nestedStartLine = i + 1;
                  nestedList.items.push(nestedListItem);
                  i += 1;
                  while (i < lines.length) {
                    const contNestedLine = lines[i] ?? "";
                    if (isBlank(contNestedLine)) {
                      if (nestedItemParaLines.length > 0) {
                        nestedListItem.children.push(paragraphFromLines(nestedItemParaLines));
                        nestedItemParaLines = [];
                      }
                      break;
                    }
                    if (isHeadlineStart(contNestedLine)) {
                      if (nestedItemParaLines.length > 0) {
                        nestedListItem.children.push(paragraphFromLines(nestedItemParaLines));
                        nestedItemParaLines = [];
                      }
                      break;
                    }
                    const nextNestedLeadingSpaces = contNestedLine.match(/^(\s*)/)?.[1]?.length ?? 0;
                    if (nextNestedLeadingSpaces > 0) {
                      const nextNestedUnindentedLine = contNestedLine.slice(nextNestedLeadingSpaces);
                      const nextNestedItem = parseListItemLine(nextNestedUnindentedLine);
                      if (nextNestedItem) {
                        const nextNestedItemIndentColumn = nextNestedLeadingSpaces + nextNestedItem.indentColumn;
                        if (nextNestedItemIndentColumn > nestedItemIndentColumn) {
                          if (nestedItemParaLines.length > 0) {
                            nestedListItem.children.push(paragraphFromLines(nestedItemParaLines));
                            nestedItemParaLines = [];
                          }
                          const deeperList = {
                            type: "List",
                            ordered: nextNestedItem.ordered,
                            items: []
                          };
                          nestedListItem.children.push(deeperList);
                          while (i < lines.length) {
                            const deeperLine = lines[i] ?? "";
                            if (isBlank(deeperLine) || isHeadlineStart(deeperLine)) break;
                            const deeperLeadingSpaces = deeperLine.match(/^(\s*)/)?.[1]?.length ?? 0;
                            const deeperUnindentedLine = deeperLine.slice(deeperLeadingSpaces);
                            const deeperItem = parseListItemLine(deeperUnindentedLine);
                            if (!deeperItem) break;
                            const deeperItemIndentColumn = deeperLeadingSpaces + deeperItem.indentColumn;
                            if (deeperItemIndentColumn < nextNestedItemIndentColumn) break;
                            if (deeperList.ordered != deeperItem.ordered) break;
                            const deeperOrdinal = deeperItem.ordered && deeperItem.ordinal !== deeperList.items.length + 1 ? deeperItem.ordinal : void 0;
                            const deeperListItem = listItemNode(
                              deeperItem.content,
                              deeperItem.checkbox,
                              deeperItem.progressCookie,
                              deeperOrdinal,
                              deeperItem.counter,
                              deeperItem.descriptionTag
                            );
                            setSourceRange(deeperListItem, i + 1, i + 1);
                            deeperList.items.push(deeperListItem);
                            i += 1;
                          }
                          continue;
                        }
                        if (nestedItemParaLines.length > 0) {
                          nestedListItem.children.push(paragraphFromLines(nestedItemParaLines));
                          nestedItemParaLines = [];
                        }
                        break;
                      }
                    }
                    if (!contNestedLine.startsWith(" ".repeat(nestedItemIndentColumn))) {
                      if (nestedItemParaLines.length > 0) {
                        nestedListItem.children.push(paragraphFromLines(nestedItemParaLines));
                        nestedItemParaLines = [];
                      }
                      break;
                    }
                    nestedItemParaLines.push(contNestedLine.slice(nestedItemIndentColumn));
                    i += 1;
                  }
                  setSourceRange(nestedListItem, nestedStartLine, i);
                }
                continue;
              }
            }
          }
          if (!contLine.startsWith(" ".repeat(listItem.indentColumn))) {
            flushItemParagraph2();
            break;
          }
          const directive = parseSrcBlockLine(contLine, contLineNumber);
          if (directive) {
            if (isBeginSrc(directive)) {
              flushItemParagraph2();
              const { block, nextLineIndex } = parseSrcBlock(lines, i);
              item.children.push(block);
              i = nextLineIndex;
              continue;
            }
            const kind = getBlockKindFromBegin(directive);
            if (kind) {
              flushItemParagraph2();
              const { block, nextLineIndex } = parseBlock(lines, i, kind);
              item.children.push(block);
              i = nextLineIndex;
              continue;
            }
            flushItemParagraph2();
            item.children.push({
              type: "DirectiveLine",
              raw: contLine,
              indent: directive.indent,
              keywordRaw: directive.keywordRaw,
              afterKeywordRaw: directive.afterKeywordRaw
            });
            i += 1;
            continue;
          }
          if (matchTableLine(contLine, " ".repeat(listItem.indentColumn))) {
            flushItemParagraph2();
            const { table, nextLineIndex } = parseTable(lines, i, " ".repeat(listItem.indentColumn));
            item.children.push(table);
            i = nextLineIndex;
            continue;
          }
          itemParagraphLines.push(contLine.slice(listItem.indentColumn));
          i += 1;
        }
        setSourceRange(item, itemStartLine, i);
        continue;
      }
      endList();
      flushAffiliatedKeywords();
      if (paragraphStartLine === null) paragraphStartLine = lineNumber;
      paragraphLines.push(line);
      i += 1;
    }
    flushAffiliatedKeywords();
    flushParagraph();
    while (headlineStack.length > 0) {
      const popped = headlineStack.pop();
      if (popped) finalizeHeadline(popped, documentEndLine);
    }
    return doc;
  }

  // src/colorBinding.ts
  var NAMED_COLORS = {
    black: "1c1c1e",
    blue: "007aff",
    brown: "a2845e",
    gray: "8e8e93",
    green: "34c759",
    grey: "8e8e93",
    indigo: "5856d6",
    mint: "00c7be",
    orange: "ff9500",
    pink: "ff2d55",
    purple: "af52de",
    red: "ff3b30",
    teal: "30b0c7",
    white: "f2f2f7",
    yellow: "ffcc00"
  };
  function parseOrgColorValue(raw) {
    const source = String(raw || "").trim();
    if (!source) return null;
    const namedHex = NAMED_COLORS[source.toLowerCase()];
    if (namedHex) return { source, css: `#${namedHex}`, hex: namedHex };
    const match = /^#([a-f\d]{3}|[a-f\d]{6})$/i.exec(source);
    if (!match) return null;
    const compact = (match[1] || "").toLowerCase();
    const hex = compact.length === 3 ? compact.split("").map((digit) => `${digit}${digit}`).join("") : compact;
    return { source, css: `#${hex}`, hex };
  }
  function parseOrgColorBindingTarget(rawTarget) {
    const target = String(rawTarget || "").trim();
    if (!/^color:/i.test(target)) return null;
    const body = target.slice(target.indexOf(":") + 1).trim();
    if (!body) return null;
    const binding = {};
    const declarations = body.split(";").map((value) => value.trim()).filter(Boolean);
    for (const declaration of declarations) {
      const equals = declaration.indexOf("=");
      if (equals === -1) {
        if (declarations.length !== 1 || binding.foreground) return null;
        const color2 = parseOrgColorValue(declaration);
        if (!color2) return null;
        binding.foreground = color2;
        continue;
      }
      const key = declaration.slice(0, equals).trim().toLowerCase();
      const color = parseOrgColorValue(declaration.slice(equals + 1));
      if (!color) return null;
      if (["fg", "foreground", "text"].includes(key)) {
        if (binding.foreground) return null;
        binding.foreground = color;
      } else if (["bg", "background"].includes(key)) {
        if (binding.background) return null;
        binding.background = color;
      } else {
        return null;
      }
    }
    return binding.foreground || binding.background ? binding : null;
  }

  // src/link-abbrev.ts
  var ABBREVIATION_PREFIX_RE = /^[A-Za-z][A-Za-z0-9+.-]*$/;
  var RESERVED_PREFIXES = /* @__PURE__ */ new Set(["http", "https", "mailto", "file", "id"]);
  function normalizePrefix(value) {
    return String(value || "").trim().toLowerCase();
  }
  function normalizeTemplate(value) {
    return String(value || "").trim();
  }
  function parseLinkAbbreviationEntry(prefixRaw, templateRaw) {
    const prefix = normalizePrefix(prefixRaw);
    const template = normalizeTemplate(templateRaw);
    if (!prefix || !template) return null;
    if (!ABBREVIATION_PREFIX_RE.test(prefix)) return null;
    return { prefix, template };
  }
  function parseLinkAbbreviationDefinition(raw) {
    const definition = String(raw || "").trim();
    if (!definition) return null;
    const match = /^([^\s]+)\s+(.+?)\s*$/.exec(definition);
    if (!match) return null;
    return parseLinkAbbreviationEntry(match[1] || "", match[2] || "");
  }
  function setIfValid(out, prefixRaw, templateRaw) {
    const entry = parseLinkAbbreviationEntry(prefixRaw, templateRaw);
    if (!entry) return;
    out.set(entry.prefix, entry.template);
  }
  function buildBuiltInLinkAbbreviations(linearTeam) {
    const out = /* @__PURE__ */ new Map();
    out.set("gh", "https://github.com/%s");
    out.set("gl", "https://gitlab.com/%s");
    out.set("yt", "https://www.youtube.com/watch?v=%s");
    out.set("wiki", "https://en.wikipedia.org/wiki/%s");
    const team = String(linearTeam || "").trim();
    if (team) {
      out.set("linear", `https://linear.app/${team}/issue/%s`);
    }
    return out;
  }
  function collectLinkAbbreviationsFromDoc(doc) {
    const out = /* @__PURE__ */ new Map();
    for (const node of doc.children) {
      if (node.type !== "KeywordLine") continue;
      if (String(node.keyRaw || "").trim().toUpperCase() !== "LINK") continue;
      const entry = parseLinkAbbreviationDefinition(node.valueRaw || "");
      if (!entry) continue;
      out.set(entry.prefix, entry.template);
    }
    return out;
  }
  function collectLinkAbbreviationsFromRecord(record) {
    const out = /* @__PURE__ */ new Map();
    if (!record || typeof record !== "object") return out;
    for (const [prefix, template] of Object.entries(record)) {
      setIfValid(out, prefix, template);
    }
    return out;
  }
  function mergeLinkAbbreviations(sources) {
    const out = /* @__PURE__ */ new Map();
    for (const source of sources) {
      if (!source) continue;
      for (const [prefix, template] of source.entries()) {
        out.set(prefix, template);
      }
    }
    return out;
  }
  function expandLinkAbbreviationTarget(targetRaw, abbreviations) {
    const target = String(targetRaw || "").trim();
    if (!target || !abbreviations || abbreviations.size === 0) return target;
    const match = /^([A-Za-z][A-Za-z0-9+.-]*):(.*)$/.exec(target);
    if (!match) return target;
    const prefix = normalizePrefix(match[1]);
    if (!prefix || RESERVED_PREFIXES.has(prefix)) return target;
    const suffix = match[2] ?? "";
    const template = abbreviations.get(prefix);
    if (!template) return target;
    if (template.includes("%s")) {
      return template.replace(/%s/g, suffix);
    }
    return `${template}${suffix}`;
  }

  // src/publish-defaults.ts
  var PRISM_LIGHT_THEME_LINK = '<link id="org2-prism-light" rel="stylesheet" href="https://cdn.jsdelivr.net/npm/prismjs/themes/prism.min.css" media="all" />';
  var PRISM_DARK_THEME_LINK = '<link id="org2-prism-dark" rel="stylesheet" href="https://cdn.jsdelivr.net/npm/prismjs/themes/prism-okaidia.min.css" media="not all" />';
  var PRISM_LIGHT_TEXT_FIX_STYLE = '<style>:root[data-theme="light"] code[class*="language-"],:root[data-theme="light"] pre[class*="language-"]{color:#1f2937!important;} @media (prefers-color-scheme: light){:root:not([data-theme]) code[class*="language-"],:root:not([data-theme]) pre[class*="language-"]{color:#1f2937!important;}}</style>';
  var PRISM_THEME_AUTODETECT_SCRIPT = '<script>(function(){var root=document.documentElement;function hasDarkToken(s){return /(^|\\s)(dark|theme-dark|dark-mode)(\\s|$)/.test((s||"").toLowerCase());}function luminanceFromRgb(rgb){var m=(rgb||"").match(/d+(?:.d+)?/g);if(!m||m.length<3)return null;var r=Number(m[0]),g=Number(m[1]),b=Number(m[2]);if(!Number.isFinite(r)||!Number.isFinite(g)||!Number.isFinite(b))return null;return (0.2126*r+0.7152*g+0.0722*b)/255;}function inferDarkFromStyles(){try{var el=document.body||root;var bg=getComputedStyle(el).backgroundColor;var l=luminanceFromRgb(bg);if(l===null)return null;return l<0.5;}catch(_){return null;}}function pick(){var explicit=(root.getAttribute("data-theme")||"").toLowerCase();var rootDark=hasDarkToken(root.className||"");var bodyDark=hasDarkToken((document.body&&document.body.className)||"");var inferred=inferDarkFromStyles();var dark=(explicit==="dark")||(explicit!=="light"&&(rootDark||bodyDark||(inferred===null?window.matchMedia("(prefers-color-scheme: dark)").matches:inferred)));var l=document.getElementById("org2-prism-light");var d=document.getElementById("org2-prism-dark");if(l&&d){l.media=dark?"not all":"all";d.media=dark?"all":"not all";}}pick();try{window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change",pick);}catch(_){};if(typeof MutationObserver!=="undefined"){var mo=new MutationObserver(pick);mo.observe(root,{attributes:true,attributeFilter:["class","data-theme","style"]});if(document.body){mo.observe(document.body,{attributes:true,attributeFilter:["class","data-theme","style"]});}else{document.addEventListener("DOMContentLoaded",function(){if(document.body)mo.observe(document.body,{attributes:true,attributeFilter:["class","data-theme","style"]});pick();},{once:true});}}window.addEventListener("load",pick);})();<\/script>';
  var PRISM_CORE_SCRIPT = '<script defer src="https://cdn.jsdelivr.net/npm/prismjs/prism.min.js"><\/script>';
  var PRISM_LANGUAGE_SCRIPTS = [
    '<script defer src="https://cdn.jsdelivr.net/npm/prismjs/components/prism-haskell.min.js"><\/script>',
    '<script defer src="https://cdn.jsdelivr.net/npm/prismjs/components/prism-bash.min.js"><\/script>',
    '<script defer src="https://cdn.jsdelivr.net/npm/prismjs/components/prism-json.min.js"><\/script>',
    '<script defer src="https://cdn.jsdelivr.net/npm/prismjs/components/prism-typescript.min.js"><\/script>',
    '<script defer src="https://cdn.jsdelivr.net/npm/prismjs/components/prism-jsx.min.js"><\/script>'
  ];
  var DEFAULT_SYNTAX_HEAD_INCLUDES = [
    PRISM_LIGHT_THEME_LINK,
    PRISM_DARK_THEME_LINK,
    PRISM_LIGHT_TEXT_FIX_STYLE,
    PRISM_THEME_AUTODETECT_SCRIPT,
    PRISM_CORE_SCRIPT,
    ...PRISM_LANGUAGE_SCRIPTS
  ];
  var COMPAT_CONTENT_OPEN = '<div id="content" class="content">\n';
  var COMPAT_CONTENT_CLOSE = "</div>\n";
  var COMPAT_CONTENT_STYLE_SECTION = "<style>\n#content { max-width: 60em; margin: auto; line-height: 1.35; }\n#content li > p { margin: 0; }\n#content li + li { margin-top: 0.2rem; }\n</style>\n";

  // src/presentation.ts
  function presentationKeyword(keyRaw, valueRaw) {
    const key = keyRaw.trim().toUpperCase();
    const value = valueRaw.trim().toLowerCase();
    if (key === "LATEX_CLASS" && value.split(/\s+/)[0] === "beamer") return true;
    if (key === "LATEX_CLASS_OPTIONS" && /(^|[^a-z0-9_-])presentation([^a-z0-9_-]|$)/.test(value)) return true;
    if (["ORG2_DOCUMENT_KIND", "ORG2_PREVIEW", "ORG2_VIEW"].includes(key)) {
      return ["slides", "presentation", "beamer"].includes(value);
    }
    return key.startsWith("BEAMER_") || key.startsWith("SLIDE_") || key.startsWith("ORG2_SLIDE_");
  }
  function presentationProperty(keyRaw) {
    const key = keyRaw.trim().toUpperCase();
    return key.startsWith("BEAMER_") || key.startsWith("SLIDE_") || key.startsWith("ORG2_SLIDE_");
  }
  function nodeDeclaresPresentation(node) {
    switch (node.type) {
      case "KeywordLine":
        return presentationKeyword(node.keyRaw, node.valueRaw);
      case "PropertyDrawer":
        return node.properties.some((property2) => presentationProperty(property2.key));
      case "Drawer":
        return node.nameRaw.trim().toUpperCase() === "PROPERTIES" && node.bodyRaw.split("\n").some((line) => {
          const match = /^\s*:([^:\s]+):/.exec(line);
          return Boolean(match?.[1] && presentationProperty(match[1]));
        });
      case "Block": {
        const exportBackend = node.kind === "export" ? node.begin.afterKeywordRaw.trim().split(/\s+/)[0]?.toLowerCase() : void 0;
        return exportBackend === "beamer" || (node.affiliatedKeywords || []).some((keyword) => presentationKeyword(keyword.keyRaw, keyword.valueRaw));
      }
      case "SrcBlock":
      case "Table":
        return (node.affiliatedKeywords || []).some((keyword) => presentationKeyword(keyword.keyRaw, keyword.valueRaw));
      case "Headline":
      case "ListItem":
        return node.children.some(nodeDeclaresPresentation);
      case "List":
        return node.items.some(nodeDeclaresPresentation);
      default:
        return false;
    }
  }
  function isPresentationDocument(doc) {
    return doc.children.some(nodeDeclaresPresentation);
  }

  // src/tableFormula.ts
  function cloneTable(table) {
    return {
      ...table,
      rows: table.rows.map((row) => row.type === "TableRow" ? { ...row, cells: [...row.cells], contents: row.contents?.map((items) => [...items]) } : { ...row }),
      formulas: table.formulas?.map((line) => ({ ...line, assignments: line.assignments.map((item) => ({ ...item })) }))
    };
  }
  function dataRows(table) {
    return table.rows.filter((row) => row.type === "TableRow");
  }
  function dataRowIndexForPhysical(table, physical) {
    let index = 0;
    for (let i = 0; i < physical; i += 1) if (table.rows[i]?.type === "TableRow") index += 1;
    return index;
  }
  function firstBodyRow(table) {
    const hline = table.rows.findIndex((row) => row.type === "TableHline");
    return hline < 0 ? 1 : dataRowIndexForPhysical(table, hline) + 1;
  }
  function columnCount(table) {
    return dataRows(table).reduce((max, row) => Math.max(max, row.cells.length), 0);
  }
  function parseCellText(raw, emptyAsZero) {
    const text2 = raw.trim();
    if (text2 === "" && emptyAsZero) return 0;
    if (/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)(?:e[+-]?\d+)?$/i.test(text2)) return Number(text2);
    if (/^[+-]?\d+:\d{2}(?::\d{2}(?:\.\d+)?)?$/.test(text2)) {
      const sign = text2.startsWith("-") ? -1 : 1;
      const fields = text2.replace(/^[+-]/, "").split(":").map(Number);
      return sign * (fields.length === 3 ? fields[0] * 3600 + fields[1] * 60 + fields[2] : fields[0] * 3600 + fields[1] * 60);
    }
    return text2;
  }
  function scalarNumber(value) {
    if (typeof value === "number") return value;
    if (typeof value === "boolean") return value ? 1 : 0;
    const parsed = Number(value);
    if (!Number.isFinite(parsed)) throw new Error(`Expected a number, got ${JSON.stringify(value)}`);
    return parsed;
  }
  function flattened(values) {
    return values.flatMap((value) => Array.isArray(value) ? value : [value]);
  }
  function splitMode(mode = "") {
    const flags = /* @__PURE__ */ new Set();
    let format;
    for (const original of mode.split(/[;,\s]+/).filter(Boolean)) {
      let token = original;
      const printf = /%[-+ #0]*\d*(?:\.\d+)?[feg]$/i.exec(token);
      const calc = /[fesn]\d+$/i.exec(token);
      const matchedFormat = printf ?? calc;
      if (matchedFormat) {
        format = matchedFormat[0];
        token = token.slice(0, matchedFormat.index);
      }
      for (const flag of token) flags.add(flag);
    }
    return { flags, format };
  }
  function formatValue(value, mode) {
    if (Array.isArray(value)) value = value[0] ?? "";
    const { flags, format } = splitMode(mode);
    if (typeof value === "boolean") return value ? "1" : "0";
    if (typeof value === "string") return value;
    if (!Number.isFinite(value)) return "nan";
    if (flags.has("t")) return String(value / 3600);
    if (flags.has("T") || flags.has("U")) {
      const sign = value < 0 ? "-" : "";
      let seconds = Math.abs(value);
      const hours = Math.floor(seconds / 3600);
      seconds -= hours * 3600;
      const minutes = Math.floor(seconds / 60);
      const secs = Math.round(seconds - minutes * 60);
      return flags.has("U") ? `${sign}${hours}:${String(minutes).padStart(2, "0")}` : `${sign}${hours}:${String(minutes).padStart(2, "0")}:${String(secs).padStart(2, "0")}`;
    }
    if (format?.startsWith("%")) {
      const match = /\.([0-9]+)([feg])$/i.exec(format);
      const digits = Number(match?.[1] ?? 6);
      const kind = match?.[2]?.toLowerCase();
      if (kind === "f") return value.toFixed(digits);
      if (kind === "e") return value.toExponential(digits);
      if (kind === "g") return value.toPrecision(digits);
    }
    if (format) {
      const digits = Number(format.slice(1));
      if (format[0] === "f") return value.toFixed(digits);
      if (format[0] === "e") return value.toExponential(digits);
      if (format[0] === "s") return value.toPrecision(digits);
      if (format[0] === "n") return value.toPrecision(digits);
    }
    return Number.isInteger(value) ? String(value) : String(Number(value.toPrecision(12)));
  }
  function tokenize(expression) {
    const tokens = [];
    let i = 0;
    while (i < expression.length) {
      const rest = expression.slice(i);
      const whitespace = /^\s+/.exec(rest);
      if (whitespace) {
        i += whitespace[0].length;
        continue;
      }
      const string = /^("(?:\\.|[^"\\])*"|'(?:\\.|[^'\\])*')/.exec(rest);
      if (string) {
        tokens.push({ kind: "string", value: string[0] });
        i += string[0].length;
        continue;
      }
      const reference = /^(?:@[+\-]?(?:\d+|[<>]+|I+|#|[A-Za-z][\w-]*)\$[+\-]?(?:\d+|[<>]+|#|[A-Za-z][\w-]*)|@[+\-]?(?:\d+|[<>]+|I+|#)|\$[+\-]?(?:\d+|[<>]+|#|[A-Za-z][\w-]*)|[A-Z]+\d+)(?:\.\.(?:@[+\-]?(?:\d+|[<>]+|I+|#|[A-Za-z][\w-]*)\$[+\-]?(?:\d+|[<>]+|#|[A-Za-z][\w-]*)|@[+\-]?(?:\d+|[<>]+|I+|#)|\$[+\-]?(?:\d+|[<>]+|#|[A-Za-z][\w-]*)|[A-Z]+\d+))?/.exec(rest);
      if (reference) {
        tokens.push({ kind: "reference", value: reference[0] });
        i += reference[0].length;
        continue;
      }
      const number = /^(?:\d+(?:\.\d*)?|\.\d+)(?:e[+\-]?\d+)?/i.exec(rest);
      if (number) {
        tokens.push({ kind: "number", value: number[0] });
        i += number[0].length;
        continue;
      }
      const name = /^[A-Za-z_][\w-]*/.exec(rest);
      if (name) {
        tokens.push({ kind: "name", value: name[0] });
        i += name[0].length;
        continue;
      }
      const op = /^(?:<=|>=|==|!=|&&|\|\||[-+*/%^(),<>])/.exec(rest);
      if (op) {
        tokens.push({ kind: "operator", value: op[0] });
        i += op[0].length;
        continue;
      }
      throw new Error(`Unsupported formula syntax near ${JSON.stringify(rest.slice(0, 20))}`);
    }
    tokens.push({ kind: "eof", value: "" });
    return tokens;
  }
  function lettersToColumn(value) {
    let result = 0;
    for (const ch of value) result = result * 26 + ch.charCodeAt(0) - 64;
    return result;
  }
  function referenceCoordinate(part, current, rows, columns) {
    const a1 = /^([A-Z]+)(\d+)$/.exec(part);
    if (a1) return { row: Number(a1[2]), column: lettersToColumn(a1[1]) };
    let row = current.row;
    let column = current.column;
    const rowMatch = /^@([^$]+)/.exec(part);
    const colMatch = /\$([^$]+)$/.exec(part);
    const resolve = (raw, at, last) => {
      if (raw === "#" || raw === "0") return at;
      if (/^<+$/.test(raw)) return 1 + raw.length - 1;
      if (/^>+$/.test(raw)) return last - raw.length + 1;
      if (/^[+-]\d+$/.test(raw)) return at + Number(raw);
      if (/^\d+$/.test(raw)) return Number(raw);
      throw new Error(`Unsupported named or hline reference ${raw}`);
    };
    if (rowMatch) row = resolve(rowMatch[1], current.row, rows);
    if (colMatch) column = resolve(colMatch[1], current.column, columns);
    return { row, column };
  }
  function readReference(raw, current, table, emptyAsZero) {
    const rows = dataRows(table);
    const columns = columnCount(table);
    const [startRaw, endRaw] = raw.split("..");
    const start = referenceCoordinate(startRaw, current, rows.length, columns);
    if (!endRaw) {
      if (start.row < 1 || start.row > rows.length || start.column < 1 || start.column > columns) throw new Error(`Reference ${raw} is outside the table`);
      return parseCellText(rows[start.row - 1]?.cells[start.column - 1] ?? "", emptyAsZero);
    }
    const end = referenceCoordinate(endRaw, current, rows.length, columns);
    const result = [];
    for (let row = Math.min(start.row, end.row); row <= Math.max(start.row, end.row); row += 1) {
      for (let column = Math.min(start.column, end.column); column <= Math.max(start.column, end.column); column += 1) {
        const rawCell = rows[row - 1]?.cells[column - 1] ?? "";
        if (rawCell.trim() !== "" || emptyAsZero) result.push(parseCellText(rawCell, emptyAsZero));
      }
    }
    return result;
  }
  var ExpressionParser = class {
    constructor(tokens, table, current, emptyAsZero) {
      this.tokens = tokens;
      this.table = table;
      this.current = current;
      this.emptyAsZero = emptyAsZero;
    }
    tokens;
    table;
    current;
    emptyAsZero;
    index = 0;
    parse() {
      const value = this.binary(0);
      if (this.peek().kind !== "eof") throw new Error(`Unexpected token ${this.peek().value}`);
      return value;
    }
    peek() {
      return this.tokens[this.index];
    }
    take() {
      return this.tokens[this.index++];
    }
    binary(min) {
      let left = this.unary();
      const precedence = { "||": 1, "&&": 2, "==": 3, "!=": 3, "<": 4, "<=": 4, ">": 4, ">=": 4, "+": 5, "-": 5, "/": 6, "*": 7, "%": 7, "^": 8 };
      while (this.peek().kind === "operator" && (precedence[this.peek().value] ?? -1) >= min) {
        const op = this.take().value;
        const right = this.binary((precedence[op] ?? 0) + (op === "^" ? 0 : 1));
        left = this.apply(op, left, right);
      }
      return left;
    }
    unary() {
      if (this.peek().kind === "operator" && ["+", "-"].includes(this.peek().value)) {
        const op = this.take().value;
        const value = scalarNumber(this.scalar(this.unary()));
        return op === "-" ? -value : value;
      }
      return this.primary();
    }
    primary() {
      const token = this.take();
      if (token.kind === "number") return Number(token.value);
      if (token.kind === "string") return token.value.slice(1, -1).replace(/\\([\\"'])/g, "$1");
      if (token.kind === "reference") return readReference(token.value, this.current, this.table, this.emptyAsZero);
      if (token.kind === "operator" && token.value === "(") {
        const value = this.binary(0);
        if (this.take().value !== ")") throw new Error("Expected )");
        return value;
      }
      if (token.kind === "name") {
        if (token.value === "pi") return Math.PI;
        if (token.value === "e") return Math.E;
        if (this.peek().value !== "(") throw new Error(`Unknown name ${token.value}`);
        this.take();
        const args = [];
        if (this.peek().value !== ")") {
          do {
            args.push(this.binary(0));
            if (this.peek().value !== ",") break;
            this.take();
          } while (true);
        }
        if (this.take().value !== ")") throw new Error("Expected ) after function arguments");
        return this.call(token.value, args);
      }
      throw new Error(`Unexpected token ${token.value || "end of formula"}`);
    }
    scalar(value) {
      if (Array.isArray(value)) throw new Error("A range cannot be used as a scalar");
      return value;
    }
    apply(op, leftValue, rightValue) {
      const left = this.scalar(leftValue);
      const right = this.scalar(rightValue);
      if (op === "+" && (typeof left === "string" || typeof right === "string")) return String(left) + String(right);
      if (op === "+") return scalarNumber(left) + scalarNumber(right);
      if (op === "-") return scalarNumber(left) - scalarNumber(right);
      if (op === "*") return scalarNumber(left) * scalarNumber(right);
      if (op === "/") return scalarNumber(left) / scalarNumber(right);
      if (op === "%") return scalarNumber(left) % scalarNumber(right);
      if (op === "^") return scalarNumber(left) ** scalarNumber(right);
      if (op === "&&") return Boolean(left) && Boolean(right);
      if (op === "||") return Boolean(left) || Boolean(right);
      if (op === "==") return left === right;
      if (op === "!=") return left !== right;
      if (op === "<") return left < right;
      if (op === "<=") return left <= right;
      if (op === ">") return left > right;
      return left >= right;
    }
    call(name, args) {
      const values = flattened(args);
      const numbers = values.map(scalarNumber);
      const lower = name.toLowerCase();
      if (lower === "vsum") return numbers.reduce((sum, value) => sum + value, 0);
      if (lower === "vmean") return numbers.reduce((sum, value) => sum + value, 0) / numbers.length;
      if (lower === "vmin" || lower === "min") return Math.min(...numbers);
      if (lower === "vmax" || lower === "max") return Math.max(...numbers);
      if (lower === "vcount") return values.length;
      if (lower === "vmedian") {
        const sorted = [...numbers].sort((a, b) => a - b);
        const mid = Math.floor(sorted.length / 2);
        return sorted.length % 2 ? sorted[mid] : (sorted[mid - 1] + sorted[mid]) / 2;
      }
      const unary = { abs: Math.abs, round: Math.round, floor: Math.floor, ceil: Math.ceil, sqrt: Math.sqrt, exp: Math.exp, log: Math.log10, ln: Math.log, sin: (v) => Math.sin(v * Math.PI / 180), cos: (v) => Math.cos(v * Math.PI / 180), tan: (v) => Math.tan(v * Math.PI / 180) };
      if (unary[lower]) return unary[lower](numbers[0] ?? 0);
      if (lower === "pow") return (numbers[0] ?? 0) ** (numbers[1] ?? 0);
      if (lower === "string") return String(values[0] ?? "");
      if (lower === "if") return Boolean(values[0]) ? values[1] ?? "" : values[2] ?? "";
      throw new Error(`Unsupported function ${name}`);
    }
  };
  function targetCells(target, table) {
    const rows = dataRows(table).length;
    const columns = columnCount(table);
    const all = [];
    if (/^\$\d+$/.test(target)) {
      const column = Number(target.slice(1));
      for (let row = firstBodyRow(table); row <= rows; row += 1) all.push({ row, column });
      return all;
    }
    if (/^@\d+$/.test(target)) {
      const row = Number(target.slice(1));
      for (let column = 1; column <= columns; column += 1) all.push({ row, column });
      return all;
    }
    const parts = target.split("..");
    const start = referenceCoordinate(parts[0], { row: 1, column: 1 }, rows, columns);
    const end = parts[1] ? referenceCoordinate(parts[1], start, rows, columns) : start;
    for (let row = Math.min(start.row, end.row); row <= Math.max(start.row, end.row); row += 1) {
      for (let column = Math.min(start.column, end.column); column <= Math.max(start.column, end.column); column += 1) all.push({ row, column });
    }
    return all;
  }
  function evaluateTableNode(source, formulaIndex = 0) {
    const table = cloneTable(source);
    const diagnostics = [];
    const formula = table.formulas?.[formulaIndex];
    if (!formula) return { ok: false, table, changes: [], diagnostics: [{ message: `Table has no formula line ${formulaIndex + 1}`, formulaIndex }], formulaIndex };
    const jobs = /* @__PURE__ */ new Map();
    formula.assignments.forEach((assignment, assignmentIndex) => {
      try {
        if (!assignment.targetRaw) throw new Error("Formula assignment is missing a target");
        if (assignment.expressionRaw.startsWith("'")) throw new Error("Emacs Lisp table formulas are preserved but are not executed");
        const specificity = /^\$\d+$/.test(assignment.targetRaw) ? 1 : /^@\d+$/.test(assignment.targetRaw) ? 2 : 3;
        for (const cell of targetCells(assignment.targetRaw, table)) {
          const key = `${cell.row}:${cell.column}`;
          const existing = jobs.get(key);
          if (!existing || specificity >= existing.specificity) jobs.set(key, { ...cell, expression: assignment.expressionRaw, mode: assignment.modeRaw, assignmentIndex, specificity });
        }
      } catch (error) {
        diagnostics.push({ message: error instanceof Error ? error.message : String(error), formulaIndex, assignmentIndex });
      }
    });
    const beforeRows = dataRows(source);
    const rows = dataRows(table);
    if (diagnostics.length === 0) {
      const orderedJobs = [...jobs.values()].sort(
        (left, right) => left.specificity - right.specificity || left.assignmentIndex - right.assignmentIndex
      );
      for (const job of orderedJobs) {
        try {
          if (job.row < 1 || job.row > rows.length || job.column < 1) throw new Error("Formula target is outside the table");
          const { flags } = splitMode(job.mode);
          const value = new ExpressionParser(tokenize(job.expression), table, job, flags.has("N")).parse();
          while ((rows[job.row - 1]?.cells.length ?? 0) < job.column) rows[job.row - 1].cells.push("");
          rows[job.row - 1].cells[job.column - 1] = formatValue(value, job.mode);
        } catch (error) {
          diagnostics.push({ message: error instanceof Error ? error.message : String(error), formulaIndex, assignmentIndex: job.assignmentIndex, row: job.row, column: job.column });
        }
      }
    }
    if (diagnostics.length > 0) return { ok: false, table: cloneTable(source), changes: [], diagnostics, formulaIndex };
    const changes = [];
    rows.forEach((row, rowIndex) => row.cells.forEach((after, columnIndex) => {
      const before = beforeRows[rowIndex]?.cells[columnIndex] ?? "";
      if (before !== after) changes.push({ row: rowIndex + 1, column: columnIndex + 1, before, after });
    }));
    return { ok: true, table, changes, diagnostics, formulaIndex };
  }

  // src/export.ts
  function escapeHtml(value) {
    return String(value).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\"/g, "&quot;");
  }
  function escapeAttr(value) {
    return escapeHtml(value).replace(/`/g, "&#96;");
  }
  function normalizeStylesheets(stylesheets) {
    if (!Array.isArray(stylesheets)) return [];
    return stylesheets.map((href) => String(href || "").trim()).filter((href) => href.length > 0);
  }
  var DEFAULT_DOCUMENT_STYLE = `:root { color-scheme: light dark; }
body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; margin: 2rem auto; max-width: 860px; padding: 0 1rem; line-height: 1.5; }
main { display: grid; gap: 0.75rem; }
section.org2-headline { margin: 0.5rem 0 1rem; }
h1,h2,h3,h4,h5,h6 { margin: 1rem 0 0.5rem; line-height: 1.25; }
.org2-todo { font-size: 0.8em; font-weight: 700; letter-spacing: 0.02em; text-transform: uppercase; opacity: 0.9; }
.org2-tags { font-size: 0.8em; opacity: 0.8; }
.org2-tag { border: 1px solid currentColor; border-radius: 999px; padding: 0 0.35em; }
.org2-planning { font-size: 0.95em; opacity: 0.9; }
.org2-planning-kind { font-weight: 600; }
.org2-properties { display: grid; grid-template-columns: max-content 1fr; gap: 0.15rem 0.75rem; margin: 0.5rem 0; }
.org2-properties dt { font-weight: 600; }
.org2-properties dd { margin: 0; }
.org2-src, .org2-example, .org2-verse, .org2-comment, .org2-directive, pre { overflow-x: auto; padding: 0.75rem; border-radius: 0.5rem; background: rgba(127,127,127,0.12); }
.org2-center { text-align: center; }
.org2-underline { text-decoration: underline; }
table { border-collapse: collapse; width: 100%; margin: 0.5rem 0 1rem; }
th, td { border: 1px solid rgba(127,127,127,0.35); padding: 0.35rem 0.5rem; text-align: left; }
thead th { background: rgba(127,127,127,0.16); }
a { text-decoration-thickness: 0.08em; text-underline-offset: 0.15em; }`;
  var DOCUMENT_IMAGE_STYLE = `
.org2-image-figure { max-width: 100%; margin: 0.75rem 0 1rem; }
.org2-image-link { display: block; border: 0; }
.org2-image { display: block; width: auto; max-width: 100%; height: auto; border-radius: 0.5rem; }
li > .org2-image-figure { margin-top: 0.55rem; }`;
  var DOCUMENT_CHART_STYLE = `:root {
  --org2-chart-axis: #475569;
  --org2-chart-grid: #d7dee8;
  --org2-chart-mark: #2563eb;
  --org2-chart-label: #475569;
  --org2-chart-title: #0f172a;
  --org2-chart-surface: #ffffff;
}
@media (prefers-color-scheme: dark) {
  :root {
    --org2-chart-axis: rgba(233, 234, 237, 0.5);
    --org2-chart-grid: rgba(233, 234, 237, 0.12);
    --org2-chart-mark: #79b8ed;
    --org2-chart-label: #a4a8b0;
    --org2-chart-title: #e9eaed;
    --org2-chart-surface: #202226;
  }
}
.org2-chart { width: min(100%, 800px); margin: 1rem 0 1.35rem; overflow-x: auto; }
.org2-chart-compact { width: min(100%, 680px); }
.org2-chart-wide { width: 100%; }
.org2-chart svg { display: block; width: 100%; height: auto; margin: 0; }`;
  var DOCUMENT_TOC_STYLE = `.org2-toc { border: 1px solid rgba(127,127,127,0.35); border-radius: 0.5rem; padding: 0.75rem 1rem; margin: 0.25rem 0 1rem; }
.org2-toc h2 { margin: 0 0 0.5rem; font-size: 1rem; }
.org2-toc ul { margin: 0; padding-left: 1.25rem; display: grid; gap: 0.25rem; }
.org2-toc li.org2-toc-level-2 { margin-left: 0.75rem; }
.org2-toc li.org2-toc-level-3 { margin-left: 1.5rem; }
.org2-toc li.org2-toc-level-4 { margin-left: 2.25rem; }
.org2-toc li.org2-toc-level-5 { margin-left: 3rem; }
.org2-toc li.org2-toc-level-6 { margin-left: 3.75rem; }`;
  var APP_DOCUMENT_STYLE = `:root {
  color-scheme: light dark;
  --org2-text: #18201e;
  --org2-muted: #5e6b66;
  --org2-faint: rgba(40, 84, 215, 0.065);
  --org2-rule: #d7d6ce;
  --org2-code: #f1f3ef;
  --org2-surface: #fcfbf7;
  --org2-elevated-surface: #ffffff;
  --org2-shadow: rgba(29, 43, 38, 0.065);
  --org2-link: #2854d7;
  --org2-accent: #2854d7;
  --org2-signal: #c2472c;
  --org2-success: #20804a;
  --org2-danger: #b64238;
  --org2-warning: #966512;
  --org2-chart-axis: rgba(56, 61, 69, 0.54);
  --org2-chart-grid: rgba(56, 61, 69, 0.12);
  --org2-chart-mark: #3478d4;
  --org2-chart-label: #6c7078;
  --org2-chart-title: #24262a;
  --org2-chart-surface: #ffffff;
  --org2-font-mono: ui-monospace, "SFMono-Regular", Menlo, Consolas, monospace;
  --org2-content-width: 960px;
  --org2-page-padding: 28px;
}
@media (prefers-color-scheme: dark) {
  :root {
    --org2-text: #dce3de;
    --org2-muted: #a2ada7;
    --org2-faint: rgba(134, 163, 255, 0.09);
    --org2-rule: #36403c;
    --org2-code: #121715;
    --org2-surface: #1b211f;
    --org2-elevated-surface: #222927;
    --org2-shadow: rgba(0, 0, 0, 0.24);
    --org2-link: #86a3ff;
    --org2-accent: #86a3ff;
    --org2-signal: #ff8668;
    --org2-success: #6ac58c;
    --org2-danger: #ee8178;
    --org2-warning: #e0b361;
    --org2-chart-axis: rgba(233, 234, 237, 0.5);
    --org2-chart-grid: rgba(233, 234, 237, 0.12);
    --org2-chart-mark: #79b8ed;
    --org2-chart-label: #a4a8b0;
    --org2-chart-title: #e9eaed;
    --org2-chart-surface: #202226;
  }
}
*, *::before, *::after { box-sizing: border-box; }
html, body { width: 100%; max-width: 100%; min-height: 100%; margin: 0; background: transparent; overflow-x: hidden; }
body {
  color: var(--org2-text);
  font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif;
  font-size: 15px;
  line-height: 1.58;
  letter-spacing: 0;
  -webkit-font-smoothing: antialiased;
  overflow-wrap: anywhere;
}
main.org2-document {
  width: min(100%, var(--org2-content-width));
  max-width: 100%;
  margin: 0 auto;
  padding: 22px clamp(16px, 5vw, var(--org2-page-padding)) 64px;
}
main.org2-document,
main.org2-document > *,
section.org2-headline,
details.org2-headline,
.org2-headline-body { min-width: 0; }
.org2-document-header { margin: 0.15rem 0 1.1rem; }
.org2-document-title {
  margin: 0;
  font-size: clamp(1.9rem, 4vw, 2.45rem);
  font-weight: 650;
  line-height: 1.14;
  letter-spacing: -0.025em;
}
.org2-document-title::before {
  display: inline-block;
  margin-right: 0.48rem;
  color: var(--org2-signal);
  content: "*";
  font-family: var(--org2-font-mono);
  font-size: 0.38em;
  font-weight: 650;
  vertical-align: 0.72em;
}
.org2-document-subtitle { margin: 0.32rem 0 0; color: var(--org2-muted); font-size: 1rem; }
.org2-file-properties {
  margin: 0 0 1rem;
  color: var(--org2-muted);
  font-family: var(--org2-font-mono);
  font-size: 0.84rem;
}
.org2-file-properties > summary {
  position: relative;
  display: inline-flex;
  align-items: center;
  gap: 0.42rem;
  padding-left: 1rem;
  color: var(--org2-muted);
  cursor: pointer;
  list-style: none;
  font-weight: 600;
  user-select: none;
}
.org2-file-properties > summary::-webkit-details-marker { display: none; }
.org2-file-properties > summary::before {
  content: "\u25B6";
  position: absolute;
  left: 0;
  color: var(--org2-muted);
  font-size: 0.7rem;
}
.org2-file-properties[open] > summary::before { content: "\u25BC"; }
.org2-file-properties-count {
  min-width: 1.35rem;
  padding: 0.04rem 0.32rem;
  border-radius: 999px;
  background: var(--org2-faint);
  text-align: center;
  font-size: 0.72rem;
  font-variant-numeric: tabular-nums;
}
.org2-file-properties-body {
  margin: 0.55rem 0 0;
  padding: 0.58rem 0.72rem;
  border: 1px solid var(--org2-rule);
  border-radius: 10px;
  background: var(--org2-elevated-surface);
  box-shadow: 0 1px 2px var(--org2-shadow);
}
.org2-file-properties .org2-keyword { margin: 0.16rem 0; font-size: inherit; }
.org2-headline { margin: 0; }
.org2-headline + .org2-headline { margin-top: 0.72rem; }
.org2-headline-summary {
  margin: 0.9rem 0 0.36rem;
  padding-left: 1.15rem;
  color: var(--org2-text);
  cursor: pointer;
  list-style: none;
  position: relative;
}
.org2-headline-summary::-webkit-details-marker,
.org2-properties-drawer > summary::-webkit-details-marker,
.org2-large-source > summary::-webkit-details-marker,
.org2-drawer > summary::-webkit-details-marker { display: none; }
.org2-headline-summary::before,
.org2-properties-drawer > summary::before,
.org2-large-source > summary::before,
.org2-drawer > summary::before {
  content: "\u25B6";
  position: absolute;
  left: 0.05rem;
  top: 0.22em;
  color: var(--org2-muted);
  font-size: 0.82rem;
  font-weight: 750;
  line-height: 1.2;
}
details[open] > summary::before { content: "\u25BC"; }
.org2-headline-summary:hover::before,
.org2-properties-drawer > summary:hover::before,
.org2-drawer > summary:hover::before { color: var(--org2-accent); }
.org2-headline-summary > h1,
.org2-headline-summary > h2,
.org2-headline-summary > h3,
.org2-headline-summary > h4,
.org2-headline-summary > h5,
.org2-headline-summary > h6 { display: inline; margin: 0; }
.org2-headline-summary > h1::before,
.org2-headline-summary > h2::before,
.org2-headline-summary > h3::before,
.org2-headline-summary > h4::before,
.org2-headline-summary > h5::before,
.org2-headline-summary > h6::before {
  display: inline-block;
  margin-right: 0.44rem;
  color: var(--org2-accent);
  content: "*";
  font-family: var(--org2-font-mono);
  font-size: 0.64em;
  font-weight: 650;
  vertical-align: 0.08em;
}
.org2-headline-summary > h2::before {
  color: var(--org2-signal);
  content: "**";
  font-size: 0.52em;
  letter-spacing: -0.16em;
}
.org2-headline-summary > h3::before,
.org2-headline-summary > h4::before,
.org2-headline-summary > h5::before,
.org2-headline-summary > h6::before {
  color: var(--org2-muted);
  content: "***";
  font-size: 0.46em;
  letter-spacing: -0.18em;
}
.org2-heading-ai-action {
  display: inline-flex;
  align-items: center;
  margin-left: 0.45rem;
  padding: 0.12rem 0.34rem;
  border: 0;
  border-radius: 4px;
  background: transparent;
  color: var(--org2-muted);
  font: inherit;
  font-size: 0.76rem;
  font-weight: 600;
  line-height: 1.25;
  letter-spacing: 0;
  vertical-align: middle;
  transform: translateY(-2px);
  opacity: 0.46;
  cursor: pointer;
  transition: opacity 110ms ease-out, color 110ms ease-out, background-color 110ms ease-out;
}
.org2-headline-summary:hover > .org2-heading-ai-action,
.org2-section-label:hover > .org2-heading-ai-action,
.org2-heading-ai-action:focus-visible {
  opacity: 1;
}
.org2-heading-ai-action:hover,
.org2-heading-ai-action:focus-visible {
  color: var(--org2-accent);
  background: color-mix(in srgb, var(--org2-accent) 9%, transparent);
  outline: none;
}
.org2-headline.level-1 > .org2-headline-summary { margin-top: 0.08rem; }
.org2-headline-body { min-width: 0; }
.org2-headline-body > .org2-headline.level-2 { margin-left: 0.35rem; padding-left: 0.5rem; border-left: 1px solid var(--org2-rule); }
.org2-headline-body > .org2-headline.level-3 { margin-left: 0.25rem; padding-left: 0.45rem; border-left: 1px solid var(--org2-rule); }
.org2-headline-body > .org2-headline.level-4,
.org2-headline-body > .org2-headline.level-5,
.org2-headline-body > .org2-headline.level-6 { margin-left: 0.2rem; padding-left: 0.35rem; border-left: 1px solid var(--org2-rule); }
h1, h2, h3, h4, h5, h6 {
  color: var(--org2-text);
  font-weight: 620;
  line-height: 1.3;
  letter-spacing: 0;
  margin: 1rem 0 0.42rem;
  overflow-wrap: normal;
  word-break: normal;
  text-wrap: wrap;
}
h1 { font-size: 1.16rem; margin-top: 0.1rem; }
h2 { font-size: 1.08rem; color: color-mix(in srgb, var(--org2-text) 78%, var(--org2-accent)); }
h3 { font-size: 1.02rem; color: color-mix(in srgb, var(--org2-text) 72%, var(--org2-muted)); }
h4, h5, h6 { font-size: 0.98rem; color: var(--org2-muted); }
p { max-width: 100%; margin: 0.55rem 0 0.8rem; overflow-wrap: anywhere; }
a { color: var(--org2-link); text-decoration: none; border-bottom: 1px solid color-mix(in srgb, var(--org2-link) 35%, transparent); }
a:hover { border-bottom-color: var(--org2-link); }
strong { font-weight: 650; }
code {
  font-family: var(--org2-font-mono);
  font-size: 0.9em;
  background: var(--org2-code);
  border: 1px solid var(--org2-rule);
  border-radius: 4px;
  padding: 0.08em 0.3em;
}
pre, .org2-src, .org2-example, .org2-verse, .org2-export, .org2-directive {
  box-sizing: border-box;
  max-width: 100%;
  overflow: auto;
  margin: 0.85rem 0 1rem;
  padding: 0.9rem 1rem !important;
  color: var(--org2-text);
  background: var(--org2-code) !important;
  border: 1px solid var(--org2-rule) !important;
  border-radius: 10px !important;
  font-family: var(--org2-font-mono);
  font-size: 0.88rem !important;
  line-height: 1.45 !important;
  white-space: pre;
}
pre code { padding: 0; border: 0; background: transparent; font-size: inherit; }
.org2-large-source { margin: 0.85rem 0 1rem; }
.org2-large-source > summary { position: relative; padding-left: 1.15rem; list-style: none; cursor: pointer; color: var(--org2-muted); font-size: 0.88rem; }
.org2-large-source > pre { margin-top: 0.5rem; }
blockquote { margin: 0.9rem 0; padding: 0.15rem 0 0.15rem 1rem; border-left: 3px solid var(--org2-accent); color: var(--org2-muted); }
.org2-quote { white-space: pre-wrap; overflow-wrap: anywhere; }
ul, ol { margin: 0.55rem 0 0.9rem; padding-left: 1.55rem; }
li { min-width: 0; margin: 0.24rem 0; padding-left: 0.12rem; overflow-wrap: anywhere; }
li > p { display: inline; }
input[type="checkbox"] { width: 0.95rem; height: 0.95rem; margin: 0 0.42rem 0 -0.05rem; accent-color: var(--org2-accent); vertical-align: -0.11rem; }
.org2-checkbox-mixed { display: inline-grid; width: 0.95rem; height: 0.95rem; margin: 0 0.42rem 0 -0.05rem; place-items: center; border: 1px solid var(--org2-muted); border-radius: 3px; color: var(--org2-muted); font-size: 0.8rem; line-height: 1; vertical-align: -0.11rem; }
.org2-priority { color: var(--org2-danger); font-family: var(--org2-font-mono); font-size: 0.72em; font-weight: 750; }
.org2-comment-keyword { color: var(--org2-muted); font-family: var(--org2-font-mono); font-size: 0.72em; font-weight: 700; }
.org2-headline.commented { opacity: 0.7; }
.org2-progress-cookie, .org2-latex-fragment, .org2-citation, .org2-export-snippet { font-family: var(--org2-font-mono); }
.org2-latex-fragment { color: var(--org2-accent); }
.org2-footnote-definition { margin: 0.7rem 0; padding-top: 0.45rem; border-top: 1px solid var(--org2-rule); color: var(--org2-muted); font-size: 0.86rem; }
.org2-description-list { display: grid; grid-template-columns: max-content 1fr; gap: 0.32rem 0.8rem; }
.org2-description-list dt { font-weight: 700; }
.org2-description-list dd { margin: 0; }
.org2-image-figure {
  width: fit-content;
  max-width: 100%;
  margin: 0.8rem 0 1.1rem;
}
.org2-image-link { display: block; max-width: 100%; border: 0; }
.org2-image-link:hover { border: 0; }
.org2-image {
  display: block;
  width: auto;
  max-width: 100%;
  height: auto;
  max-height: 72vh;
  border: 1px solid color-mix(in srgb, var(--org2-text) 10%, transparent);
  border-radius: 10px;
  background: color-mix(in srgb, var(--org2-elevated-surface) 96%, var(--org2-faint));
  box-shadow: 0 1px 3px color-mix(in srgb, var(--org2-text) 8%, transparent);
  object-fit: contain;
}
li > .org2-image-figure { margin-top: 0.65rem; }
.org2-todo {
  display: inline-block;
  margin-right: 0.35rem;
  padding: 0.08rem 0.34rem;
  color: var(--org2-accent);
  background: color-mix(in srgb, var(--org2-accent) 10%, transparent);
  border-radius: 4px;
  font-size: 0.67em;
  font-weight: 750;
  line-height: 1.35;
  vertical-align: 0.12em;
}
.org2-todo.todo-done { color: var(--org2-success); background: color-mix(in srgb, var(--org2-success) 11%, transparent); }
.org2-todo.todo-canceled, .org2-todo.todo-cancelled { color: var(--org2-danger); background: color-mix(in srgb, var(--org2-danger) 10%, transparent); }
.org2-tags { display: inline-flex; flex-wrap: wrap; gap: 0.28rem; margin-left: 0.35rem; vertical-align: 0.1em; }
.org2-tag { color: var(--org2-muted); background: var(--org2-faint); border-radius: 4px; padding: 0.08rem 0.34rem; font-size: 0.65em; font-weight: 550; }
.org2-planning { color: var(--org2-muted); font-family: var(--org2-font-mono); font-size: 0.82rem; font-variant-numeric: tabular-nums; }
.org2-planning-kind { color: var(--org2-warning); font-size: 0.78em; font-weight: 700; }
.org2-timestamp, .org2-timestamp-range { font-variant-numeric: tabular-nums; }
.org2-properties-drawer {
  margin: 0.75rem 0 1rem;
  padding: 0.58rem 0.72rem;
  border: 1px solid var(--org2-rule);
  border-radius: 10px;
  background: var(--org2-elevated-surface);
  box-shadow: 0 1px 2px var(--org2-shadow);
  font-size: 0.82rem;
}
.org2-properties-drawer > summary {
  position: relative;
  padding-left: 1.15rem;
  color: var(--org2-muted);
  cursor: pointer;
  font-weight: 650;
  list-style: none;
  user-select: none;
}
.org2-properties-drawer[open] > summary { margin-bottom: 0.52rem; }
.org2-properties {
  display: grid;
  grid-template-columns: minmax(6rem, max-content) 1fr;
  gap: 0.3rem 0.9rem;
  margin: 0;
  padding: 0.5rem 0 0;
  border-top: 1px solid var(--org2-rule);
}
.org2-properties dt { color: var(--org2-muted); font-family: var(--org2-font-mono); font-weight: 650; }
.org2-properties dd { margin: 0; min-width: 0; overflow-wrap: anywhere; font-family: var(--org2-font-mono); }
.org2-drawer { margin: 0.75rem 0; color: var(--org2-muted); font-size: 0.9rem; }
.org2-drawer summary { position: relative; padding-left: 1.15rem; cursor: pointer; font-weight: 600; list-style: none; }
.org2-keyword { color: var(--org2-muted); font-size: 0.88rem; }
.org2-keyword-name { font-weight: 650; }
table { width: 100%; margin: 0.8rem 0 1.15rem; border-collapse: collapse; font-size: 0.9rem; font-variant-numeric: tabular-nums; }
th, td { padding: 0.46rem 0.58rem; border-bottom: 1px solid var(--org2-rule); text-align: left; vertical-align: top; }
th { color: var(--org2-muted); background: var(--org2-faint); font-family: var(--org2-font-mono); font-size: 0.78rem; font-weight: 650; }
.org2-table-scroll { display: block; width: 100%; max-width: 100%; min-width: 0; margin: 0.8rem 0 1.15rem; overflow-x: auto; overscroll-behavior-inline: contain; -webkit-overflow-scrolling: touch; }
.org2-table-scroll table { width: auto; min-width: 100%; margin: 0; }
.org2-table-scroll th, .org2-table-scroll td { overflow-wrap: normal; word-break: normal; hyphens: none; }
.org2-table-controls {
  position: sticky;
  left: 0;
  display: flex;
  align-items: center;
  gap: 0.48rem;
  width: max-content;
  min-width: min(100%, 420px);
  margin: 0 0 0.42rem;
  color: var(--org2-muted);
  font-size: 0.76rem;
}
.org2-table-filter {
  box-sizing: border-box;
  width: clamp(150px, 34vw, 260px);
  min-height: 28px;
  padding: 0.25rem 0.55rem;
  border: 1px solid color-mix(in srgb, var(--org2-text) 16%, transparent);
  border-radius: 8px;
  color: var(--org2-text);
  background: color-mix(in srgb, var(--org2-surface) 96%, var(--org2-faint));
  font: inherit;
  outline: none;
}
.org2-table-filter:focus { border-color: var(--org2-accent); box-shadow: 0 0 0 2px color-mix(in srgb, var(--org2-accent) 16%, transparent); }
.org2-table-row-count { min-width: 64px; font-variant-numeric: tabular-nums; white-space: nowrap; }
.org2-table-control-button,
.org2-table-sort-button {
  border: 0;
  border-radius: 7px;
  color: var(--org2-muted);
  background: transparent;
  font: inherit;
  cursor: pointer;
}
.org2-table-control-button { min-height: 26px; padding: 0.22rem 0.45rem; white-space: nowrap; }
.org2-table-control-button:hover,
.org2-table-sort-button:hover { color: var(--org2-text); background: var(--org2-faint); }
.org2-table-control-button:disabled { cursor: default; opacity: 0.44; }
.org2-table-save-button { color: var(--org2-accent); font-weight: 650; }
.org2-table-formula-button { color: var(--org2-accent); font-weight: 650; }
.org2-table-formula-status { display: block; margin: -0.75rem 0 1rem; color: var(--org2-muted); font-size: 0.78rem; }
.org2-table-formula-error { color: var(--org2-danger, #b42318); }
.org2-table-sort-button { margin-left: 0.28rem; padding: 0.06rem 0.22rem; font-size: 0.72rem; }
.org2-table-sort-button[aria-pressed="true"] { color: var(--org2-accent); background: color-mix(in srgb, var(--org2-accent) 10%, transparent); }
.org2-resizable-table th, .org2-resizable-table td { min-width: 72px; }
.org2-table-resize-anchor { position: relative; }
.org2-chart {
  box-sizing: border-box;
  position: relative;
  width: min(100%, 800px);
  max-width: 100%;
  min-width: min(360px, 100%);
  margin: 0.85rem 0 1.25rem;
  padding: 0.65rem 0.75rem 0.5rem;
  border: 1px solid color-mix(in srgb, var(--org2-text) 11%, transparent);
  border-radius: 12px;
  background: color-mix(in srgb, var(--org2-chart-surface) 96%, var(--org2-faint));
  box-shadow: 0 1px 2px color-mix(in srgb, var(--org2-text) 7%, transparent);
  overflow: hidden;
  resize: horizontal;
}
.org2-chart-compact { width: min(100%, 680px); }
.org2-chart-wide { width: 100%; }
.org2-chart svg { display: block; width: 100%; height: auto; margin: 0; overflow: visible; }
.org2-chart-line { opacity: 0.9; }
.org2-chart-mark { cursor: crosshair; outline: none; transition: opacity 100ms ease-out, stroke-width 100ms ease-out; }
.org2-chart-mark:focus-visible { stroke: var(--org2-text); stroke-width: 3; }
.org2-chart-mark-active { opacity: 1; stroke: var(--org2-text); stroke-width: 3; }
.org2-chart-crosshair { opacity: 0.42; }
.org2-chart-tooltip {
  position: absolute;
  z-index: 3;
  min-width: 108px;
  max-width: min(220px, calc(100% - 20px));
  padding: 0.42rem 0.55rem;
  border: 1px solid color-mix(in srgb, var(--org2-text) 14%, transparent);
  border-radius: 8px;
  color: var(--org2-text);
  background: color-mix(in srgb, var(--org2-chart-surface) 96%, var(--org2-faint));
  box-shadow: 0 7px 22px color-mix(in srgb, var(--org2-text) 16%, transparent);
  font-size: 0.78rem;
  line-height: 1.32;
  pointer-events: none;
}
.org2-chart-tooltip[hidden] { display: none; }
.org2-chart-tooltip-label { display: block; color: var(--org2-muted); }
.org2-chart-tooltip-value { display: block; margin-top: 0.08rem; font-weight: 650; font-variant-numeric: tabular-nums; }
.org2-plugin-render { width: 100%; margin: 0.85rem 0 1.25rem; overflow: hidden; }
.org2-plugin-render iframe { color-scheme: light dark; }
.org2-plugin-render-error {
  display: grid;
  gap: 0.25rem;
  margin: 0.75rem 0 1rem;
  padding: 0.75rem 0.9rem;
  border: 1px solid color-mix(in srgb, var(--org2-danger, #b42318) 40%, transparent);
  border-radius: 10px;
  color: var(--org2-danger, #b42318);
  background: color-mix(in srgb, var(--org2-danger, #b42318) 7%, transparent);
  font-size: 0.86rem;
}
.org2-column-resizer {
  position: absolute;
  top: 0;
  right: -5px;
  bottom: 0;
  width: 10px;
  z-index: 2;
  cursor: col-resize;
  touch-action: none;
}
.org2-column-resizer::after {
  content: "";
  position: absolute;
  top: 18%;
  right: 4px;
  bottom: 18%;
  width: 2px;
  border-radius: 1px;
  background: transparent;
}
.org2-column-resizer:hover::after,
body.org2-resizing-column .org2-column-resizer::after { background: var(--org2-accent); }
body.org2-resizing-column { cursor: col-resize; user-select: none; }
::selection { background: color-mix(in srgb, var(--org2-accent) 30%, transparent); }
@media (max-width: 560px) {
  main.org2-document { padding-top: 18px; padding-bottom: 48px; }
  .org2-properties { grid-template-columns: 1fr; gap: 0.08rem; }
  .org2-properties dd { margin-bottom: 0.35rem; }
}`;
  var APP_DOCUMENT_SCRIPT = `(() => {
  const minimumColumnWidth = 72;

  function appendAIAction(container, line) {
    if (!container || !line || container.querySelector(":scope > .org2-heading-ai-action")) return;
    const button = document.createElement("button");
    button.type = "button";
    button.className = "org2-heading-ai-action";
    button.textContent = "\u2726 Ask AI";
    button.title = "Ask AI about this section";
    button.setAttribute("aria-label", "Ask AI about this section");
    button.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      window.location.href = "org2-workspace://ask-ai?line=" + encodeURIComponent(line);
    });
    container.appendChild(button);
  }

  function installHeadingActions() {
    document.querySelectorAll("details.org2-headline[data-org2-start-line]").forEach((headline) => {
      const summary = headline.querySelector(":scope > .org2-headline-summary");
      const line = headline.dataset.org2StartLine;
      appendAIAction(summary, line);
    });

    document.querySelectorAll("p[data-org2-start-line]").forEach((paragraph) => {
      const meaningfulChildren = Array.from(paragraph.childNodes).filter((node) =>
        node.nodeType !== Node.TEXT_NODE || Boolean(node.textContent && node.textContent.trim())
      );
      if (meaningfulChildren.length !== 1) return;
      const label = meaningfulChildren[0];
      if (!(label instanceof HTMLElement) || label.tagName !== "STRONG") return;
      paragraph.classList.add("org2-section-label");
      appendAIAction(paragraph, paragraph.dataset.org2StartLine);
    });
  }

  function installTableResizers() {
    document.querySelectorAll(".org2-table-scroll table").forEach((table) => {
      if (table.dataset.org2Resizable === "true") return;
      const row = table.tHead && table.tHead.rows.length > 0 ? table.tHead.rows[0] : table.rows[0];
      if (!row || row.cells.length === 0) return;

      const measuredTableWidth = Math.ceil(table.getBoundingClientRect().width);
      const measuredWidths = Array.from(row.cells, (cell) =>
        Math.max(minimumColumnWidth, Math.ceil(cell.getBoundingClientRect().width))
      );
      const colgroup = document.createElement("colgroup");
      const columns = measuredWidths.map((width) => {
        const column = document.createElement("col");
        column.style.width = width + "px";
        colgroup.appendChild(column);
        return column;
      });
      table.insertBefore(colgroup, table.firstChild);
      table.dataset.org2Resizable = "true";
      table.classList.add("org2-resizable-table");
      table.style.tableLayout = "fixed";
      table.style.width = Math.max(
        measuredTableWidth,
        measuredWidths.reduce((total, width) => total + width, 0)
      ) + "px";

      Array.from(row.cells).forEach((cell, index) => {
        cell.classList.add("org2-table-resize-anchor");
        const handle = document.createElement("span");
        handle.className = "org2-column-resizer";
        handle.setAttribute("role", "separator");
        handle.setAttribute("aria-orientation", "vertical");
        handle.setAttribute("aria-label", "Resize table column " + (index + 1));
        handle.title = "Drag to resize column";

        handle.addEventListener("mousedown", (event) => {
          if (event.button !== 0) return;
          event.preventDefault();
          event.stopPropagation();

          const startX = event.clientX;
          const startWidth = parseFloat(columns[index].style.width) || measuredWidths[index];
          const startTableWidth = table.getBoundingClientRect().width;
          document.body.classList.add("org2-resizing-column");

          const move = (moveEvent) => {
            const nextWidth = Math.max(minimumColumnWidth, startWidth + moveEvent.clientX - startX);
            columns[index].style.width = nextWidth + "px";
            table.style.width = Math.max(
              table.parentElement ? table.parentElement.clientWidth : 0,
              startTableWidth + nextWidth - startWidth
            ) + "px";
          };
          const finish = () => {
            document.body.classList.remove("org2-resizing-column");
            window.removeEventListener("mousemove", move);
            window.removeEventListener("mouseup", finish);
          };

          window.addEventListener("mousemove", move);
          window.addEventListener("mouseup", finish);
        });

        cell.appendChild(handle);
      });
    });
  }

  function installInteractiveTables() {
    const initialVisibleRowLimit = 100;
    const collator = new Intl.Collator(undefined, { numeric: true, sensitivity: "base" });
    document.querySelectorAll(".org2-table-scroll table").forEach((table) => {
      if (table.dataset.org2Interactive === "true") return;
      const body = table.tBodies && table.tBodies[0];
      const originalRows = body ? Array.from(body.rows) : [];
      if (!body || originalRows.length === 0) return;

      table.dataset.org2Interactive = "true";
      const rowRecords = originalRows.map((row, index) => ({
        originalIndex: index,
        searchText: row.innerText.toLocaleLowerCase(),
        cellTexts: Array.from(row.cells, (cell) => cell.innerText.trim()),
        html: row.innerHTML,
        attributes: Array.from(row.attributes, (attribute) => [attribute.name, attribute.value])
      }));
      body.replaceChildren();

      const wrapper = table.closest(".org2-table-scroll");
      if (!wrapper) return;
      const controls = document.createElement("div");
      controls.className = "org2-table-controls";
      controls.setAttribute("role", "group");
      controls.setAttribute("aria-label", "Table view controls");

      const filter = document.createElement("input");
      filter.type = "search";
      filter.className = "org2-table-filter";
      filter.placeholder = "Filter rows";
      filter.setAttribute("aria-label", "Filter table rows");

      const count = document.createElement("span");
      count.className = "org2-table-row-count";
      count.setAttribute("aria-live", "polite");

      const reset = document.createElement("button");
      reset.type = "button";
      reset.className = "org2-table-control-button";
      reset.textContent = "Reset";
      reset.title = "Clear filtering and restore source order";

      const showMore = document.createElement("button");
      showMore.type = "button";
      showMore.className = "org2-table-control-button";
      showMore.dataset.org2TableShowMore = "true";

      const save = document.createElement("button");
      save.type = "button";
      save.className = "org2-table-control-button org2-table-save-button";
      save.textContent = "Save view to source";
      save.title = "Replace source table rows with this filtered and sorted view";
      save.hidden = !window.__org2TablePersistenceEnabled;
      save.dataset.org2TableSave = "true";

      const recalculate = document.createElement("button");
      recalculate.type = "button";
      recalculate.className = "org2-table-control-button org2-table-formula-button";
      recalculate.textContent = "Recalculate";
      recalculate.title = "Recalculate this table from its #+TBLFM formulas and save the results";
      recalculate.hidden = !window.__org2TablePersistenceEnabled || Number(table.dataset.org2FormulaCount || 0) === 0;
      recalculate.dataset.org2TableFormula = "true";

      controls.append(filter, count, showMore, reset, recalculate, save);
      wrapper.insertBefore(controls, table);

      let sortColumn = null;
      let sortDirection = null;
      let visibleRowLimit = initialVisibleRowLimit;
      let currentRows = [...rowRecords];
      const headerRow = table.tHead && table.tHead.rows.length > 0
        ? table.tHead.rows[table.tHead.rows.length - 1]
        : null;
      const sortButtons = [];

      function renderedRow(record) {
        const row = document.createElement("tr");
        record.attributes.forEach(([name, value]) => row.setAttribute(name, value));
        row.dataset.org2OriginalRowIndex = String(record.originalIndex);
        row.innerHTML = record.html;
        return row;
      }

      function cellText(record, column) {
        return record.cellTexts[column] || "";
      }

      function refresh(resetVisibleLimit = false) {
        if (resetVisibleLimit) visibleRowLimit = initialVisibleRowLimit;
        const query = filter.value.trim().toLocaleLowerCase();
        let rows = [...rowRecords];
        if (sortColumn !== null && sortDirection) {
          rows.sort((lhs, rhs) => {
            const comparison = collator.compare(cellText(lhs, sortColumn), cellText(rhs, sortColumn));
            if (comparison !== 0) return sortDirection === "ascending" ? comparison : -comparison;
            return lhs.originalIndex - rhs.originalIndex;
          });
        } else {
          rows.sort((lhs, rhs) => lhs.originalIndex - rhs.originalIndex);
        }
        currentRows = rows.filter((row) => !query || row.searchText.includes(query));
        const renderedCount = Math.min(currentRows.length, visibleRowLimit);
        body.replaceChildren(...currentRows.slice(0, renderedCount).map(renderedRow));

        count.textContent = currentRows.length === rowRecords.length
          ? renderedCount + " of " + rowRecords.length + " rows shown"
          : renderedCount + " of " + currentRows.length + " matching \xB7 " + rowRecords.length + " total";
        const remainingCount = Math.max(0, currentRows.length - renderedCount);
        showMore.hidden = remainingCount === 0;
        showMore.textContent = remainingCount > 0
          ? "Show " + Math.min(initialVisibleRowLimit, remainingCount) + " more"
          : "";
        reset.disabled = !query && sortColumn === null;
        save.disabled = currentRows.length === 0;
      }

      if (headerRow) {
        Array.from(headerRow.cells).forEach((cell, column) => {
          const button = document.createElement("button");
          button.type = "button";
          button.className = "org2-table-sort-button";
          button.textContent = "\u2195";
          button.title = "Sort by " + (cell.innerText.trim() || "column " + (column + 1));
          button.setAttribute("aria-label", button.title);
          button.setAttribute("aria-pressed", "false");
          button.addEventListener("click", (event) => {
            event.preventDefault();
            event.stopPropagation();
            sortDirection = sortColumn === column && sortDirection === "ascending"
              ? "descending"
              : "ascending";
            sortColumn = column;
            sortButtons.forEach((candidate, candidateColumn) => {
              const active = candidateColumn === sortColumn;
              candidate.textContent = active ? (sortDirection === "ascending" ? "\u2191" : "\u2193") : "\u2195";
              candidate.setAttribute("aria-pressed", active ? "true" : "false");
              candidate.parentElement?.setAttribute("aria-sort", active ? sortDirection : "none");
            });
            refresh(true);
          });
          sortButtons.push(button);
          cell.appendChild(button);
        });
      }

      filter.addEventListener("input", () => refresh(true));
      showMore.addEventListener("click", () => {
        visibleRowLimit += initialVisibleRowLimit;
        refresh();
      });
      reset.addEventListener("click", () => {
        filter.value = "";
        sortColumn = null;
        sortDirection = null;
        sortButtons.forEach((button) => {
          button.textContent = "\u2195";
          button.setAttribute("aria-pressed", "false");
          button.parentElement?.setAttribute("aria-sort", "none");
        });
        refresh(true);
        filter.focus();
      });
      save.addEventListener("click", () => {
        if (currentRows.length === 0) return;
        const handler = window.webkit?.messageHandlers?.org2TableView;
        if (!handler) return;
        handler.postMessage({
          startLine: Number(table.dataset.org2StartLine || 0),
          endLine: Number(table.dataset.org2EndLine || 0),
          visibleBodyRowIndices: currentRows.map((row) => row.originalIndex),
          totalBodyRowCount: rowRecords.length,
          filterActive: Boolean(filter.value.trim()),
          sortActive: sortColumn !== null
        });
      });
      recalculate.addEventListener("click", () => {
        const handler = window.webkit?.messageHandlers?.org2TableFormula;
        if (!handler) return;
        handler.postMessage({ startLine: Number(table.dataset.org2StartLine || 0) });
      });

      refresh();
    });

    window.__org2SetTablePersistenceEnabled = (enabled) => {
      window.__org2TablePersistenceEnabled = Boolean(enabled);
      document.querySelectorAll("[data-org2-table-save='true']").forEach((button) => {
        button.hidden = !window.__org2TablePersistenceEnabled;
      });
      document.querySelectorAll("[data-org2-table-formula='true']").forEach((button) => {
        const table = button.closest(".org2-table-scroll")?.querySelector("table");
        button.hidden = !window.__org2TablePersistenceEnabled || Number(table?.dataset.org2FormulaCount || 0) === 0;
      });
    };
  }

  function installInteractiveCharts() {
    document.querySelectorAll("figure.org2-chart[data-org2-chart-interactive='true']").forEach((figure, chartIndex) => {
      if (figure.dataset.org2ChartEnhanced === "true") return;
      const svg = figure.querySelector("svg.org2-chart-svg");
      if (!svg || svg.dataset.org2ChartInteractive !== "true") return;
      const marks = Array.from(svg.querySelectorAll("[data-org2-chart-mark='true']"));
      if (marks.length === 0) return;

      figure.dataset.org2ChartEnhanced = "true";
      const tooltip = document.createElement("div");
      const tooltipID = "org2-chart-tooltip-" + chartIndex;
      tooltip.id = tooltipID;
      tooltip.className = "org2-chart-tooltip";
      tooltip.setAttribute("role", "tooltip");
      tooltip.hidden = true;
      const tooltipLabel = document.createElement("span");
      tooltipLabel.className = "org2-chart-tooltip-label";
      const tooltipValue = document.createElement("span");
      tooltipValue.className = "org2-chart-tooltip-value";
      tooltip.append(tooltipLabel, tooltipValue);
      figure.appendChild(tooltip);

      const crosshair = svg.querySelector(".org2-chart-crosshair");
      let activeMark = null;

      marks.forEach((mark, index) => {
        const nativeTitle = mark.querySelector(":scope > title");
        if (nativeTitle) nativeTitle.remove();
        mark.setAttribute("aria-describedby", tooltipID);
        mark.addEventListener("focus", () => showMark(mark));
        mark.addEventListener("blur", hideMark);
        mark.addEventListener("keydown", (event) => {
          if (event.key !== "ArrowLeft" && event.key !== "ArrowRight") return;
          event.preventDefault();
          const direction = event.key === "ArrowRight" ? 1 : -1;
          const next = marks[Math.max(0, Math.min(marks.length - 1, index + direction))];
          if (next && typeof next.focus === "function") next.focus();
        });
      });

      function showMark(mark) {
        if (!mark) return;
        if (activeMark && activeMark !== mark) activeMark.classList.remove("org2-chart-mark-active");
        activeMark = mark;
        mark.classList.add("org2-chart-mark-active");
        tooltipLabel.textContent = mark.dataset.label || "";
        const yLabel = svg.dataset.org2ChartYLabel || "value";
        const seriesLabel = mark.dataset.series || yLabel;
        tooltipValue.textContent = seriesLabel + ": " + (mark.dataset.value || "");
        tooltip.hidden = false;

        if (crosshair) {
          const x = mark.dataset.chartX || "0";
          crosshair.setAttribute("x1", x);
          crosshair.setAttribute("x2", x);
          crosshair.setAttribute("visibility", "visible");
        }

        const figureRect = figure.getBoundingClientRect();
        const markRect = mark.getBoundingClientRect();
        const centerX = markRect.left - figureRect.left + markRect.width / 2;
        let left = centerX - tooltip.offsetWidth / 2;
        left = Math.max(10, Math.min(left, figure.clientWidth - tooltip.offsetWidth - 10));
        let top = markRect.top - figureRect.top - tooltip.offsetHeight - 10;
        if (top < 8) top = markRect.bottom - figureRect.top + 10;
        tooltip.style.left = left + "px";
        tooltip.style.top = top + "px";
      }

      function hideMark() {
        if (activeMark) activeMark.classList.remove("org2-chart-mark-active");
        activeMark = null;
        tooltip.hidden = true;
        if (crosshair) crosshair.setAttribute("visibility", "hidden");
      }

      figure.addEventListener("mousemove", (event) => {
        let nearest = null;
        let nearestDistance = Infinity;
        marks.forEach((mark) => {
          const rect = mark.getBoundingClientRect();
          const distance = Math.abs(event.clientX - (rect.left + rect.width / 2));
          if (distance < nearestDistance) {
            nearest = mark;
            nearestDistance = distance;
          }
        });
        showMark(nearest);
      });
      figure.addEventListener("mouseleave", () => {
        if (!marks.includes(document.activeElement)) hideMark();
      });
    });
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", () => {
      installHeadingActions();
      installTableResizers();
      installInteractiveTables();
      installInteractiveCharts();
    }, { once: true });
  } else {
    installHeadingActions();
    installTableResizers();
    installInteractiveTables();
    installInteractiveCharts();
  }
})();`;
  function renderHeadStyleSection(opts) {
    const includeDefaultStyle = opts.includeDefaultStyle !== false;
    const stylesheets = normalizeStylesheets(opts.stylesheets);
    const stylesheetLinks = stylesheets.map((href) => `<link rel="stylesheet" href="${escapeAttr(href)}" />`).join("\n");
    const defaultStyleBlock = includeDefaultStyle ? `<style>
${opts.defaultStyle}
</style>` : "";
    if (stylesheetLinks && defaultStyleBlock) {
      return `${stylesheetLinks}
${defaultStyleBlock}
`;
    }
    if (stylesheetLinks) {
      return `${stylesheetLinks}
`;
    }
    if (defaultStyleBlock) {
      return `${defaultStyleBlock}
`;
    }
    return "";
  }
  var HIDDEN_DOCUMENT_KEYWORDS = /* @__PURE__ */ new Set([
    "TITLE",
    "AUTHOR",
    "DATE",
    "SUBTITLE",
    "DESCRIPTION",
    "KEYWORDS",
    "LANGUAGE",
    "HTML_HEAD",
    "HTML_HEAD_EXTRA",
    "OPTIONS",
    "LINK"
  ]);
  function parseKeywordList(value) {
    const values = String(value || "").split(/[;,]/).map((entry) => entry.trim()).filter((entry) => entry.length > 0);
    return Array.from(new Set(values));
  }
  function normalizeDocumentLanguage(value) {
    const normalized = String(value || "").trim();
    if (!normalized) return null;
    if (!/^[A-Za-z0-9-]+$/.test(normalized)) return null;
    return normalized;
  }
  function parseKeywordOptionsMap(value) {
    const assignments = /* @__PURE__ */ new Map();
    const tokens = String(value || "").trim().split(/\s+/).filter((token) => token.length > 0);
    for (const token of tokens) {
      const separatorIndex = token.indexOf(":");
      if (separatorIndex <= 0) continue;
      const key = token.slice(0, separatorIndex).trim().toLowerCase();
      const optionValue = token.slice(separatorIndex + 1).trim();
      if (!key || !optionValue) continue;
      assignments.set(key, optionValue);
    }
    return assignments;
  }
  function parseKeywordBooleanOption(value) {
    const normalized = String(value || "").trim().toLowerCase();
    if (!normalized) return null;
    if (["t", "true", "yes", "on"].includes(normalized)) return true;
    if (["nil", "false", "no", "off"].includes(normalized)) return false;
    if (/^\d+$/.test(normalized)) {
      return Number(normalized) > 0;
    }
    return null;
  }
  function parseKeywordPositiveIntegerOption(value) {
    const normalized = String(value || "").trim();
    if (!/^\d+$/.test(normalized)) return null;
    const parsed = Number.parseInt(normalized, 10);
    if (!Number.isFinite(parsed) || parsed < 1) return null;
    return parsed;
  }
  function collectKeywordOptions(doc) {
    const options = {};
    for (const node of doc.children) {
      if (node.type !== "KeywordLine") continue;
      const key = String(node.keyRaw || "").trim().toUpperCase();
      if (key !== "OPTIONS") continue;
      const assignments = parseKeywordOptionsMap(node.valueRaw);
      if (assignments.has("toc")) {
        const tocRaw = assignments.get("toc");
        const parsed = parseKeywordBooleanOption(tocRaw);
        if (parsed !== null) {
          options.toc = parsed;
        }
        const parsedDepth = parseKeywordPositiveIntegerOption(tocRaw);
        if (parsedDepth !== null) {
          options.toc = true;
          options.tocDepth = parsedDepth;
        }
      }
      if (assignments.has("num")) {
        const numRaw = assignments.get("num");
        const parsed = parseKeywordBooleanOption(numRaw);
        if (parsed !== null) {
          options.num = parsed;
        }
        const parsedDepth = parseKeywordPositiveIntegerOption(numRaw);
        if (parsedDepth !== null) {
          options.num = true;
          options.numDepth = parsedDepth;
        }
      }
    }
    return options;
  }
  function collectKeywordMetadata(doc) {
    const metadata = {};
    for (const node of doc.children) {
      if (node.type !== "KeywordLine") continue;
      const key = String(node.keyRaw || "").trim().toUpperCase();
      const value = String(node.valueRaw || "").trim();
      if (!value) continue;
      if (key === "AUTHOR" && !metadata.author) {
        metadata.author = value;
        continue;
      }
      if (key === "DATE" && !metadata.date) {
        metadata.date = value;
        continue;
      }
      if (key === "SUBTITLE" && !metadata.subtitle) {
        metadata.subtitle = value;
        continue;
      }
      if (key === "DESCRIPTION" && !metadata.description) {
        metadata.description = value;
        continue;
      }
      if (key === "KEYWORDS" && !metadata.keywords) {
        const parsedKeywords = parseKeywordList(value);
        if (parsedKeywords.length > 0) {
          metadata.keywords = parsedKeywords;
        }
        continue;
      }
      if (key === "LANGUAGE" && !metadata.language) {
        const normalizedLanguage = normalizeDocumentLanguage(value);
        if (normalizedLanguage) {
          metadata.language = normalizedLanguage;
        }
        continue;
      }
      if (key === "HTML_HEAD" || key === "HTML_HEAD_EXTRA") {
        if (!metadata.htmlHead) metadata.htmlHead = [];
        metadata.htmlHead.push(value);
      }
    }
    return metadata;
  }
  function hasKeywordMetadata(metadata) {
    return Boolean(
      metadata.author || metadata.date || metadata.subtitle || metadata.description || Array.isArray(metadata.keywords) && metadata.keywords.length > 0 || metadata.language || Array.isArray(metadata.htmlHead) && metadata.htmlHead.length > 0
    );
  }
  function renderHeadMetaSection(metadata) {
    if (!hasKeywordMetadata(metadata)) return "";
    const rows = [];
    if (metadata.author) {
      rows.push(`<meta name="author" content="${escapeAttr(metadata.author)}" />`);
    }
    if (metadata.date) {
      rows.push(`<meta name="date" content="${escapeAttr(metadata.date)}" />`);
    }
    if (metadata.subtitle) {
      rows.push(`<meta name="subtitle" content="${escapeAttr(metadata.subtitle)}" />`);
    }
    if (metadata.description) {
      rows.push(`<meta name="description" content="${escapeAttr(metadata.description)}" />`);
    }
    if (Array.isArray(metadata.keywords) && metadata.keywords.length > 0) {
      rows.push(`<meta name="keywords" content="${escapeAttr(metadata.keywords.join(", "))}" />`);
    }
    return rows.length > 0 ? `${rows.join("\n")}
` : "";
  }
  function renderHeadExtraSection(metadata, extraHead, includeDocumentHtml = true) {
    const snippets = [
      ...includeDocumentHtml && Array.isArray(metadata.htmlHead) ? metadata.htmlHead : [],
      ...Array.isArray(extraHead) ? extraHead : []
    ].map((snippet) => String(snippet || "").trim()).filter((snippet) => snippet.length > 0);
    return snippets.length > 0 ? `${snippets.join("\n")}
` : "";
  }
  function slugifyHeadlineTitle(value) {
    const normalized = String(value || "").trim().toLowerCase().replace(/[^a-z0-9\s-]/g, "").replace(/\s+/g, "-").replace(/-+/g, "-").replace(/^-|-$/g, "");
    return normalized || "section";
  }
  function normalizeAnchorId(value) {
    const normalized = String(value || "").trim().replace(/\s+/g, "-").replace(/[^A-Za-z0-9_.:-]/g, "-").replace(/-+/g, "-").replace(/^-|-$/g, "");
    return normalized || null;
  }
  function findHeadlineCustomId(node) {
    for (const child of node.children) {
      if (child.type !== "PropertyDrawer") continue;
      for (const property2 of child.properties) {
        if (String(property2.key || "").trim().toUpperCase() !== "CUSTOM_ID") continue;
        const normalized = normalizeAnchorId(property2.value);
        if (normalized) return normalized;
      }
    }
    return null;
  }
  function normalizeTocDepth(value) {
    if (value === void 0 || value === null) return null;
    if (!Number.isFinite(value)) return null;
    const normalized = Math.trunc(value);
    if (normalized < 1) return null;
    return Math.min(6, normalized);
  }
  function normalizeHeadlineNumberDepth(value) {
    if (value === void 0 || value === null) return null;
    if (!Number.isFinite(value)) return null;
    const normalized = Math.trunc(value);
    if (normalized < 1) return null;
    return Math.min(6, normalized);
  }
  function buildHeadlineAnchors(doc, opts = {}) {
    const includeToc = opts.includeToc === true;
    const includeTocDepth = normalizeTocDepth(opts.includeTocDepth);
    const includeHeadlineNumbers = opts.includeHeadlineNumbers === true;
    const includeHeadlineNumberDepth = normalizeHeadlineNumberDepth(opts.includeHeadlineNumberDepth);
    const items = [];
    const headlineIds = /* @__PURE__ */ new WeakMap();
    const headlineSlugIds = /* @__PURE__ */ new Map();
    const headlineNumbers = /* @__PURE__ */ new WeakMap();
    const headlineNumberCounts = [0, 0, 0, 0, 0, 0];
    const idCounts = /* @__PURE__ */ new Map();
    const nextId = (baseId) => {
      const normalizedBase = normalizeAnchorId(baseId) || "section";
      const key = normalizedBase.toLowerCase();
      const count = (idCounts.get(key) || 0) + 1;
      idCounts.set(key, count);
      if (count === 1) return normalizedBase;
      return `${normalizedBase}-${count}`;
    };
    const visitNodes = (nodes) => {
      for (const node of nodes) {
        if (node.type !== "Headline") continue;
        const title = node.title.map((child) => inlineToText(child)).join("").trim() || "Untitled";
        const titleSlug = slugifyHeadlineTitle(title);
        const customId = findHeadlineCustomId(node);
        const id = nextId(customId || titleSlug);
        const level = Math.max(1, Math.min(6, node.level));
        headlineIds.set(node, id);
        const titleKey = titleSlug.toLowerCase();
        if (!headlineSlugIds.has(titleKey)) {
          headlineSlugIds.set(titleKey, id);
        }
        headlineNumberCounts[level - 1] += 1;
        for (let idx = level; idx < headlineNumberCounts.length; idx += 1) {
          headlineNumberCounts[idx] = 0;
        }
        let headlineNumber = null;
        if (includeHeadlineNumbers && (includeHeadlineNumberDepth === null || level <= includeHeadlineNumberDepth)) {
          const parts = headlineNumberCounts.slice(0, level).filter((value) => value > 0);
          if (parts.length > 0) {
            headlineNumber = parts.join(".");
            headlineNumbers.set(node, headlineNumber);
          }
        }
        if (includeToc && (includeTocDepth === null || level <= includeTocDepth)) {
          items.push({
            id,
            title,
            level,
            ...headlineNumber ? { number: headlineNumber } : {}
          });
        }
        visitNodes(node.children);
      }
    };
    visitNodes(doc.children);
    return { items, headlineIds, headlineSlugIds, headlineNumbers };
  }
  function renderToc(items) {
    if (!Array.isArray(items) || items.length === 0) return "";
    const rows = items.map((item) => {
      const level = Math.max(1, Math.min(6, item.level));
      const numberPrefix = item.number ? `${escapeHtml(item.number)} ` : "";
      return `<li class="org2-toc-level-${level}"><a href="#${escapeAttr(item.id)}">${numberPrefix}${escapeHtml(item.title)}</a></li>`;
    }).join("\n");
    return `<nav class="org2-toc" aria-label="Table of contents">
<h2>Contents</h2>
<ul>
${rows}
</ul>
</nav>`;
  }
  function rewriteOrgFileHrefForHtml(rawHref) {
    const href = String(rawHref || "").trim();
    if (!href) return href;
    const hasScheme = /^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(href);
    const isFileScheme = /^file:/i.test(href);
    if (hasScheme && !isFileScheme) return href;
    const source = isFileScheme ? href.slice(5) : href;
    const searchSeparatorIndex = source.indexOf("::");
    const pathPart = searchSeparatorIndex >= 0 ? source.slice(0, searchSeparatorIndex) : source;
    const searchSuffix = searchSeparatorIndex >= 0 ? source.slice(searchSeparatorIndex + 2) : "";
    if (!/\.(org|org2)(?=($|[?#]))/i.test(pathPart)) return href;
    const rewrittenPath = pathPart.replace(/\.(org|org2)(?=($|[?#]))/i, ".html");
    if (!searchSuffix.trim()) return rewrittenPath;
    const trimmedSearchSuffix = searchSuffix.trim();
    if (trimmedSearchSuffix.startsWith("#")) {
      return `${rewrittenPath}${trimmedSearchSuffix}`;
    }
    if (trimmedSearchSuffix.startsWith("*")) {
      const headingText = trimmedSearchSuffix.replace(/^\*+\s*/, "").trim();
      return `${rewrittenPath}#${slugifyHeadlineTitle(headingText || "section")}`;
    }
    return href;
  }
  function rewriteOrgInternalHrefForHtml(rawHref, context) {
    const href = String(rawHref || "").trim();
    if (!href) return href;
    if (href.startsWith("#")) {
      const normalizedAnchor = normalizeAnchorId(href.slice(1));
      return normalizedAnchor ? `#${normalizedAnchor}` : href;
    }
    if (!href.startsWith("*")) return href;
    const headingText = href.replace(/^\*+\s*/, "").trim();
    const headingSlug = slugifyHeadlineTitle(headingText || "section");
    const resolvedHeadingId = context.headlineSlugIds?.get(headingSlug.toLowerCase()) || headingSlug;
    return `#${resolvedHeadingId}`;
  }
  function linkTargetNeedsHeadingAnchor(rawTarget) {
    const target = String(rawTarget || "").trim();
    if (!target) return false;
    return target.startsWith("#") || target.startsWith("*");
  }
  function resolveDefaultInternalLinkText(rawTarget) {
    const target = String(rawTarget || "").trim();
    if (!target) return null;
    if (target.startsWith("*")) {
      const headingText = target.replace(/^\*+\s*/, "").trim();
      return headingText || null;
    }
    if (target.startsWith("#")) {
      const anchorRaw = target.slice(1).trim();
      if (!anchorRaw) return null;
      return normalizeAnchorId(anchorRaw) || anchorRaw;
    }
    return null;
  }
  function inlineNodesNeedHeadingAnchors(nodes) {
    for (const node of nodes) {
      if (node.type !== "Link") continue;
      if (linkTargetNeedsHeadingAnchor(node.targetRaw)) return true;
    }
    return false;
  }
  function nodesNeedHeadingAnchors(nodes) {
    for (const node of nodes) {
      if (node.type === "Headline") {
        if (inlineNodesNeedHeadingAnchors(node.title)) return true;
        if (nodesNeedHeadingAnchors(node.children)) return true;
        continue;
      }
      if (node.type === "Paragraph") {
        if (inlineNodesNeedHeadingAnchors(node.children)) return true;
        continue;
      }
      if (node.type === "List") {
        for (const item of node.items) {
          if (nodesNeedHeadingAnchors(item.children)) return true;
        }
        continue;
      }
      if (node.type === "ListItem") {
        if (nodesNeedHeadingAnchors(node.children)) return true;
      }
    }
    return false;
  }
  function inlineToText(node) {
    if (node.type === "Text") return node.value;
    if (node.type === "Timestamp") return node.raw;
    if (node.type === "TimestampRange") return `${node.start.raw}${node.separatorRaw}${node.end.raw}`;
    if (node.type === "Emphasis") return node.content;
    if (node.type === "Link") return node.descriptionRaw || node.targetRaw;
    if (node.type === "Target") return node.valueRaw;
    if (node.type === "Script") return node.valueRaw;
    if (node.type === "ExportSnippet") return node.valueRaw;
    if (node.type === "FootnoteReference") return node.definitionRaw || node.labelRaw || "";
    return node.raw;
  }
  var HTML_ENTITY_VALUES = {
    alpha: "\u03B1",
    beta: "\u03B2",
    gamma: "\u03B3",
    delta: "\u03B4",
    epsilon: "\u03B5",
    zeta: "\u03B6",
    eta: "\u03B7",
    theta: "\u03B8",
    iota: "\u03B9",
    kappa: "\u03BA",
    lambda: "\u03BB",
    mu: "\u03BC",
    nu: "\u03BD",
    xi: "\u03BE",
    omicron: "\u03BF",
    pi: "\u03C0",
    rho: "\u03C1",
    sigma: "\u03C3",
    tau: "\u03C4",
    upsilon: "\u03C5",
    phi: "\u03C6",
    chi: "\u03C7",
    psi: "\u03C8",
    omega: "\u03C9",
    Alpha: "\u0391",
    Beta: "\u0392",
    Gamma: "\u0393",
    Delta: "\u0394",
    Theta: "\u0398",
    Lambda: "\u039B",
    Xi: "\u039E",
    Pi: "\u03A0",
    Sigma: "\u03A3",
    Upsilon: "\u03A5",
    Phi: "\u03A6",
    Psi: "\u03A8",
    Omega: "\u03A9",
    nbsp: "\xA0",
    copy: "\xA9",
    reg: "\xAE",
    trade: "\u2122",
    ndash: "\u2013",
    mdash: "\u2014",
    hellip: "\u2026",
    laquo: "\xAB",
    raquo: "\xBB",
    lsquo: "\u2018",
    rsquo: "\u2019",
    ldquo: "\u201C",
    rdquo: "\u201D",
    bull: "\u2022",
    middot: "\xB7",
    times: "\xD7",
    divide: "\xF7",
    plusmn: "\xB1",
    le: "\u2264",
    ge: "\u2265",
    ne: "\u2260",
    infin: "\u221E",
    rarr: "\u2192",
    larr: "\u2190",
    uarr: "\u2191",
    darr: "\u2193",
    harr: "\u2194",
    check: "\u2713",
    deg: "\xB0"
  };
  function renderTimestamp(node) {
    const klass = node.active ? "org2-timestamp active" : "org2-timestamp inactive";
    return `<time class="${klass}">${escapeHtml(node.raw)}</time>`;
  }
  function renderTimestampRange(node) {
    const klass = node.start.active ? "org2-timestamp-range active" : "org2-timestamp-range inactive";
    return `<time class="${klass}">${escapeHtml(node.start.raw + node.separatorRaw + node.end.raw)}</time>`;
  }
  function renderEmphasis(node) {
    const content = escapeHtml(node.content);
    if (node.kind === "bold") return `<strong>${content}</strong>`;
    if (node.kind === "italic") return `<em>${content}</em>`;
    if (node.kind === "underline") return `<span class="org2-underline">${content}</span>`;
    if (node.kind === "strike") return `<del>${content}</del>`;
    return `<code>${content}</code>`;
  }
  function appLinkHref(rawTarget, expandedTarget, context) {
    if (!context.nativeInternalLinks && linkTargetNeedsHeadingAnchor(expandedTarget)) {
      return rewriteOrgInternalHrefForHtml(expandedTarget, context);
    }
    if (/^(https?|mailto):/i.test(expandedTarget)) {
      return expandedTarget;
    }
    return `org2-workspace://open-link?target=${encodeURIComponent(rawTarget)}`;
  }
  function renderLink(node, context) {
    const hrefRaw = String(node.targetRaw || "").trim();
    const colorBinding = node.descriptionRaw !== void 0 ? parseOrgColorBindingTarget(hrefRaw) : null;
    if (colorBinding) {
      return renderColorBinding(node.descriptionRaw || "", colorBinding, false);
    }
    const expandedHrefRaw = expandLinkAbbreviationTarget(hrefRaw, context.linkAbbreviations);
    let href = context.profile === "app" ? appLinkHref(hrefRaw, expandedHrefRaw, context) : expandedHrefRaw;
    if (context.profile !== "app" && context.rewriteFileLinks) {
      href = rewriteOrgFileHrefForHtml(expandedHrefRaw);
    }
    if (context.profile !== "app" && linkTargetNeedsHeadingAnchor(expandedHrefRaw)) {
      href = rewriteOrgInternalHrefForHtml(expandedHrefRaw, context);
    }
    const explicitDescription = String(node.descriptionRaw || "").trim();
    const defaultInternalText = resolveDefaultInternalLinkText(hrefRaw);
    const text2 = explicitDescription || defaultInternalText || String(node.targetRaw || "").trim() || href;
    return `<a href="${escapeAttr(href)}">${escapeHtml(text2)}</a>`;
  }
  function colorBindingStyles(binding, wholeCell) {
    const styles = [];
    if (binding.foreground) styles.push(`color: ${binding.foreground.css}`);
    if (binding.background) styles.push(`background-color: ${binding.background.css}`);
    if (binding.background && !wholeCell) {
      styles.push("border-radius: 0.2em", "padding: 0 0.12em", "box-decoration-break: clone", "-webkit-box-decoration-break: clone");
    }
    return styles.join("; ") + (styles.length > 0 ? ";" : "");
  }
  function renderColorBinding(label, binding, wholeCell) {
    const style = escapeAttr(colorBindingStyles(binding, wholeCell));
    if (wholeCell) return escapeHtml(label);
    return `<span class="org2-color-binding" style="${style}">${escapeHtml(label)}</span>`;
  }
  var IMAGE_LINK_EXTENSION = /\.(?:avif|bmp|gif|heic|heif|jpe?g|png|svg|webp)$/i;
  function resolveImageLinkSource(rawTarget, context) {
    const expandedTarget = expandLinkAbbreviationTarget(
      String(rawTarget || "").trim(),
      context.linkAbbreviations
    );
    if (!expandedTarget) return null;
    if (/^data:image\/(?:avif|gif|jpeg|png|webp);base64,[a-z0-9+/=]+$/i.test(expandedTarget)) {
      return expandedTarget;
    }
    const targetWithoutSearch = expandedTarget.split("::", 1)[0] || "";
    const match = targetWithoutSearch.match(/^([^?#]*)([?#].*)?$/);
    const pathPart = match?.[1] || "";
    const suffix = match?.[2] || "";
    if (!IMAGE_LINK_EXTENSION.test(pathPart)) return null;
    if (/^https?:/i.test(pathPart)) return `${pathPart}${suffix}`;
    if (/^file:/i.test(pathPart)) {
      const fileTarget = pathPart.slice(5);
      if (fileTarget.startsWith("//")) return `file:${fileTarget}${suffix}`;
      if (fileTarget.startsWith("/")) return `file://${fileTarget}${suffix}`;
      return `${fileTarget}${suffix}`;
    }
    if (/^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(pathPart)) return null;
    return `${pathPart}${suffix}`;
  }
  function imageAltText(source) {
    if (/^data:image\//i.test(source)) return "Image";
    const pathPart = source.split(/[?#]/, 1)[0] || "";
    const filename = node_path_default.basename(pathPart).replace(/\.[^.]+$/, "");
    try {
      return decodeURIComponent(filename).replace(/[-_]+/g, " ").trim() || "Image";
    } catch {
      return filename.replace(/[-_]+/g, " ").trim() || "Image";
    }
  }
  function renderStandaloneImageParagraph(node, context) {
    const meaningfulChildren = node.children.filter(
      (child) => child.type !== "Text" || child.value.trim().length > 0
    );
    if (meaningfulChildren.length !== 1 || meaningfulChildren[0]?.type !== "Link") return null;
    const link2 = meaningfulChildren[0];
    const description = String(link2.descriptionRaw || "").trim();
    const source = resolveImageLinkSource(link2.targetRaw, context);
    if (!source) return null;
    const expandedTarget = expandLinkAbbreviationTarget(link2.targetRaw, context.linkAbbreviations);
    const href = context.profile === "app" ? appLinkHref(link2.targetRaw, expandedTarget, context) : source;
    const sourceAttributes = renderSourceAttributes(node, context);
    return `<figure class="org2-image-figure"${sourceAttributes}><a class="org2-image-link" href="${escapeAttr(href)}"><img class="org2-image" src="${escapeAttr(source)}" alt="${escapeAttr(description || imageAltText(source))}" loading="lazy" decoding="async" /></a></figure>`;
  }
  function renderInline(node, context) {
    if (node.type === "Text") return escapeHtml(node.value);
    if (node.type === "Timestamp") return renderTimestamp(node);
    if (node.type === "TimestampRange") return renderTimestampRange(node);
    if (node.type === "Emphasis") return renderEmphasis(node);
    if (node.type === "Link") return renderLink(node, context);
    if (node.type === "ProgressCookie") return `<span class="org2-progress-cookie">${escapeHtml(node.raw)}</span>`;
    if (node.type === "Entity") return `<span class="org2-entity" title="${escapeAttr(node.raw)}">${escapeHtml(HTML_ENTITY_VALUES[node.nameRaw] || node.raw)}</span>`;
    if (node.type === "LatexFragment") {
      const displayClass = node.display ? " display" : "";
      return `<span class="org2-latex-fragment${displayClass}">${escapeHtml(node.raw)}</span>`;
    }
    if (node.type === "ExportSnippet") {
      if (node.backendRaw.toLowerCase() !== "html") return "";
      return context.profile === "app" ? `<span class="org2-export-snippet">${escapeHtml(node.valueRaw)}</span>` : node.valueRaw;
    }
    if (node.type === "FootnoteReference") {
      const label = node.labelRaw || "*";
      if (node.definitionRaw !== void 0) {
        return `<sup class="org2-footnote-reference inline" title="${escapeAttr(node.definitionRaw)}">${escapeHtml(label)}</sup>`;
      }
      const id = normalizeAnchorId(`fn-${label}`) || "fn-inline";
      return `<sup class="org2-footnote-reference"><a href="#${escapeAttr(id)}">${escapeHtml(label)}</a></sup>`;
    }
    if (node.type === "Citation") {
      const references = node.references.map(
        (reference) => [reference.prefixRaw, `@${reference.keyRaw}`, reference.suffixRaw].filter(Boolean).join(" ")
      ).join("; ");
      const content = [node.prefixRaw, references, node.suffixRaw].filter(Boolean).join(" ");
      return `<cite class="org2-citation" data-org2-citation-style="${escapeAttr(node.styleRaw || "")}">${escapeHtml(content)}</cite>`;
    }
    if (node.type === "Target") {
      const id = normalizeAnchorId(node.valueRaw) || "target";
      return `<span class="org2-target${node.radio ? " radio" : ""}" id="${escapeAttr(id)}"></span>`;
    }
    if (node.type === "Script") {
      const tag = node.kind === "subscript" ? "sub" : "sup";
      return `<${tag}>${escapeHtml(node.valueRaw)}</${tag}>`;
    }
    if (node.type === "LineBreak") return "<br />";
    return "";
  }
  function renderInlineChildren(nodes, context) {
    return nodes.map((node) => renderInline(node, context)).join("");
  }
  function renderSourceAttributes(node, context) {
    if (context.profile !== "app") return "";
    const range = node.sourceRange;
    if (!range) return "";
    return ` data-org2-start-line="${range.startLine}" data-org2-end-line="${range.endLine}"`;
  }
  function renderParagraph(node, context) {
    const image = renderStandaloneImageParagraph(node, context);
    if (image) return image;
    const sourceAttributes = renderSourceAttributes(node, context);
    if (node.children.length === 1 && node.children[0]?.type === "Text") {
      const raw = String(node.children[0].value || "").trim();
      if (raw === "--") return `<p${sourceAttributes}>&#x2013;</p>`;
      if (raw === "---") return `<p${sourceAttributes}>&#x2014;</p>`;
    }
    return `<p${sourceAttributes}>${renderInlineChildren(node.children, context)}</p>`;
  }
  function renderPlanning(node, context) {
    const raw = node.timestamp ? node.timestamp.type === "Timestamp" ? node.timestamp.raw : node.timestamp.start.raw + node.timestamp.separatorRaw + node.timestamp.end.raw : node.raw;
    return `<p class="org2-planning"${renderSourceAttributes(node, context)}><span class="org2-planning-kind">${escapeHtml(node.kind)}</span> ${escapeHtml(raw)}</p>`;
  }
  function renderPropertyDrawer(node, context) {
    if (!node.properties.length) return "";
    const rows = node.properties.map((property2) => `<dt>${escapeHtml(property2.key)}</dt><dd>${escapeHtml(property2.value)}</dd>`).join("\n");
    if (context.profile === "app") {
      return `<details class="org2-properties-drawer" open${renderSourceAttributes(node, context)}>
<summary>Properties</summary>
<dl class="org2-properties">
${rows}
</dl>
</details>`;
    }
    return `<dl class="org2-properties"${renderSourceAttributes(node, context)}>
${rows}
</dl>`;
  }
  function renderSrcBlock(node, context) {
    const languageRaw = String(node.begin.afterKeywordRaw || "").trim().split(/\s+/)[0];
    const language = languageRaw.toLowerCase().replace(/[^a-z0-9_+-]/g, "");
    const sourceRange = node.sourceRange;
    const pluginRender = sourceRange ? context.pluginRendersByBlockLine?.get(sourceRange.startLine) : void 0;
    if (pluginRender) return renderPluginFrame(pluginRender, renderSourceAttributes(node, context));
    const embeddedChart = sourceRange && (language === "chart" || language === "plot") ? context.chartsByBlockLine?.get(sourceRange.startLine) : void 0;
    if (embeddedChart) return renderEmbeddedChart(embeddedChart, renderSourceAttributes(node, context));
    const languageClass = language ? ` language-${language}` : "";
    const codeClassAttr = language ? ` class="language-${escapeAttr(language)}"` : "";
    const raw = node.bodyRaw.replace(/\n$/, "");
    const body = escapeHtml(raw);
    const baseStyle = "padding: 0.9rem 1rem; border: 1px solid rgba(127,127,127,0.28); border-radius: 0.6rem; background: rgba(127,127,127,0.11); font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, 'Liberation Mono', monospace; font-size: 0.92rem; line-height: 1.28;";
    const pre = `<pre class="org2-src${languageClass}"${renderSourceAttributes(node, context)} style="${escapeAttr(baseStyle)}"><code${codeClassAttr}>${body}</code></pre>`;
    const lineCount = raw.split("\n").length;
    if (context.profile === "app" && (raw.length > 32768 || lineCount > 200)) {
      return `<details class="org2-large-source"${renderSourceAttributes(node, context)}><summary>${language ? `${escapeHtml(language)} source` : "Source"} \xB7 ${lineCount} lines</summary>${pre}</details>`;
    }
    return pre;
  }
  function renderPluginFrame(render, sourceAttributes = "") {
    const identity = `${render.pluginId}:${render.rendererId}`;
    if (render.error) {
      return `<aside class="org2-plugin-render-error" data-org2-plugin="${escapeAttr(identity)}"${sourceAttributes}><strong>Plugin renderer unavailable</strong><span>${escapeHtml(render.error)}</span></aside>`;
    }
    const csp = "default-src 'none'; base-uri 'none'; connect-src 'none'; font-src data:; form-action 'none'; frame-src 'none'; img-src data: blob:; media-src data: blob:; navigate-to 'none'; object-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; worker-src 'none'";
    const css = String(render.css || "").replace(/<\/style/gi, "<\\/style");
    const script = String(render.script || "").replace(/<\/script/gi, "<\\/script");
    const document = `<!doctype html><html><head><meta charset="utf-8"><meta http-equiv="Content-Security-Policy" content="${escapeAttr(csp)}"><meta name="referrer" content="no-referrer"><style>html,body{margin:0;padding:0;color-scheme:light dark;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}*{box-sizing:border-box}${css}</style></head><body>${render.html || ""}${script ? `<script>${script}<\/script>` : ""}</body></html>`;
    const title = render.title || `${render.pluginId} ${render.rendererId}`;
    return `<figure class="org2-plugin-render" data-org2-plugin="${escapeAttr(identity)}"${sourceAttributes}><iframe title="${escapeAttr(title)}" sandbox="allow-scripts" referrerpolicy="no-referrer" loading="lazy" style="display:block;width:100%;height:${render.height}px;border:0;background:transparent" srcdoc="${escapeAttr(document)}"></iframe></figure>`;
  }
  function renderEmbeddedChart(chart, sourceAttributes = "") {
    const presentation = chart.presentation;
    const size = presentation?.size || "medium";
    const interactive = presentation?.interactive !== false;
    return `<figure class="org2-chart org2-chart-${size}" data-org2-chart-interactive="${interactive}"${sourceAttributes}>
${chart.svg.trim()}
</figure>`;
  }
  function renderBlock(node, context) {
    const bodyRaw = node.bodyRaw.replace(/\n$/, "");
    const body = escapeHtml(node.kind === "quote" ? dedentBlockBody(bodyRaw, node.begin.indent) : bodyRaw);
    if (node.kind === "quote") {
      const appClass = context.profile === "app" ? ' class="org2-quote"' : "";
      return `<blockquote${appClass}${renderSourceAttributes(node, context)}>${body}</blockquote>`;
    }
    if (node.kind === "center") return `<div class="org2-center">${body}</div>`;
    if (node.kind === "verse") return `<pre class="org2-verse">${body}</pre>`;
    if (node.kind === "comment") return `<pre class="org2-comment">${body}</pre>`;
    if (node.kind === "export") {
      const exportTarget = String(node.begin.afterKeywordRaw || "").trim().toLowerCase();
      if (exportTarget === "html" && context.profile !== "app") return bodyRaw;
      return `<pre class="org2-export">${body}</pre>`;
    }
    if (node.kind === "example") return `<pre class="org2-example">${body}</pre>`;
    const kind = node.kind.toLowerCase().replace(/[^a-z0-9_-]+/g, "-");
    return `<div class="org2-special-block org2-special-block-${escapeAttr(kind)}"${renderSourceAttributes(node, context)}><pre>${body}</pre></div>`;
  }
  function dedentBlockBody(bodyRaw, indent) {
    if (!indent) return bodyRaw;
    return bodyRaw.split("\n").map((line) => line.startsWith(indent) ? line.slice(indent.length) : line).join("\n");
  }
  function renderTableRow(row, asHeader, context) {
    const cellTag = asHeader ? "th" : "td";
    const cells = row.cells.map((cell) => {
      const parsed = parseInlinesFromText(String(cell || "").trim());
      if (parsed.length === 1 && parsed[0]?.type === "Link" && parsed[0].descriptionRaw !== void 0) {
        const binding = parseOrgColorBindingTarget(parsed[0].targetRaw);
        if (binding) {
          const style = escapeAttr(colorBindingStyles(binding, true));
          return `<${cellTag} class="org2-color-cell" style="${style}">${renderColorBinding(parsed[0].descriptionRaw, binding, true)}</${cellTag}>`;
        }
      }
      const rendered = renderInlineChildren(parsed, context);
      return `<${cellTag}>${rendered}</${cellTag}>`;
    }).join("");
    return `<tr>${cells}</tr>`;
  }
  function renderTable(node, context) {
    const formulaResult = node.formulas?.length ? evaluateTableNode(node) : void 0;
    const renderedNode = formulaResult?.ok ? formulaResult.table : node;
    const rows = renderedNode.rows;
    const firstHline = rows.findIndex((row) => row.type === "TableHline");
    const headerRows = firstHline > 0 ? rows.slice(0, firstHline).filter((row) => row.type === "TableRow") : [];
    const bodyRows = firstHline >= 0 ? rows.slice(firstHline + 1).filter((row) => row.type === "TableRow") : rows.filter((row) => row.type === "TableRow");
    const resolvedBodyRows = bodyRows.length > 0 ? bodyRows : headerRows;
    const renderedHead = headerRows.length > 0 ? `<thead>
${headerRows.map((row) => renderTableRow(row, true, context)).join("\n")}
</thead>` : "";
    const renderedBody = `<tbody>
${resolvedBodyRows.map((row) => renderTableRow(row, false, context)).join("\n")}
</tbody>`;
    const sourceAttributes = renderSourceAttributes(node, context);
    const formulaAttributes = node.formulas?.length ? ` data-org2-formula-count="${node.formulas.length}" data-org2-formula-state="${formulaResult?.ok ? "current" : "error"}"` : "";
    const table = `<table${sourceAttributes}${formulaAttributes}>
${[renderedHead, renderedBody].filter(Boolean).join("\n")}
</table>`;
    const tableHtml = context.profile === "app" || context.profile === "publish" ? `<div class="org2-table-scroll">
${table}
</div>` : table;
    const formulaStatus = formulaResult ? `<small class="org2-table-formula-status${formulaResult.ok ? "" : " org2-table-formula-error"}">${formulaResult.ok ? `Calculated from TBLFM${(node.formulas?.length ?? 0) > 1 ? ` line 1 of ${node.formulas?.length}` : ""}` : `Formula not evaluated: ${escapeHtml(formulaResult.diagnostics[0]?.message ?? "unsupported formula")}`}</small>` : "";
    const sourceRange = node.sourceRange;
    const chart = sourceRange ? context.chartsByTableLine?.get(sourceRange.startLine) : void 0;
    const output = `${tableHtml}${formulaStatus ? `
${formulaStatus}` : ""}`;
    return chart ? `${output}
${renderEmbeddedChart(chart)}` : output;
  }
  function renderListItem(node, context) {
    const body = renderNodes(node.children, context);
    const sourceAttributes = renderSourceAttributes(node, context);
    const ordinalAttribute = node.ordinal !== void 0 ? ` value="${node.ordinal}"` : "";
    const counterAttribute = node.counter !== void 0 ? ` data-org2-counter="${node.counter}"` : "";
    const checkbox = node.checkbox === "checked" ? '<input type="checkbox" checked disabled /> ' : node.checkbox === "unchecked" ? '<input type="checkbox" disabled /> ' : node.checkbox === "indeterminate" ? '<span class="org2-checkbox-mixed" role="checkbox" aria-checked="mixed">\u2212</span> ' : "";
    const description = node.descriptionTag ? `<strong class="org2-description-tag">${renderInlineChildren(node.descriptionTag, context)}</strong> \u2014 ` : "";
    if (!body.trim()) return `<li${ordinalAttribute}${counterAttribute}${sourceAttributes}>${checkbox}${description}</li>`;
    if (body.includes("\n")) return `<li${ordinalAttribute}${counterAttribute}${sourceAttributes}>${checkbox}${description}
${body}
</li>`;
    return `<li${ordinalAttribute}${counterAttribute}${sourceAttributes}>${checkbox}${description}${body}</li>`;
  }
  function renderList(node, context) {
    if (node.items.length > 0 && node.items.every((item) => item.descriptionTag)) {
      const entries = node.items.map((item) => {
        const term = renderInlineChildren(item.descriptionTag || [], context);
        const body = renderNodes(item.children, context);
        const checkbox = item.checkbox === "checked" ? '<input type="checkbox" checked disabled /> ' : item.checkbox === "unchecked" ? '<input type="checkbox" disabled /> ' : item.checkbox === "indeterminate" ? '<span class="org2-checkbox-mixed" role="checkbox" aria-checked="mixed">\u2212</span> ' : "";
        const counterAttribute = item.counter !== void 0 ? ` data-org2-counter="${item.counter}"` : "";
        return `<dt${counterAttribute}>${term}</dt><dd>${checkbox}${body}</dd>`;
      }).join("\n");
      return `<dl class="org2-description-list"${renderSourceAttributes(node, context)}>
${entries}
</dl>`;
    }
    const tag = node.ordered ? "ol" : "ul";
    const items = node.items.map((item) => renderListItem(item, context)).join("\n");
    return `<${tag}${renderSourceAttributes(node, context)}>
${items}
</${tag}>`;
  }
  function renderHeadline(node, context) {
    const headingLevel = Math.max(1, Math.min(6, node.level));
    const headingTag = `h${headingLevel}`;
    const title = renderInlineChildren(node.title, context);
    const headingNumberRaw = context.headlineNumbers?.get(node);
    const headingNumber = headingNumberRaw ? `<span class="org2-headline-number">${escapeHtml(headingNumberRaw)}</span> ` : "";
    const todoClass = String(node.todo || "").trim().toLowerCase().replace(/[^a-z0-9_-]+/g, "-");
    const appTodoClass = context.profile === "app" && todoClass ? ` todo-${escapeAttr(todoClass)}` : "";
    const todo = node.todo ? `<span class="org2-todo${appTodoClass}">${escapeHtml(node.todo)}</span> ` : "";
    const priority = node.priority ? `<span class="org2-priority">[#${escapeHtml(node.priority)}]</span> ` : "";
    const comment = node.commented ? '<span class="org2-comment-keyword">COMMENT</span> ' : "";
    const tags = node.tags && node.tags.length > 0 ? ` <span class="org2-tags">${node.tags.map((tag) => `<span class="org2-tag">${escapeHtml(tag)}</span>`).join(" ")}</span>` : "";
    const headingId = context.headlineIds?.get(node);
    const headingIdAttr = headingId ? ` id="${escapeAttr(headingId)}"` : "";
    const childrenHtml = renderNodes(node.children, context);
    if (context.profile === "app") {
      const body = childrenHtml.trim() ? `
<div class="org2-headline-body">
${childrenHtml}
</div>` : "";
      return `<details class="org2-headline level-${node.level}${node.commented ? " commented" : ""}" open${renderSourceAttributes(node, context)}>
<summary class="org2-headline-summary"><${headingTag}${headingIdAttr}>${headingNumber}${todo}${priority}${comment}${title}${tags}</${headingTag}></summary>${body}
</details>`;
    }
    if (!childrenHtml.trim()) {
      return `<section class="org2-headline level-${node.level}${node.commented ? " commented" : ""}"${renderSourceAttributes(node, context)}>
<${headingTag}${headingIdAttr}>${headingNumber}${todo}${priority}${comment}${title}${tags}</${headingTag}>
</section>`;
    }
    return `<section class="org2-headline level-${node.level}${node.commented ? " commented" : ""}"${renderSourceAttributes(node, context)}>
<${headingTag}${headingIdAttr}>${headingNumber}${todo}${priority}${comment}${title}${tags}</${headingTag}>
${childrenHtml}
</section>`;
  }
  function renderNode(node, context) {
    if (node.type === "Headline") return renderHeadline(node, context);
    if (node.type === "Paragraph") return renderParagraph(node, context);
    if (node.type === "List") return renderList(node, context);
    if (node.type === "ListItem") return renderListItem(node, context);
    if (node.type === "Planning") return renderPlanning(node, context);
    if (node.type === "PropertyDrawer") return renderPropertyDrawer(node, context);
    if (node.type === "SrcBlock") return renderSrcBlock(node, context);
    if (node.type === "Block") return renderBlock(node, context);
    if (node.type === "DynamicBlock") {
      const body = escapeHtml(node.bodyRaw.replace(/\n$/, ""));
      return `<section class="org2-dynamic-block" data-org2-dynamic-block="${escapeAttr(node.nameRaw)}"${renderSourceAttributes(node, context)}><header>${escapeHtml(node.nameRaw)}</header><pre>${body}</pre></section>`;
    }
    if (node.type === "FixedWidth") {
      const body = node.lines.map((line) => line.valueRaw).join("\n");
      return `<pre class="org2-fixed-width"${renderSourceAttributes(node, context)}>${escapeHtml(body)}</pre>`;
    }
    if (node.type === "HorizontalRule") return `<hr${renderSourceAttributes(node, context)} />`;
    if (node.type === "LatexEnvironment") {
      const raw = [node.beginRaw, node.bodyRaw, node.endRaw].filter(Boolean).join("\n");
      return `<pre class="org2-latex-environment" data-org2-latex-environment="${escapeAttr(node.nameRaw)}"${renderSourceAttributes(node, context)}>${escapeHtml(raw)}</pre>`;
    }
    if (node.type === "DiarySexp") return `<code class="org2-diary-sexp"${renderSourceAttributes(node, context)}>${escapeHtml(node.raw)}</code>`;
    if (node.type === "FootnoteDefinition") {
      const id = normalizeAnchorId(`fn-${node.labelRaw}`) || "fn";
      return `<aside class="org2-footnote-definition" id="${escapeAttr(id)}"${renderSourceAttributes(node, context)}><sup>${escapeHtml(node.labelRaw)}</sup> ${renderInlineChildren(node.children, context)}</aside>`;
    }
    if (node.type === "Table") return renderTable(node, context);
    if (node.type === "Drawer") {
      const name = escapeHtml(node.nameRaw);
      const body = escapeHtml(node.bodyRaw.replace(/\n$/, ""));
      return `<details class="org2-drawer"${renderSourceAttributes(node, context)}><summary>${name}</summary><pre>${body}</pre></details>`;
    }
    if (node.type === "KeywordLine") {
      const key = String(node.keyRaw || "").trim().toUpperCase();
      if (HIDDEN_DOCUMENT_KEYWORDS.has(key)) return "";
      return `<p class="org2-keyword"><span class="org2-keyword-name">${escapeHtml(node.keyRaw)}</span>: ${escapeHtml(node.valueRaw.trim())}</p>`;
    }
    if (node.type === "DirectiveLine") {
      return `<pre class="org2-directive">${escapeHtml(node.raw)}</pre>`;
    }
    if (node.type === "CommentLine") return "";
    if (node.type === "Text") {
      const text2 = String(node.value || "");
      return text2.trim().length > 0 ? `<p>${escapeHtml(text2)}</p>` : "";
    }
    return "";
  }
  function renderNodes(nodes, context = {}) {
    return nodes.map((node) => renderNode(node, context)).filter((html) => String(html || "").trim().length > 0).join("\n");
  }
  function splitAppFileProperties(nodes) {
    const properties = [];
    const body = [];
    let inPreamble = true;
    for (const node of nodes) {
      if (inPreamble && node.type === "CommentLine") continue;
      if (inPreamble && node.type === "KeywordLine") {
        const key = String(node.keyRaw || "").trim().toUpperCase();
        if (!HIDDEN_DOCUMENT_KEYWORDS.has(key)) properties.push(node);
        continue;
      }
      inPreamble = false;
      body.push(node);
    }
    return { properties, body };
  }
  function renderAppFileProperties(nodes, context) {
    if (nodes.length === 0) return "";
    const rows = renderNodes(nodes, context);
    if (!rows.trim()) return "";
    return `<details class="org2-file-properties">
<summary>File properties <span class="org2-file-properties-count">${nodes.length}</span></summary>
<div class="org2-file-properties-body">
${rows}
</div>
</details>`;
  }
  function findTitleFromKeywords(doc) {
    for (const node of doc.children) {
      if (node.type !== "KeywordLine") continue;
      if (String(node.keyRaw || "").trim().toUpperCase() !== "TITLE") continue;
      const value = String(node.valueRaw || "").trim();
      if (value) return value;
    }
    return null;
  }
  function findSubtitleFromKeywords(doc) {
    for (const node of doc.children) {
      if (node.type !== "KeywordLine") continue;
      if (String(node.keyRaw || "").trim().toUpperCase() !== "SUBTITLE") continue;
      const value = String(node.valueRaw || "").trim();
      if (value) return value;
    }
    return null;
  }
  function findTitleFromHeadlines(doc) {
    for (const node of doc.children) {
      if (node.type !== "Headline") continue;
      const text2 = node.title.map((child) => inlineToText(child)).join("").trim();
      if (text2) return text2;
    }
    return null;
  }
  function resolveTitle(doc, explicitTitle, sourcePath) {
    const explicit = String(explicitTitle || "").trim();
    if (explicit) return explicit;
    const keyword = findTitleFromKeywords(doc);
    if (keyword) return keyword;
    const headline = findTitleFromHeadlines(doc);
    if (headline) return headline;
    if (sourcePath) return node_path_default.basename(sourcePath);
    return "Org2 Document";
  }
  function renderDocumentHeader(opts) {
    const title = String(opts.title || "").trim();
    if (!title) return "";
    const subtitle = String(opts.subtitle || "").trim();
    const subtitleHtml = subtitle ? `
<p class="org2-document-subtitle" role="doc-subtitle">${escapeHtml(subtitle)}</p>` : "";
    return `<header class="org2-document-header">
<h1 class="org2-document-title">${escapeHtml(title)}</h1>${subtitleHtml}
</header>`;
  }
  function resolveDocumentRenderOptions(doc, opts) {
    const exportOptions = collectKeywordOptions(doc);
    const includeToc = opts.includeToc === true || opts.includeToc !== false && exportOptions.toc === true;
    const includeTocDepth = normalizeTocDepth(opts.includeTocDepth) ?? normalizeTocDepth(exportOptions.tocDepth) ?? void 0;
    const includeHeadlineNumbers = opts.includeHeadlineNumbers === true || opts.includeHeadlineNumbers !== false && exportOptions.num === true;
    const includeHeadlineNumberDepth = normalizeHeadlineNumberDepth(opts.includeHeadlineNumberDepth) ?? normalizeHeadlineNumberDepth(exportOptions.numDepth) ?? void 0;
    const includeDocumentHeader = opts.includeDocumentHeader === true || opts.includeDocumentHeader !== false && Boolean(findSubtitleFromKeywords(doc));
    return {
      includeToc,
      includeTocDepth,
      includeHeadlineNumbers,
      includeHeadlineNumberDepth,
      includeDocumentHeader
    };
  }
  function buildDocumentRenderContext(doc, opts) {
    let tocItems = [];
    const includeHeadingAnchors = opts.includeToc || opts.rewriteFileLinks === true || nodesNeedHeadingAnchors(doc.children);
    const includeHeadlineData = includeHeadingAnchors || opts.includeHeadlineNumbers;
    const builtIns = buildBuiltInLinkAbbreviations(opts.linearTeam);
    const configAbbreviations = collectLinkAbbreviationsFromRecord(opts.linkAbbreviations);
    const documentAbbreviations = collectLinkAbbreviationsFromDoc(doc);
    const context = {
      rewriteFileLinks: opts.rewriteFileLinks === true,
      nativeInternalLinks: opts.nativeInternalLinks,
      profile: opts.profile,
      // Precedence: built-ins < config < document-local #+LINK
      linkAbbreviations: mergeLinkAbbreviations([builtIns, configAbbreviations, documentAbbreviations])
    };
    if (opts.charts?.length) {
      context.chartsByTableLine = /* @__PURE__ */ new Map();
      context.chartsByBlockLine = /* @__PURE__ */ new Map();
      for (const chart of opts.charts) {
        if (chart.source.chartLine) context.chartsByBlockLine.set(chart.source.chartLine, chart);
        else context.chartsByTableLine.set(chart.source.line, chart);
      }
    }
    if (opts.pluginRenders?.length) {
      context.pluginRendersByBlockLine = new Map(opts.pluginRenders.map((render) => [render.source.line, render]));
    }
    if (includeHeadlineData) {
      const anchors = buildHeadlineAnchors(doc, {
        includeToc: opts.includeToc,
        includeTocDepth: opts.includeTocDepth,
        includeHeadlineNumbers: opts.includeHeadlineNumbers,
        includeHeadlineNumberDepth: opts.includeHeadlineNumberDepth
      });
      tocItems = anchors.items;
      context.headlineIds = anchors.headlineIds;
      context.headlineSlugIds = anchors.headlineSlugIds;
      context.headlineNumbers = anchors.headlineNumbers;
    }
    return { context, tocItems };
  }
  function renderMainBody(opts) {
    const appDocument = opts.context.profile === "app";
    const split = appDocument ? splitAppFileProperties(opts.doc.children) : { properties: [], body: opts.doc.children };
    const body = renderNodes(split.body, opts.context);
    const fileProperties = appDocument ? renderAppFileProperties(split.properties, opts.context) : "";
    const tocHtml = opts.includeToc ? renderToc(opts.tocItems) : "";
    const documentHeader = opts.includeDocumentHeader ? renderDocumentHeader({ title: opts.title, subtitle: opts.subtitle }) : "";
    return [documentHeader, fileProperties, tocHtml, body].filter((segment) => String(segment || "").trim().length > 0).join("\n");
  }
  function renderDocumentHtml(opts) {
    const headMetaSection = renderHeadMetaSection(opts.metadata);
    const headExtraSection = renderHeadExtraSection(
      opts.metadata,
      opts.headIncludes,
      opts.includeDocumentHtml
    );
    const headStyleSection = renderHeadStyleSection({
      stylesheets: opts.stylesheets,
      includeDefaultStyle: opts.includeDefaultStyle,
      defaultStyle: [
        DEFAULT_DOCUMENT_STYLE,
        opts.mainBody.includes('class="org2-image-figure"') ? DOCUMENT_IMAGE_STYLE : "",
        opts.includeToc ? DOCUMENT_TOC_STYLE : ""
      ].filter(Boolean).join("\n")
    });
    const preambleSection = opts.preambleHtml ? `${opts.preambleHtml}
` : "";
    const postambleSection = opts.postambleHtml ? `${opts.postambleHtml}
` : "";
    const compatOpen = opts.compatContentWrapper ? COMPAT_CONTENT_OPEN : "";
    const compatClose = opts.compatContentWrapper ? COMPAT_CONTENT_CLOSE : "";
    const compatStyleSection = opts.compatContentWrapper ? COMPAT_CONTENT_STYLE_SECTION : "";
    return `<!doctype html>
<html lang="${escapeAttr(opts.language)}">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>${escapeHtml(opts.title)}</title>
${headMetaSection}${headExtraSection}${headStyleSection}${compatStyleSection}</head>
<body>
${compatOpen}<main class="org2-document">
${preambleSection}${opts.mainBody}
</main>
${compatClose}${postambleSection}</body>
</html>
`;
  }
  function renderOrgDocumentToHtml(doc, opts = {}) {
    const title = resolveTitle(doc, opts.title, opts.sourcePath);
    const metadata = collectKeywordMetadata(doc);
    const renderOptions = resolveDocumentRenderOptions(doc, opts);
    const { context, tocItems } = buildDocumentRenderContext(doc, {
      includeToc: renderOptions.includeToc,
      includeTocDepth: renderOptions.includeTocDepth,
      includeHeadlineNumbers: renderOptions.includeHeadlineNumbers,
      includeHeadlineNumberDepth: renderOptions.includeHeadlineNumberDepth,
      rewriteFileLinks: opts.rewriteFileLinks,
      linkAbbreviations: opts.linkAbbreviations,
      linearTeam: opts.linearTeam,
      nativeInternalLinks: opts.nativeInternalLinks,
      profile: opts.profile,
      charts: opts.charts,
      pluginRenders: opts.pluginRenders
    });
    const mainBody = renderMainBody({
      doc,
      context,
      includeToc: renderOptions.includeToc,
      tocItems,
      includeDocumentHeader: renderOptions.includeDocumentHeader,
      title,
      subtitle: metadata.subtitle
    });
    const html = renderDocumentHtml({
      title,
      language: metadata.language || "en",
      metadata,
      headIncludes: [
        ...opts.headIncludes || [],
        opts.profile === "publish" ? `<style id="org2-publish-document-style">
${APP_DOCUMENT_STYLE}${renderOptions.includeToc ? `
${DOCUMENT_TOC_STYLE}` : ""}
</style>` : "",
        opts.charts?.length && opts.profile !== "app" && opts.profile !== "publish" ? `<style id="org2-chart-style">
${DOCUMENT_CHART_STYLE}
</style>` : ""
      ].filter(Boolean),
      stylesheets: opts.stylesheets,
      includeDefaultStyle: opts.profile === "publish" ? false : opts.includeDefaultStyle,
      includeToc: renderOptions.includeToc,
      mainBody,
      preambleHtml: String(opts.preambleHtml || "").trim(),
      postambleHtml: String(opts.postambleHtml || "").trim(),
      compatContentWrapper: opts.compatContentWrapper,
      includeDocumentHtml: opts.profile !== "app"
    });
    return { html, title, metadata };
  }
  function renderOrgDocumentToAppHtml(doc, opts = {}) {
    const customCss = String(opts.customCss || "").trim();
    const customStyle = customCss ? `<style id="org2-app-user-style">
${customCss.replace(/<\/style/gi, "<\\/style")}
</style>` : "";
    return renderOrgDocumentToHtml(doc, {
      title: opts.title,
      sourcePath: opts.sourcePath,
      includeDefaultStyle: false,
      includeToc: false,
      includeHeadlineNumbers: false,
      includeDocumentHeader: true,
      rewriteFileLinks: false,
      headIncludes: [
        `<meta name="org2-document-kind" content="${isPresentationDocument(doc) ? "slides" : "document"}" />`,
        `<style id="org2-app-document-style">
${APP_DOCUMENT_STYLE}
</style>`,
        `<script id="org2-app-document-script">
${APP_DOCUMENT_SCRIPT}
<\/script>`,
        customStyle
      ].filter(Boolean),
      linkAbbreviations: opts.linkAbbreviations,
      linearTeam: opts.linearTeam,
      nativeInternalLinks: opts.nativeInternalLinks,
      profile: "app",
      charts: opts.charts,
      pluginRenders: opts.pluginRenders
    });
  }

  // src/mobileDocument.ts
  function inlineText(nodes) {
    return nodes.map((node) => node.type === "Text" ? node.value : node.type === "Emphasis" ? node.content : node.type === "Link" ? node.descriptionRaw ?? node.targetRaw : "raw" in node ? node.raw : "").join("");
  }
  function visibleText(nodes) {
    return nodes.map((node) => {
      switch (node.type) {
        case "Paragraph":
          return inlineText(node.children);
        case "Headline":
          return `${inlineText(node.title)} ${visibleText(node.children)}`;
        case "List":
          return visibleText(node.items);
        case "ListItem":
          return `${inlineText(node.descriptionTag ?? [])} ${visibleText(node.children)}`;
        case "Text":
          return node.value;
        case "SrcBlock":
          return node.bodyRaw;
        case "Table":
          return node.rows.filter((row) => row.type === "TableRow").map((row) => row.cells.map((cell) => inlineText(parseInlinesFromText(cell))).join(" ")).join("\n");
        case "Block":
          return ["quote", "verse", "example", "center"].includes(node.kind) ? node.bodyRaw : "";
        default:
          return "";
      }
    }).join("\n").trim();
  }
  function parse(source, sequences = []) {
    const normalized = source.replace(/\r\n?/g, "\n");
    const options = { sourceRanges: true, todoSequences: documentTodoSequences(
      normalized,
      sequences.length ? parseTodoSequenceDefinitions(sequences) : []
    ) };
    try {
      return parseOrgToCanonicalAst(normalized, options);
    } catch (error) {
      if (!(error instanceof Error) || !error.message.includes("Unsupported construct: tab character")) throw error;
      return parseOrgToCanonicalAst(normalized.replace(/\t/g, "  "), options);
    }
  }
  function property(nodes, key) {
    const drawer = nodes.find((node) => node.type === "PropertyDrawer");
    return drawer?.type === "PropertyDrawer" ? drawer.properties.find((p) => p.key.toUpperCase() === key)?.value ?? "" : "";
  }
  function resolveEntry(source, path, selector, sequences = []) {
    const doc = parse(source, sequences);
    const entries = indexDocument(source, path, sequences);
    if (!selector) return { entry: null };
    let matches = [];
    if (selector.startsWith("id:")) {
      matches = entries.filter((entry) => entry.nodeID === selector.slice(3));
    } else if (/^[1-9][0-9]*$/.test(selector)) {
      const line = Number(selector);
      if (line > source.replace(/\r\n?/g, "\n").split("\n").length) throw new Error("The linked line no longer exists.");
      matches = entries.filter((entry) => entry.line > 0 && entry.line <= line).slice(-1);
      if (!matches.length) return { entry: null };
    } else if (selector.startsWith("#")) {
      let walk2 = function(nodes) {
        for (const node of nodes) if (node.type === "Headline") {
          if (property(node.children, "CUSTOM_ID") === selector.slice(1)) lines.add(node.sourceRange.startLine);
          walk2(node.children);
        }
      };
      var walk = walk2;
      const lines = /* @__PURE__ */ new Set();
      walk2(doc.children);
      matches = entries.filter((entry) => lines.has(entry.line));
      if (property(doc.children, "CUSTOM_ID") === selector.slice(1)) matches.push(entries[0]);
    } else {
      const title = selector.replace(/^\*+\s*/, "");
      matches = entries.filter((entry) => entry.line > 0 && entry.title === title);
    }
    if (matches.length !== 1) throw new Error(matches.length ? "This entry link is ambiguous." : "The linked entry no longer exists. Refresh the corpus and try again.");
    return { entry: matches[0].line === 0 ? null : matches[0] };
  }
  function indexDocument(source, path, sequences = []) {
    const doc = parse(source, sequences);
    const title = doc.children.find((node) => node.type === "KeywordLine" && node.keyRaw.toUpperCase() === "TITLE");
    const pageTitle = title?.type === "KeywordLine" ? title.valueRaw.trim() : path.split("/").pop() ?? path;
    const entries = [{ path, title: pageTitle, parent: "", line: 0, nodeID: property(doc.children, "ID"), body: visibleText(doc.children.filter((n) => n.type !== "Headline")) }];
    function walk(nodes, parents) {
      for (const node of nodes) {
        if (node.type !== "Headline") continue;
        const title2 = inlineText(node.title);
        const drawer = node.children.find((n) => n.type === "PropertyDrawer");
        const nodeID = drawer?.type === "PropertyDrawer" ? drawer.properties.find((p) => p.key.toUpperCase() === "ID")?.value ?? "" : "";
        entries.push({
          path,
          title: title2,
          parent: parents.join(" \u203A "),
          line: node.sourceRange.startLine,
          nodeID,
          body: visibleText(node.children.filter((n) => n.type !== "Headline"))
        });
        walk(node.children, [...parents, title2]);
      }
    }
    walk(doc.children, [pageTitle]);
    return entries;
  }
  function renderDocument(source, path, line = 0, nodeID = "", sequences = [], expectedTitle = "") {
    const doc = parse(source, sequences);
    let selected;
    function walk(nodes) {
      for (const node of nodes) {
        if (node.type !== "Headline") continue;
        const drawer = node.children.find((n) => n.type === "PropertyDrawer");
        const id = drawer?.type === "PropertyDrawer" ? drawer.properties.find((p) => p.key.toUpperCase() === "ID")?.value : void 0;
        if (nodeID ? id === nodeID : node.sourceRange?.startLine === line) selected = node;
        walk(node.children);
      }
    }
    if (line > 0) {
      walk(doc.children);
      if (!selected || !nodeID && expectedTitle && inlineText(selected.title) !== expectedTitle) throw new Error("This entry changed. Refresh search and try again.");
    }
    const rendered = renderOrgDocumentToAppHtml(selected ? { ...doc, children: [selected] } : doc, {
      sourcePath: path,
      nativeInternalLinks: true,
      title: selected ? inlineText(selected.title) : void 0,
      customCss: "body{padding:16px;margin:0;max-width:none} .org2-document-title::before,.org2-headline-summary::before,.org2-headline-summary>h1::before,.org2-headline-summary>h2::before,.org2-headline-summary>h3::before,.org2-headline-summary>h4::before,.org2-headline-summary>h5::before,.org2-headline-summary>h6::before{content:none} .org2-headline{border-left:0}"
    });
    const policy = `<meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; img-src data:; script-src 'none'">`;
    return { title: rendered.title, html: rendered.html.replace("<head>", `<head>${policy}`) };
  }
  return __toCommonJS(mobileDocument_exports);
})();
