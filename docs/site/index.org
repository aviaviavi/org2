#+TITLE: Org2
#+SUBTITLE: Plain-text knowledge for humans and agents working together

#+BEGIN_EXPORT html
<section class="org2-hero">
  <p class="org2-hero-lede">Org2 keeps notes, tasks, sources, and agent work in plain files. Humans can write and plan naturally; agents get enough structure to query the graph, track work, cite sources, and promote useful output without losing the trail.</p>
  <div class="org2-hero-points" aria-label="Org2 value summary">
    <div><strong>Local files.</strong><span>Readable text on disk, not a hosted workspace.</span></div>
    <div><strong>Real planning.</strong><span>TODOs, dates, tags, agendas, and work views come from the same corpus.</span></div>
    <div><strong>Scoped agent access.</strong><span>Encrypt individual subtrees to the specific people, devices, or agents allowed to read them.</span></div>
  </div>
</section>
#+END_EXPORT

Org2 is an early OSS project for turning a folder of notes, transcripts, tasks, links, and metadata into a corpus you can search, plan from, lint, refactor, hand to agents, and publish. Because the corpus is just local files, sensitive notes can stay end-to-end encrypted with keys you control, while selected subtrees can be encrypted to a particular agent when you want it to help.

#+BEGIN_EXPORT html
<section class="org2-sample" aria-labelledby="org2-sample-title">
  <div class="org2-sample-intro">
    <h2 id="org2-sample-title">What an Org2 file looks like</h2>
    <p>One plain-text document can carry notes, tasks, dates, links, code, source provenance, and AI handoff boundaries while staying readable in any editor.</p>
  </div>
  <div class="org2-sample-layout">
    <aside class="org2-sample-callouts org2-sample-callouts-left" aria-label="Document structure notes">
      <div style="--callout-top: 1.1rem"><strong>Metadata</strong><span>Title, file tags, and stable IDs make the file queryable.</span></div>
      <div style="--callout-top: 7.8rem"><strong>Agenda</strong><span>TODO state, schedule, deadline, effort, and tags feed planning views.</span></div>
      <div style="--callout-top: 40.4rem"><strong>Provenance</strong><span>Generated or imported work keeps a trail back to sources.</span></div>
    </aside>
    <figure class="org2-sample-file">
      <figcaption>notes/product-launch.org2</figcaption>
      <pre aria-label="Example Org2 document"><code><span class="org2-syntax-keyword">#+title:</span> Product launch workspace
<span class="org2-syntax-keyword">#+filetags:</span> <span class="org2-syntax-tag">:launch:q3:</span>

<span class="org2-syntax-heading">*</span> <span class="org2-syntax-todo">TODO</span> Ship beta checklist <span class="org2-syntax-tag">:product:</span>
  <span class="org2-syntax-planning">SCHEDULED:</span> <span class="org2-syntax-date">&lt;2026-06-01 Mon&gt;</span>
  <span class="org2-syntax-planning">DEADLINE:</span>  <span class="org2-syntax-date">&lt;2026-06-12 Fri&gt;</span>
  <span class="org2-syntax-drawer">:PROPERTIES:</span>
  <span class="org2-syntax-prop">:ID:</span> launch-beta-checklist
  <span class="org2-syntax-prop">:EFFORT:</span> 3h
  <span class="org2-syntax-drawer">:END:</span>

  - [X] Draft invite copy
  - [ ] Confirm onboarding path with <span class="org2-syntax-link">[[id:design-review][design review]]</span>
  - [ ] Publish release notes from <span class="org2-syntax-link">[[file:release-notes.org2][release notes]]</span>

<span class="org2-syntax-heading">**</span> Meeting notes
  Decisions:
  - Keep beta small until support docs are complete.
  - Track follow-ups in the same file as the project plan.

<span class="org2-syntax-heading">**</span> Source query for docs
  <span class="org2-syntax-block">``` sh</span>
  npm run org2 -- query --tag launch --format json
  <span class="org2-syntax-block">```</span>

<span class="org2-syntax-heading">*</span> AI draft summary <span class="org2-syntax-tag">:draft:</span>
  <span class="org2-syntax-drawer">:PROPERTIES:</span>
  <span class="org2-syntax-prop">:ORG2_ARTIFACT_ROLE:</span> draft
  <span class="org2-syntax-prop">:ORG2_PROVENANCE:</span> raw/calls/beta-kickoff.org2
  <span class="org2-syntax-drawer">:END:</span>

  The beta should emphasize local files, agenda workflows,
  and source-backed AI summaries.</code></pre>
    </figure>
    <aside class="org2-sample-callouts org2-sample-callouts-right" aria-label="Document feature notes">
      <div style="--callout-top: 11.8rem"><strong>Properties</strong><span>IDs, effort, and roles are first-class data, not comments.</span></div>
      <div style="--callout-top: 18.6rem"><strong>Links</strong><span>File links and ID links stay plain text but resolve through the corpus graph.</span></div>
      <div style="--callout-top: 30.9rem"><strong>Code</strong><span>Commands and examples live beside the notes they explain.</span></div>
    </aside>
  </div>
</section>
#+END_EXPORT

* The idea

Org2 combines four useful traditions:

- the portability and low friction of plain-text notes,
- the structured programmability of Org-style TODOs, links, dates, tags, and properties,
- the Zettelkasten habit of growing knowledge through small linked notes that compound into a graph,
- the provenance and workflow boundaries agents need to work safely in a real knowledge base.

The result is a shared working graph: notes, tasks, plans, sources, generated drafts, and durable knowledge stay connected while still living in ordinary files. Raw imports can live in =raw/=, durable notes in the normal corpus, generated drafts in =views/=, and compiled indexes in =compiled/=. Agents can automate the pipeline, while humans still have natural places to inspect or steer when judgment matters.

* What you can do with it

- *Plan from the same files you write in*: build agendas and work views from TODOs, deadlines, scheduled dates, tags, and metadata across a whole workspace.
- *Track work without a separate app*: keep project notes, decisions, follow-ups, and next actions connected in the same graph.
- *Ask better questions of your own notes*: query headings, links, tasks, IDs, dates, and provenance instead of grepping a pile of text.
- *Use AI without losing the trail*: keep transcripts and imports as sources, generate drafts with citations, and promote outputs into lasting notes when they are ready.
- *Keep links from rotting*: generate IDs, inspect backlinks, linkify likely references, and lint graph health.
- *Publish when useful*: turn the same source tree into HTML docs, a digital garden, or project documentation.


* Private-by-default agent collaboration

A useful agent workspace should not require dumping every private note into a hosted database. Org2's crypt workflow keeps encryption at the subtree boundary: one entry can be readable only by you, another can be encrypted to your phone and laptop, and a third can add an agent public key for a specific task.

That gives you a practical local-first access model:

- plaintext lives in your editor only when you decrypt it,
- encrypted notes remain ordinary files that can sync through Git, iCloud, Syncthing, or any other file transport,
- each subtree declares the recipients that should be able to open it,
- agents only get access to the encrypted entries whose recipient list includes their key.

It is not a replacement for good operational security, but it is a strong default for AI-assisted personal knowledge work: self-hosted, inspectable, GPG-based, and scoped per note instead of all-or-nothing.

* Screenshots

#+BEGIN_EXPORT html
<div class="org2-shot-grid">
  <figure><img src="assets/screenshots/graph.png" alt="Org2 knowledge graph showing a selected neighborhood of linked notes" loading="lazy" /><figcaption>Knowledge graph: explore linked notes, backlinks, and source relationships from the same corpus.</figcaption></figure>
  <figure><img src="assets/screenshots/agenda.png" alt="Org2 interactive agenda terminal UI" loading="lazy" /><figcaption>Agenda TUI: plan from tasks, deadlines, tags, and corpus metadata.</figcaption></figure>
  <figure><img src="assets/screenshots/basic.png" alt="A basic Org2 file open in Visual Studio Code" loading="lazy" /><figcaption>VS Code editing: plain files with TODOs, IDs, links, tags, and dates.</figcaption></figure>
  <figure><img src="assets/screenshots/meeting.png" alt="An Org2 meeting transcript with AI summary and action items open in Visual Studio Code" loading="lazy" /><figcaption>AI workflow: transcript, generated summary, and action items stay linked in source.</figcaption></figure>
</div>
#+END_EXPORT

* How it works

Org2 is a compiler/runtime for plain-text knowledge:

- *raw* captures source material,
- *notes* hold reviewed knowledge in normal editable files,
- *compiled* stores resolved semantics such as agenda state, graph edges, provenance, and artifact roles,
- *views* hold derived artifacts like agendas, work plans, diagnostics, AI drafts, and backlinks,
- *publish* turns selected material into websites or shareable reports.

The practical payoff: one source tree can drive daily planning, work tracking, editor features, corpus-health checks, AI handoffs, and a public website without locking the work into one app.

* Who it is for

Org2 is a good fit if you want:

- local notes that remain useful outside any single app,
- planning and work tracking that live beside the notes,
- AI workflows with citations and clear promotion paths,
- a queryable personal or team knowledge base,
- automation that can read and write structured notes safely,
- publishing from the same corpus you use privately.

It is early alpha, but already practical for CLI + VS Code workflows.

* What Org2 is not trying to be

- A hosted notes service.
- An Emacs-only system.
- A byte-for-byte clone of GNU Org internals.
- A tool that hides where generated knowledge came from.

* Start here

- [[file:getting-started.org][Getting started]]
- [[file:features.org][Features]]
- [[file:corpus-flow.org][Corpus flow]]
- [[file:ai-processing-architecture.org][AI processing architecture]]
- [[file:openclaw-knowledge-layer.org][OpenClaw knowledge layer]]
- [[file:org2-vs-markdown.org][Org2 vs Markdown]]
- [[file:org2-vs-obsidian.org][Org2 vs Obsidian]]
- [[file:org2-vs-org-mode.org][Org2 vs Org Mode]]

Reference post: [[https://avi.press/posts/2024-01-15-standalone-org.html][Standalone Org rationale]]

* Repository

- [[https://github.com/aviaviavi/org2][github.com/aviaviavi/org2]]
