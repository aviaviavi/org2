#+TITLE: Features

#+BEGIN_EXPORT html
<div class="org2-features-page">
  <section class="org2-page-intro">
    <p>Org2 is a local-first context compiler for notes, tasks, meetings, data, and agent work. Ordinary files remain the source of truth while agendas, graphs, cited context, review queues, editor behavior, and published artifacts come from one shared semantic model.</p>
    <div class="org2-page-summary" aria-label="Org2 feature summary">
      <div><strong>Files stay canonical.</strong><span>Readable Org and Org2 files work in editors, Git, scripts, and normal sync tools.</span></div>
      <div><strong>Semantics are shared.</strong><span>Planning state, links, provenance, and review status mean the same thing in every client.</span></div>
      <div><strong>Derived work stays inspectable.</strong><span>Views, reports, agent runs, and published output remain connected to their sources.</span></div>
    </div>
  </section>

  <section class="org2-feature-section" id="compiler-loop" aria-labelledby="compiler-loop-title">
    <div class="org2-feature-heading">
      <h2 id="compiler-loop-title">How Org2 works</h2>
      <p>The parser turns plain files into a corpus model that each editor, app, agent, and publishing tool can use.</p>
    </div>
    <div class="org2-compiler-flow" aria-label="Org2 compiler workflow">
      <article><strong>Capture</strong><p>Write notes, TODOs, meetings, links, data records, and agent handoffs as ordinary text.</p></article>
      <article><strong>Parse</strong><p>Org2 reads headings, properties, timestamps, blocks, tables, and source ranges.</p></article>
      <article><strong>Connect</strong><p>IDs, backlinks, planning state, provenance, approvals, and freshness become shared semantics.</p></article>
      <article><strong>Use</strong><p>Build agendas, diagnostics, graphs, context packs, editor features, reports, and sites.</p></article>
    </div>
    <p class="org2-feature-note">Those semantics are shared by the Mac app, VS Code, CLI tools, agent integrations, and publishing. Each client reads the same corpus instead of inventing its own interpretation of the files.</p>
  </section>

  <section class="org2-feature-story" id="planning-knowledge" aria-labelledby="planning-knowledge-title">
    <div class="org2-feature-copy">
      <h2 id="planning-knowledge-title">Planning and linked notes</h2>
      <p>TODO state, schedules, deadlines, priorities, effort, tags, IDs, aliases, and links remain readable in the note. Org2 turns them into focused agenda views and a navigable knowledge graph.</p>
      <ul>
        <li>Agenda across many files, with date windows, filters, grouping, and real-world TODO vocabularies.</li>
        <li>Stable IDs, backlinks, dailies, node creation, link previews, and graph-health checks.</li>
        <li>Preview-first mutations from the CLI and editors; writes require an explicit apply step.</li>
      </ul>
      <div class="org2-command" aria-label="Planning and graph commands">
        <span>Planning and graph commands</span>
        <pre class="org2-src language-sh"><code class="language-sh">org2 agenda --dir ~/notes --recursive --tui
org2 roam graph --dir ~/notes --recursive --format report</code></pre>
      </div>
      <p class="org2-feature-links"><a href="workflows.html">Everyday workflows</a><a href="tooling-reference.html">CLI reference</a></p>
    </div>
    <div class="org2-feature-media org2-feature-media-pair">
      <figure><img src="assets/screenshots/macos-workspace-agenda.png" alt="Org2 Workspace agenda with scheduled tasks, assignees, and linked note context" loading="lazy" /><figcaption>A native agenda over ordinary corpus files.</figcaption></figure>
      <figure><img src="assets/screenshots/graph.png" alt="Org2 knowledge graph connecting backlinks, source provenance, agenda, editor, and meeting nodes" loading="lazy" /><figcaption>The same IDs and links compiled into a graph.</figcaption></figure>
    </div>
  </section>

  <section class="org2-feature-story org2-feature-story-reverse" id="agent-work" aria-labelledby="agent-work-title">
    <div class="org2-feature-copy">
      <h2 id="agent-work-title">Agent work stays reviewable</h2>
      <p>Selected context, citations, plans, budgets, outputs, validation, clarifications, and approvals live in durable run and workflow records. People can inspect what happened before approving a consequential action.</p>
      <ul>
        <li>Bounded context packs carry file-and-line citations instead of an opaque prompt dump.</li>
        <li>Generated work lands in reviewable artifact zones before promotion into canonical notes.</li>
        <li>One pending-decision queue combines run-bound approvals with standalone file-backed approvals; run decisions preserve one native ID and immutable review-material fingerprint across CLI, Mac, mobile, and OpenClaw, and rejecting or canceling a required approval cancels its originating run instead of leaving a stale blocker.</li>
        <li>Recurring workflow executions roll up under stable logical work while retaining separate scheduler attempts, and event/fresh-path gates skip unchanged expensive work.</li>
        <li>OpenClaw is the first native end-to-end integration; CLI, MCP, and plain files keep the model provider-independent.</li>
      </ul>
      <div class="org2-command" aria-label="Agent context and review commands">
        <span>Context and review commands</span>
        <pre class="org2-src language-sh"><code class="language-sh">org2 agent context --query "launch risks" --dir ~/notes --recursive
org2 approvals --dir ~/notes --recursive --format json
org2 review list --status pending --dir ~/notes</code></pre>
      </div>
      <p class="org2-feature-links"><a href="agent-quickstart.html">Agent quickstart</a><a href="openclaw-knowledge-layer.html">OpenClaw integration</a></p>
    </div>
    <div class="org2-feature-media">
      <figure><img src="assets/screenshots/macos-workspace-runs-review.png" alt="Org2 Workspace Agent Work showing an approval-ready launch brief with outputs and review state" loading="lazy" /><figcaption>Agent Work keeps outcomes, artifacts, validation, and approval state together.</figcaption></figure>
    </div>
  </section>

  <section class="org2-feature-story" id="data-notebooks" aria-labelledby="data-notebooks-title">
    <div class="org2-feature-copy">
      <h2 id="data-notebooks-title">Data notebooks keep evidence close</h2>
      <p>Org2 notes can declare local or configured remote datasets, reusable SQL views, materialized results, freshness policy, and deterministic charts. Refresh is explicit; merely opening or publishing a document never runs a warehouse query.</p>
      <ul>
        <li>DuckDB-backed CSV, Parquet, JSON, table, ClickHouse, and Metabase inputs.</li>
        <li>Provenance-stamped result tables and source-aware chart definitions.</li>
        <li>Named external credential profiles; inline secrets are rejected.</li>
      </ul>
      <div class="org2-command" aria-label="Data notebook commands">
        <span>Inspect or materialize a result</span>
        <pre class="org2-src language-sh"><code class="language-sh">org2 query-data --file notes/readiness.org2 --inspect
org2 query-data --file notes/readiness.org2 --results weekly --apply</code></pre>
      </div>
      <p class="org2-feature-links"><a href="language-reference.html#local-datasets-and-sql-views">Notebook syntax</a><a href="tooling-reference.html">Query-data options</a></p>
    </div>
    <div class="org2-feature-media">
      <figure><img src="assets/screenshots/macos-workspace-data.png" alt="Org2 Workspace rendering a launch readiness data notebook with a materialized table and chart" loading="lazy" /><figcaption>Materialized rows, freshness metadata, and visualization remain connected to the source note.</figcaption></figure>
    </div>
  </section>

  <section class="org2-feature-story org2-feature-story-reverse" id="meetings-context" aria-labelledby="meetings-context-title">
    <div class="org2-feature-copy">
      <h2 id="meetings-context-title">Meetings become source-backed context</h2>
      <p>The macOS Workspace can record or import a meeting, keep audio and transcripts local, and render summaries, decisions, and follow-ups from ordinary files. Those records can then feed agenda items, cited agent runs, entity pages, and reports.</p>
      <ul>
        <li>Local-first transcription with explicit artifact paths and status.</li>
        <li>Event and timeline metadata for connecting meetings, people, projects, and later work.</li>
        <li>Raw evidence, curated notes, generated drafts, and published output retain provenance.</li>
      </ul>
      <p class="org2-feature-links"><a href="editors-macos.html">macOS Workspace</a><a href="corpus-flow.html">Artifact flow</a></p>
    </div>
    <div class="org2-feature-media">
      <figure><img src="assets/screenshots/macos-workspace-meetings.png" alt="Org2 Workspace meeting view with transcript metadata, summary, decisions, and action items" loading="lazy" /><figcaption>A meeting record keeps its transcript, decisions, and follow-ups together.</figcaption></figure>
    </div>
  </section>

  <section class="org2-feature-section" id="external-sources" aria-labelledby="external-sources-title">
    <div class="org2-feature-heading">
      <h2 id="external-sources-title">Slack and Notion arrive through a review boundary</h2>
      <p>Corpus-declared source profiles orchestrate Peter Steinberger's <code>slacrawl</code> and <code>notcrawl</code> archives. The CLI and macOS Sources surface can inspect health, preview a bounded import, or sync and stage raw captures plus review-required Org2 packets on manual, interval, or daily schedules.</p>
    </div>
    <div class="org2-feature-grid">
      <article><h3>Local and inspectable</h3><p>Crawler databases, machine bindings, and credentials stay outside canonical notes. Staged JSON and Org2 files retain stable source IDs, timestamps, hashes, URLs, and provenance.</p></article>
      <article><h3>Review before promotion</h3><p>Imported material lands under configured raw and views zones. People and agents can redact, verify, and promote selected knowledge without treating an external mirror as canonical truth.</p></article>
      <article><h3>One shared runtime</h3><p>The Mac app calls the same <code>org2 source</code> commands available to scripts and agents, keeps optional Notion credentials in macOS Keychain, and catches up missed scheduled runs after wake or launch.</p></article>
    </div>
  </section>

  <section class="org2-feature-section" id="editors-publishing" aria-labelledby="editors-publishing-title">
    <div class="org2-feature-heading">
      <h2 id="editors-publishing-title">Editors and publishing use the same semantics</h2>
      <p>Use the native Workspace for richer review flows and live in-app talk PDF preview, VS Code for the most complete editing experience, the CLI for automation, LSP where the protocol fits, HTML publishing for selected outputs, and Beamer-compatible export for talks.</p>
    </div>
    <div class="org2-editor-strip">
      <figure><img src="assets/screenshots/basic.png" alt="An Org2 project note open in Visual Studio Code with syntax highlighting, tasks, properties, and ID links" loading="lazy" /><figcaption><strong>VS Code.</strong> Syntax, folding, agenda, capture, refiling, formatting, links, search, and planning commands.</figcaption></figure>
      <div class="org2-editor-cards">
        <article><strong>Shared editor intelligence</strong><p>Definitions, references, hover, completion, rename, semantic tokens, table-aware formatting, and file-link updates come from shared semantics.</p></article>
        <article><strong>Publishing</strong><p>Preview an Org/Org2 talk as a live PDF in the Mac app, export it as Beamer TeX or PDF, export one HTML file, or publish a recursive project with navigation, styles, rewritten links, and generated indexes. This documentation site is built by Org2.</p></article>
        <div class="org2-command" aria-label="Formatting and publishing commands">
          <span>Check formatting or preview a site</span>
          <pre class="org2-src language-sh"><code class="language-sh">org2 fmt --dir ~/notes --recursive --check
org2 publish docs-site --config org2.json --preview
org2 export beamer --file talk.org2 --pdf --apply</code></pre>
        </div>
      </div>
    </div>
    <p class="org2-feature-links org2-feature-links-centered"><a href="editors.html">Editor integrations</a><a href="getting-started.html">Getting started</a></p>
  </section>

  <section class="org2-feature-section" id="feature-inventory" aria-labelledby="feature-inventory-title">
    <div class="org2-feature-heading">
      <h2 id="feature-inventory-title">Current feature coverage</h2>
      <p>A scan-friendly inventory of the broader surface available today.</p>
    </div>
    <div class="org2-feature-card-grid">
      <article><h3>Plain-text core</h3><ul><li>Headings, drawers, properties, TODOs, planning, links, blocks, tables, timestamps</li><li>Capture, archive, refile, canonical formatting</li><li>HTML/site publishing plus Beamer-compatible presentation export</li><li>Source ranges and structured compiler output</li></ul></article>
      <article><h3>Knowledge + planning</h3><ul><li>Agenda CLI, JSON, TUI, VS Code, and Mac surfaces</li><li>IDs, backlinks, dailies, nodes, link insertion, graph reports</li><li>Cited search, query, entity pages, and context bundles</li><li>Linkify previews and review-only semantic suggestions</li></ul></article>
      <article><h3>Agents + review</h3><ul><li>Durable runs, workflows, plans, steps, budgets, comments, retries</li><li>Per-thread OpenClaw or ChatGPT-authenticated local Codex chat in the Mac app, with runtime-backed model and reasoning selectors</li><li>In-flight chat stays bound to its originating corpus while the visible workspace switches</li><li>Reversible active/settled chat history with shared inactivity policy</li><li>Mac-local read, preview, and apply tools that preserve unsaved editor drafts and exact per-turn diffs</li><li>Artifacts, validation, approvals, review queues, outcome records</li><li>OpenClaw lifecycle and schedule integration</li><li>MCP resources, prompts, tools, client discovery, and snapshots</li></ul></article>
      <article><h3>Data + provenance</h3><ul><li>Datasets, SQL views, materialized results, deterministic charts</li><li>Artifact roles, dependency graphs, freshness, and rebuild plans</li><li>Meeting, event-stream, timeline, and data-link records</li><li>Evaluation fixtures and sanitized workflow replay</li></ul></article>
      <article><h3>Editors + clients</h3><ul><li>Native macOS Workspace and mobile approval flows</li><li>VS Code commands plus shared LSP capabilities</li><li>Vim / Neovim and Emacs integration paths</li><li>CLI JSON for scripts and additional clients</li></ul></article>
      <article><h3>Safety + portability</h3><ul><li>Local files stay canonical and Git-reviewable</li><li>Preview-first mutations and explicit approval gates</li><li>GPG-backed per-subtree, multi-recipient encryption</li><li>Portable personal, shared, and project corpus identity</li></ul></article>
    </div>
  </section>

  <section class="org2-feature-cta" aria-labelledby="feature-cta-title">
    <h2 id="feature-cta-title">Where to start</h2>
    <p>CLI + VS Code is the simplest path today. Add the macOS Workspace when you want richer agenda, meeting, data, and agent-review surfaces.</p>
    <p class="org2-feature-links"><a href="getting-started.html">Getting started</a><a href="reference.html">Reference</a><a href="https://github.com/aviaviavi/org2">GitHub</a></p>
  </section>
</div>
#+END_EXPORT
