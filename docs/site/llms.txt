# Org2

> Org2 is a local-first knowledge compiler and runtime for Org-shaped plain text. Ordinary .org2 and .org files are canonical; agendas, graph data, context packets, reports, editor behavior, apps, and websites derive from the shared compiler/runtime.

Installed agents should begin with:

    org2 agent capabilities
    org2 --help
    org2 agent --help

The first command emits the machine-readable `org2:capabilities:v1` manifest. Mutating commands generally preview unless `--apply` is provided. Preserve citations, IDs, provenance, and review state; place generated work in `views/` or `compiled/` before promotion into canonical notes.

Portable corpus identity lives in the root `org2.json`. Use `org2 corpus show|validate|init` before mounting a corpus for team or agent use. `org2 workspace agenda|search` can combine read-only results from repeatable, explicitly named `--mount` paths and attaches corpus identity to every result. Writes remain scoped to the active corpus; an agent must not inherit all human-visible mounts by convenience.

Portable external-source intent may also live in `org2.json`, but credentials and machine paths must not. Use `org2 source list|doctor|bind|sync` to orchestrate the external `slacrawl` and `notcrawl` binaries. Bindings live outside the corpus under the machine's Org2 index home, so the Mac app and OpenClaw can invoke the same corpus profile with independent local authorization.

The native macOS Workspace also exposes a discoverable chat slash-command layer. Its local commands reuse shared search, backlink, lint, render, and publish semantics; agent-assisted `/brief` and `/summarize` receive selected source context with citations. When the live OpenClaw Gateway supports `commands.list`, runtime-native, skill, and plugin commands for the selected agent are merged into autocomplete and forwarded unchanged through `chat.send`; they do not use HTTP compatibility. Gateway progress intentionally exposed by the provider is stored with the assistant reply, while repetitive tool completion events are grouped into a compact work log. See the macOS Workspace reference for details.

OpenClaw is currently the deepest native adapter. The repository's `integrations/openclaw` lifecycle plugin records substantial main-agent turns, subagent executions, and cron executions as correlated durable runs. It preserves available provider/model and usage metadata, records terminal outcome summaries, leaves approval or clarification boundaries open, continues an approved workflow in its correlated chat session, and verifies the Mac app's selected portable corpus identity against its single configured write corpus. Other agents can use the same run/workflow files, cited retrieval, CLI JSON, and MCP surfaces.

Durable delegation uses generated run records under `.org2/runs/` and authored workflow files under the visible `workflows/` directory. Org2 continues to read the legacy `.org2/workflows/` location. Discover the interfaces with:

    org2 run --help
    org2 workflow --help
    org2 review list --dir CORPUS --json
    org2 mcp serve --dir CORPUS

Runs preserve goals, acceptance criteria, cited inputs, plans, risk, provider metadata, budgets, artifacts, approvals, validation, comments, and observable events. Workflow packages declare inputs, capabilities, writes, outputs, triggers, checks, approval boundaries, and compatibility. External MCP/connector data that becomes durable work should be stored as a provenance-bearing snapshot; credentials stay outside the corpus.
Agents must use `org2 run block RUN_ID --reason "Specific clarification or next action"`; reasonless blocked runs are rejected because they give the reviewer nothing actionable to answer.
Agents must complete durable runs with `org2 run complete RUN_ID --summary "What happened"`; use repeatable `--highlight` and `--next-action` flags when useful. The summary is the primary human-facing result, not a log line or a restatement of the goal.
Agents must not complete runs whose artifacts remain `review-required`. After a human decision, use `org2 run artifact-review RUN_ID ARTIFACT_ID --status reviewed|rejected --actor NAME`; for linked Org/Org2 files inside the corpus, the command updates both the run snapshot and artifact metadata.
Agents and runtime adapters should record observable execution metadata with `org2 run runtime RUN_ID --provider ID --model ID` plus available `--tokens-used`, `--cost-used-usd`, or `--elapsed-seconds` values. Credentials never belong in the run.

## Agent entry points

- https://org2.avi.press/agent-quickstart.html
- https://org2.avi.press/tooling-reference.html
- https://org2.avi.press/language-reference.html
- https://org2.avi.press/corpus-flow.html
- https://org2.avi.press/collaboration.html
- https://org2.avi.press/features.html
- https://org2.avi.press/editors-macos.html
- https://org2.avi.press/agentic-workspace-roadmap.html

## Source and specification

- https://github.com/aviaviavi/org2
- https://github.com/aviaviavi/org2/blob/main/spec/v0/SPEC.org
- https://github.com/aviaviavi/org2/blob/main/spec/v0/CONFORMANCE.md
